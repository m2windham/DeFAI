# DeFAI Roadmap

Adopted 2026-07-10, after a critical review of two external AI audits (an
engineering-focused executive summary and a strategic review). What survived
that review is below; what didn't (SSM/Mamba core swap, MoE gating, EWC,
differentiable-memory rewrite — all gradient-era machinery that would break
the online/no-backprop premise and the measured continual-learning advantage)
is deliberately absent. Full reasoning lives in the session notes and the
review discussion; the standing rules of engagement in `FABLE_HANDOFF.md`
still bind everything here.

## Strategic structure: Path A main, product forks downstream

- **This repository stays Path A**: the research line. One continuously
  running oscillator field that perceives, remembers, learns world structure,
  and generates — online, label-free, no gradients inside the mechanism.
  All discovery happens here, phase scripts remain the lab notebook.
- **Path B and later products are hard forks, not branches.** First fork: a
  deployable episodic-memory module (online ingestion, measured ~15%-forgetting
  continual learning, persistent state). Later forks: agent products built on
  capabilities as they are discovered. Each fork is a separate product
  repository.
- **Discoveries flow downstream through a versioned engine, never through
  git merges.** A result graduates from main to forks only when it has
  (1) a phase script, (2) pinned numbers in the regression harness, and
  (3) a release tag. Forks upgrade by bumping the engine version.
- **Fork gate for Path B** (event-driven, not a date): E1 + E2 + E3 done and
  Phase 26 landed — the earliest moment the engine is fast, persistent,
  calibration-robust, and regression-guarded.
  **Engineering gate OPEN as of 2026-07-13**: E1 (27 pinned checks), Phase 26
  (synthetic gate), E2 (unified fastpath, 13.0× at phase-23 corpus shape,
  both backends green), E3 (bitwise-lossless persistence) are all landed.
- **RELEASE HOLD — owner decision, 2026-07-15**: the engineering gate is
  necessary but NOT sufficient. Phase 33's readiness benchmark returned an
  honest NOT-SOTA verdict (organism ACC 0.665 vs 0.872 for a supervised
  prototype baseline on class-incremental split-digits), and the owner's
  standing bar is "at least SOTA / cost-effective or we are not ready."
  **No release tag is to be cut and no Path B repository created until the
  phase-33 gate passes.** The measured paths to closing the gap are already
  in phase 33's findings: (1) task-1 slot flooding at K=40 forces later
  classes to learn by slot drift — a slot-budget/eviction policy is needed
  (the pool-mode use-it-or-lose-it machinery is the natural home); (2) the
  frozen-label readout artifact (fixed in-phase with online label evidence)
  must become mechanism, not evaluation patch. These are product-critical
  fixes for the episodic-memory module itself, not benchmark tuning. Re-run
  the phase-33 ladder after each; the gate re-opens when the organism is at
  least SOTA or demonstrably cost-effective at equal accuracy.
  **Gate re-test run 2026-08-05 (T1.4, phase 33c — row 33c): both fixes
  folded in; organism+budget ACC 0.712 / FORG 0.169 vs the 0.872 bar
  (and replay at 0.913/0.105 tops the ladder). NOT SOTA, and the
  equal-accuracy cost-effectiveness branch is not claimable. The hold
  STANDS pending the owner's decision.**
  **Follow-on measurement 2026-08-05 (T1.5, phase 33d — row 33d): the
  remaining gap is mostly CAPACITY, not mechanism — at K=160 (evict=250,
  a protocol variation; the ladder pins K=40) the organism crosses the
  bar at ACC 0.900 / FORG 0.033, spending 12.1× the bar's state bytes.
  The cost-effectiveness branch stays unmet at every swept K (the bar
  delivers more accuracy per byte everywhere; cost-matched K=24 gives
  0.644 vs 0.872). The decision input is now priced: SOTA accuracy is
  buyable with capacity at a ~12× byte premium.**
  **Follow-on measurement 2026-08-06 (T1.8, phase 33g — row 33g): that
  premium was mostly representation width, and it is now cut. Lossless
  CSR transitions + a complex64 memory store give 3.85× fewer bytes at
  ZERO accuracy cost (max |delta task-accuracy| = 0.0000 across the K
  sweep and across five paired reseeds), so K=160's 0.900 costs 96.4KB
  instead of 371.2KB — 3.14× the bar rather than 12.1×, and inside
  1.08× replay's own footprint. The cost branch is still NOT met (the
  bar delivers 35.2 KB/ACC-point; the bar-crossing organism arm 107.1),
  and the lever that would have met it — low-rank factorization — is a
  measured negative: it does not survive reseeding. The hold STANDS;
  what changed is the price, not the verdict.**

## Demonstration & outreach track (decided 2026-07-14; feeds Path B adoption and sustainability)

Four public-demonstration concepts were triaged. All are honest by
construction — each renders capabilities that already have pinned harness
numbers; nothing is staged.

| # | Concept | Disposition |
|---|---|---|
| D1 — "It learns you" | Interactive page where the ORGANISM ingests the visitor's own interaction stream live: memories crystallize from their behavior in ~2 minutes, then it starts predicting their next move. E3 persistence makes "it will remember you if you come back" literally true. The interactive front door to D3. | **Earmarked** — first interactive demo to build |
| D2 — "It remembers becoming itself" | Feed the organism its own repo history (commits, 30 phases of lab notebook) as a perceptual stream; render its learned map of the research process that produced it. | **Marketing** — launch essay / announcement material |
| D3 — The living organism | One continuous organism life on a server, ingesting a public stream, its memories/world-graph/anomalies on a public page, E3 snapshots making the life unbroken across restarts ("born <date>, has never forgotten"). Donations = keeping it alive. | **Public-facing usage scenario** — with planned pivots of the ingestion feed: codebase/commit feeds, API/event feeds, etc. The feed adapter is the only new engineering; the organism needs nothing new |
| D4 — Self-lesion, live | Ablate the perceptual field on stage; reasoning continues (the phase-30/aphasia property, performed); restore from disk intact. | **Research first** — run as a proper phase (pre-registered lesion protocol + measured verdicts) at the appropriate point, demo second |

## Research track (this repo)

| Phase | Question | Status |
|---|---|---|
| 25 — decorrelation α-sweep | Is embedding correlation the whole cause of the pool-mode perception collapse (85/395 at scale)? Sweep the PPMI-SVD weighting `emb = U·S^α`, α ∈ {0.5, 0.25, 0}; report the >0.7-neighbor fraction, transitive-component count, and core-arm coverage per α. **Run 2026-07-10 on the live 547K-word corpus — result: PARTIAL, not full closure.** Neighbor fraction fell monotonically (23% → 13% → 9%) and core-arm coverage more than doubled (85 → 168 → 216 / 395, a 2.5× recovery) as α dropped to 0 — embedding correlation is confirmed as a real, measured cause. But even fully decorrelated, coverage plateaus at 216/395 (55%), well short of both full vocabulary and the plain-recipe arm's 378/395 (phase 23) — a large residual gap that decorrelation alone does not close. Verdict: correlation is *a* cause, not *the* cause; the acceptance-bar constants (0.7 fusion, 0.6 active_bar, the 0.8/√ anneal) are very likely an independent second cause, exactly as pre-registered for this outcome. Feeds directly into Phase 26. | `phase25_decorrelation_sweep.py` — done |
| 26 — percentile acceptance bars | Replace the absolute perception constants (0.7 fusion, 0.6 active_bar, the 0.8/√ anneal) with quantiles of the measured similarity distribution, generalizing the existing `s_hat` hook. Must reproduce phases 14/17/18 on synthetic near-orthogonal data before being believed on real text. Closes handoff open thread #1 as a mechanism and makes perception embedding-source-agnostic (a Path B requirement — products won't control their input embeddings). **Run 2026-07-13 — result: PASS on the synthetic gate, with one documented estimator lesson and one pre-registered real-text caveat.** The obvious same-word estimator (median best-match overlap in a settled-token buffer) fails beyond σ\*: best-of-n matching is fluctuation-dominated exactly where the bars matter (measured m1=0.585 at σ=0.3, truth 0.27) — the same single-comparison insufficiency that forced phase 17's pooling. The working estimator pools spectrally: with vocabulary rank < N the bottom covariance eigenvalues are pure noise floor; the estimator's finite-sample bias is removed by simulating pure noise at the same (n, N) — self-calibration, no data oracle. Measured s_cal 1.29/2.69 vs true 1.20/2.70; calibrated bars reproduce oracle coverage/junk at σ = 0, 0.2, 0.3 (0.96/0.37 vs oracle 1.00/0.34 at σ=0.3, inside tolerance). Ablation: calibrated s_hat is the load-bearing quantity — cal-shat alone matches cal-full, so the midpoint active_bar placement is redundant once s is measured (the oracle bar is itself s-derived). Cross-word distribution comes free and label-free from consecutive settled tokens (a saccade means the word changed). Caveats, pre-registered: gate mode at σ=0 trades junk (0.19 vs 0.00) for coverage (1.00 vs 0.88); the spectral floor needs vocabulary rank < N, so the real-text port (V ≫ N) must re-derive that arm. Candidate for that re-derivation: PR #19 (closed 2026-07-15 as superseded by this row's landed implementation) prototyped an in-stream `qcal` calibration whose pairwise-quantile estimator (cross-pattern ceiling from a top-plateau-decontaminated overlap quantile, same-pattern mode from recurrence best-matches) does NOT need the rank < N assumption, and its unfinished Part B was exactly the real-text run — start from that branch's `_calibrate_bars` before inventing a third estimator. | `phase26_percentile_bars.py` — done (synthetic gate); pinned in harness §7 |
| 27 — 5M-word scale run (T2.1) | 50–100 Gutenberg books, with phase 24's MI-vs-null + distinctness criteria wired into stage B. Pre-registered: does category structure sharpen (MI z up, distinctness-selected k stable or gracefully growing), and do the polysemy detections hold under tighter nulls? Depends on E2 for runtime. **Run started 2026-08-06** on a 42-book, 5.22M-raw-word corpus (title-verified at fetch to guard against ebook-id mismatch; 42 books chosen over 50–100 shorter ones to hit the 5M-word target, since the phase-23 8-book set averaged ~68K words/book). MIN_COUNT scaled proportionally (150→1500) to hold vocabulary in phase-23/24's computational regime (354 words) while giving ~8.8× more occurrences per word — the intended scale lever for both stage-B statistics and stage-C nulls. Stage A deliberately runs only phase 23's measured-winning arm (recruit=0.75, 3 epochs), not a 3-arm resweep — scope control, not part of either pre-registered question. Oracle POS V-measure cross-check dropped for runtime (nltk tagging at 5M tokens was the single slowest stage at 547K already); mechanism-only questions (P1, P2) are unaffected. In progress — see commit history / next update for the result. | `phase27_5m_word_scale_run.py` — in progress |
| 28 — polysemy vs grammatical context-sensitivity | The disentangling test: cluster each detected word's occurrences by context signature (prev/next category), ask whether the clusters land in *different* induced categories (lexical polysemy) or the same category with shifted successors (grammatical context-sensitivity). Evaluated against gold-POS occurrence entropy — oracle for evaluation only, per standing rules. Closes handoff threads: the phase-23 conflation residual *and* the POS precision/recall thread, in one run. Scope caveat pre-registered: same-POS polysemes (bank/bank) will be classed as context-sensitivity; the claim narrows to POS-level multi-role detection. **Run 2026-08-05/06 on a fresh 547K-word fetch of the phase-23 corpus (T2.3) — result: the decomposition works, the caveat holds exactly as predicted.** Re-ran phase 23's stage A and stage-C gain code verbatim, but with stage B swapped to `discover_categories_v2` on slot-level counts (the standing phase-21 category method; slot-level silhouette 0.006) rather than phase 23's word-level B2 k-means — so stages C/D condition on a DIFFERENT category variable than phase 23's own stage C did (coverage 378/395, k=3 categories). The 119-word gain-detected list is therefore the same population as phase 23's ~114 (handoff note) but not a bit-exact reproduction: the drift plausibly reflects the stage-B method swap (a deliberate choice, not noise) plus corpus-refetch variation. Multi-slot words take their highest-index slot's category (last-wins; documented in-script as arbitrary). The new test partitions each word's occurrences by predecessor category (the gain test's own conditioning variable, reused rather than re-derived) and compares each bucket's dominant successor category — a bigram-level proxy: occurrence-to-category routing itself is never computed, and the polysemy/context call has no measured null of its own (MIN_BUCKET=15 / DOM_MARGIN chosen in-phase, not pre-registered; a bootstrap on chance cross-bucket conflicts is the open follow-up): 18/119 (15%) words show a cross-bucket conflict (different dominant successor category) and are classed **polysemy** (long, fast, beautiful, big, red, best, rest, tell, ...); 101/119 (85%) keep one dominant successor category throughout and are classed **context-sensitivity**. Gold-POS (nltk, eval-only, never fed back): polysemy-classified words carry higher mean gold-POS entropy than context-sensitivity-classified words (0.533 vs 0.309 bits) — the intended split direction — but the separation is soft, not clean: several polysemy verdicts (big, last, body) turn out gold-monosemous (entropy 0, tagger-consistent single POS), i.e. real predecessor/successor-category conflicts that gold POS doesn't corroborate (context-sensitivity within one POS can still flip which coarse category dominates, since k=3 categories are coarser than 4-way universal POS). Whole-detector precision/recall against a minority-POS-tag gold-multirole definition: precision 0.336, recall 0.284 (tp=40/119 detected, 141/395 gold-positive) — expected and consistent with the pre-registered scope caveat, since the gain test's target (successor-distribution shift) is a broader, genuinely different quantity than gold POS-tag switching, not a proxy for it. 'right' (phase 21's headline word) lands in context-sensitivity here (gold-entropy 1.667, four POS tags) — a direct, measured instance of the pre-registered caveat: a real multi-POS word whose senses don't split into different k=3 categories. Verdict: **the disentangling test is a working decomposition of phase 23's conflated gain statistic, and the pre-registered scope caveat is now a measured property of the method (soft gold-POS separation, not zero, not clean) rather than an assumption.** | `phase28_polysemy_vs_context_sensitivity.py` — done |
| 30 — symbolic reasoning on the logic layer (out-of-sequence; spun off the 2026-07 logic/language architecture split, PR #20) | With transition learning extracted into a decoupled `TransitionGraph` behind a confidence-gated `EventBoundary` (the MIT/McGovern language/logic separation applied to the pipeline), test the decoupling's prediction: reasoning should run on the graph alone, field absent. Three ops added to the logic layer — `kstep` (multi-step inference), `rollout` (field-free imagination), `next_hops`/`plan` + `recall_directed` (planning; logic proposes the next hop, the field renders). **Run 2026-07-11 on a 6-regime hub-and-branch world (a real fork, so planning has a decision), truth used for eval only: k-step inference corr 0.995–0.999 vs permutation-null 99th pct ~0.45; rollout bigram corr 0.999 vs field recall 0.939 at ~20× speed; directed recall reaches every goal at exactly true shortest-path length (1.8 hops, 100%) vs 23.2 hops undirected.** Refactor itself is behavior-identical (demo bitwise, harness values unchanged). Next step designed but deferred: stable symbol IDs decoupled from slot indices (downstream scripts index org.P by slot). | `phase30_symbolic_reasoning.py` — done; pinned in harness §6 |
| 31 — lesion study (from demo triage D4) | Pre-registered self-lesion protocol: ablate the perceptual field (zero/noise the input coupling, damage xi) at graded severities and measure what survives on the logic layer — kstep inference, rollout structure, plan/goal-reaching — vs matched perception-side metrics, plus recovery after E3 restore. The quantitative version of the aphasia property phase 30 demonstrated qualitatively. Timing: at an appropriate point after Phase 27/28; the public demo version comes only after the phase script pins the numbers. | planned |
| 32 — self-consistent calibration at V ≥ N | Close phase 26's pre-registered caveat: the spectral estimator needs noise-only eigendirections, absent on rank-saturated (real-text) data. Method: fixed-point self-calibration — guess s, place bars, run the organism's own pooling, measure settled-token-vs-denoised-slot overlaps, invert the phase-17 overlap law, iterate; escalate on measurement failure (bars too tight = guess too low, itself a signal). **Run 2026-07-14 at V=60 > N=30 — result: PARTIAL.** Engages at all σ (the spectral method cannot run at all here); s_cal 0.20/1.74/3.48 vs true 0.30/1.20/2.70; calibrated-bar coverage within 0.06 of oracle everywhere — but misses the pre-registered bands (30% rel err, 0.05 coverage delta) at σ=0.2 (45%/0.06). Upward bias from contaminated slots persists through per-slot/upper-quartile refinements; a 1.7–2.1 oscillation at σ=0.2 is documented for the next attempt. The V ≥ N arm stays open. | `phase32_selfconsistent_calibration.py` — partial |
| 33 — industry-standard readiness benchmark | Owner's gate (2026-07-14): "at least SOTA / cost-effective or we are not ready" — measure it. Class-incremental split-digits, standard metrics (ACC/FORG), standard ladder (naive SGD, EWC, replay, joint oracle, growing prototypes, organism-on-numba). **Measured: organism ACC 0.665 / FORG 0.200 — 2× the gradient arms (SGD/EWC 0.32) so the continual-retention claim survives, but a supervised prototype baseline wins the raw benchmark outright (0.872, faster, smaller): the organism is NOT SOTA on class-incremental accuracy.** Two mechanism findings: task-1 slot flooding at K=40 makes later classes learn by slot drift (a slow forgetting channel, the FORG 0.20); frozen-label readout mis-scores drifted slots (ACC 0.25 artifact — fixed with online per-slot label evidence). Surviving differentiated claims: unsupervised representation + structure learning + bitwise persistence in one single-pass online mechanism. Public claims must be scoped to those, not benchmark wins. | `phase33_industry_baselines.py` — done; gate verdict recorded. Readout promoted to `label_readout.py` (T1.3, 2026-08-05): eval-side module, phase script consumes it bit-identically, pinned by `test_label_readout.py` |
| 33b — slot-budget/eviction under recruitment pressure (T1.2; closes phase 33's mechanism finding #1; opened+run 2026-08-05) | Stop task-1 slot flooding as MECHANISM, no task/label information anywhere: `perceive(evict=E)` makes the pool-mode use-it-or-lose-it staleness clock persistent organism state (`org.age`, E3-serialized; evict=0 reproduces prior behavior exactly) and adds eviction UNDER RECRUITMENT PRESSURE — a token more novel than 0.8× the recruit floor (the pool-mode weak-tail fraction: recruiting out of a freed slot costs more novelty than into a free one) with no free slot reclaims the least-established stale slot (argmin lifetime `count` among slots idle > E frames — consolidate's junk signal) through the existing recycle path (graph.retire + boundary.invalidate). **Measured on phase 33's exact organism arm (stream reproduced verbatim; baseline arm re-lands ACC 0.665 / FORG 0.200 and the flood: 40/40 slots after task 1, zero fresh recruits ever after). At the pre-registered window E=2000 the mechanism recruits fresh slots for later classes (14–29/task) but ACC FALLS to 0.511 (FORG 0.513) — the pre-registered recency risk materialized; negative recorded. The diagnosis is the row's finding: THE STALE POOL MUST CONTAIN THE PRESENT, OR EVICTION EATS THE PAST — with a long window only previous eras' slots are ever stale during a task, so count-ordering has nothing to choose from and the budget degenerates to a recency flush (era census [0,2,3,6,29]); with a short window the stream's own junk recruits go stale mid-task and enter the pool, count-ordering recycles the stream's own churn first, and established old-era prototypes survive. Sweep: every window in 100–750 beats baseline on BOTH axes; primary E=250 (post-hoc, mid-plateau, ~3× the measured live-slot revisit interval): ACC 0.712, FORG 0.169, era census [9,7,4,6,14], task-0 final accuracy 0.939 vs baseline's 0.732. E3 mid-run save→load→continue bitwise (dxi=dP=dage=0).** Pinned: harness §9 (flooded-bank two-world recovery is evict-load-bearing + bitwise clock round-trip), equivalence check 6 (plain and pool modes, both backends). T1.4 re-runs the full ladder once T1.1/T1.3 land. | `phase33b_slot_budget.py` — done; pinned in harness §9 |
| 33c — gate re-test: full ladder re-run (T1.4; the RELEASE HOLD's re-test; run 2026-08-05) | The complete phase-33 ladder, torch arms included, with the two hold-named fixes folded into the organism arm as mechanism: T1.2's slot budget (`perceive(evict=250)`, phase 33b's primary window) + T1.3's `LabelEvidenceReadout` with eviction invalidations forwarded at phase-script level; the evict=0 arm kept as reproduction anchor. T1.1 (hierarchical recall) is generation-side and has no role in this classification protocol — recorded in the docstring. **Measured: every numpy anchor reproduced EXACTLY — prototypes 0.872/0.021 (120 protos), organism evict=0 0.665/0.200 incl. the 40/40 flood with zero later recruits, ORGANISM+budget 0.712/0.169 with fresh recruits 11–14/later task and census [9,7,4,6,14]. Torch arms now on record: SGD 0.323/0.842, EWC 0.322/0.844, replay 0.913/0.105, joint\* 0.974/0.000. Gate verdict, honest: 0.712 vs the 0.872 supervised prototype bar — NOT SOTA; T1.2 closed 23% of the gap (0.207→0.160), cut FORG to 0.169, lifted task-0 final accuracy 0.732→0.939 (task 1 gives a little back, 0.464→0.393). Pre-registered clause missed, recorded: experience replay beats the organism on BOTH axes and tops the entire ladder, above even the prototype bar. Equal-accuracy cost-effectiveness not claimable (accuracy is not equal). Surviving claims stay scoped to the capability flags — unsupervised representation + structure learning + bitwise persistence in one single-pass online mechanism, now without the flooding ceiling.** OWNER DECISION POINT: bar not met; the RELEASE HOLD stands unless the owner re-scopes the gate to the cost/capability axes. | `phase33c_gate_retest.py` — done; gate verdict recorded |
| 33d — cost-matched capacity sweep (T1.5; prices the post-33c gap; opened+run 2026-08-05) | Is the 0.160 gate gap (0.712 vs the 0.872 prototype bar at the ladder's pinned K=40) a capacity artifact? Sweep K ∈ {40, 60, 80, 120, 160} at evict=250 + LabelEvidenceReadout on phase 33c's stream/scorers VERBATIM (the 33c module is imported, not copied; protocol-level only, no organism.py changes), plus evict=0 twins at every K (the staleness re-check: E=250 was tuned at K=40) and a cost-matched point — the largest K whose measured state fits the bar's 30.7KB (K=24). **Measured (anchors all EXACT: prototypes 0.872/120 protos, K=40 arms 0.665/0.200 and 0.712/0.169 census [9,7,4,6,14]): ACC rises monotonically — 0.712/0.735/0.837/0.854/0.900 — and CROSSES the bar at K=160 (0.900, FORG 0.033), so the gap IS mostly capacity; prediction (b)'s plateau never appeared (0.854 at K=120 is below 0.87 but still rising). The flood dissolves at K≥60 (task 0 recruits 53-54 slots) yet the bank still fills by tasks 1-3 at every K and eviction stays load-bearing (budget arm beats its evict=0 twin everywhere except K=120's −0.005: +0.238 at K=60, +0.076 at 80, +0.046 at 160; the evict=0 ladder is itself non-monotone, 0.497 at K=60 with census [53,7,0,0,0]); E=250 transfers across the sweep (window contingency untriggered). The K=40 task-1 regression (0.393, T1.7) heals with capacity: 0.726/0.881/0.905/0.964. Cost branch NOT met anywhere: bar 35.2 KB/ACC-pt vs organism 45.9→412.4 rising in K; cost-matched K=24 gives 0.644 vs 0.872; matched slot count (K=120 vs 120 protos) nearly matches accuracy (0.854 vs 0.872) at 7.8× the bytes — the byte gap is representation width (complex128 xi + dense K×K P), not memory count. Replay (committed 0.913 at ~89.6KB) still tops the ladder.** Verdict: raw-accuracy branch met only as a protocol variation (K=160, 12.1× bytes); cost branch unmet at every K; the pinned-K=40 ladder verdict (NOT SOTA) stands. Byte-efficiency levers named for any follow-on: representation width and P sparsity; T1.6 remains open as an independent readout lever. | `phase33d_capacity_sweep.py` — done; gap priced |
| 33e — readout geometry: eval-side decoder comparison (T1.6; opened+run 2026-08-05) | Does the 0.160 gate gap live partly in `LabelEvidenceReadout`'s blunt decoder (argmax-overlap slot -> majority label) rather than in memory content? Added richer EVAL-SIDE decoders over the SAME organism state (`label_readout.py`; labels still enter at the readout only, all decoders bitwise non-mutating, and every one reduces EXACTLY to argmax at m=1 — pinned in `test_label_readout.py` §4): `soft` (evidence-weighted vote over the top-m overlapping slots), `dist` (per-slot label DISTRIBUTIONS, count-normalized), `calib` (softmax(beta·overlap)-weighted distributions). Scored on the 33c protocol, both organism arms, each trained once and scored by all 15 configs (prediction is non-mutating, so this is exact). **Measured: argmax anchors EXACT (evict=0 0.665/0.200; evict=250 0.712/0.169). The seed-0 sweep looked positive on the budget arm — five configs cleared the pre-registered +0.02 threshold (calib-b8 +0.063 → ACC 0.775, dist-m2 +0.051, dist-m3, calib-b32, soft-m40) — and dead-null on the flooded evict=0 arm (best alternative +0.000, wide routing destructive to −0.443): geometry only helps where the slot population is healthy/mixed-era, never where it is drift-dominated. The pre-registered reseeding supplement then returned the honest negative: under full-protocol reseeds calib-b8 goes +0.063/+0.030/+0.088/−0.031/+0.029 (mean +0.036, SIGN FLIP at seed 3) and dist-m2 mean +0.027 with the same flip — at seed 3 argmax itself lands strongest (0.813; per-seed range 0.692–0.813), i.e. the gain is ANTI-CORRELATED with argmax's own strength: distribution decoders recover label mass when the evidence lands messy and give it back when it lands clean. Per the pre-registered survival rule (mean > +0.02 AND no sign flip) the claim does not survive: THE NULL STANDS — no eval-side decoder moves ACC by > 0.02 robustly; the memory content (K=40 = 4 slots/class vs the prototype bar's 120), not readout geometry, is the limit, and the lever is T1.5/mechanism, exactly as pre-registered. argmax stays the default (a flip would silently re-score the committed ladder).** Null pinned under CI: `test_label_readout.py` §5 (committed evict=0 protocol: best alternative ≤ argmax + 0.02, plus the 0.665 anchor tolerance-banded). | `phase33e_readout_geometry.py` — done; null pinned in test §5 |
| 33f — eviction ledger: the task-1 regression diagnostic (T1.7; run 2026-08-05) | Phase 33c's budget arm lifted task-0 retention 0.732→0.939 but dropped task-1 final accuracy 0.464→0.393 — explain it, discriminating three pre-registered hypotheses (H1 task-1 slots evicted disproportionately → count-normalized victim choice; H2 labels stale in the readout → invalidation scope; H3 intrinsically hardest → no fix). Instrumentation: a debug-only eviction ledger, `perceive(evict_debug=...)` in organism.py + fastpath.py, both backends — each pressure eviction logs (frame, slot, count, age) before mutating; era attribution reconstructed script-side so the mechanism stays task-blind. Equivalence proven, gate-style: all 33c anchors EXACT with the ledger attached, ledger on/off bitwise identical (state + accuracy matrix), numpy/numba ledgers identical row-for-row (171 evictions), harness 31/31 both backends + equivalence suite green with the flag off. **Verdict: H3 — the regression is NOT an eviction pathology; H1 and H2 ABSENT; the victim-choice rule is exonerated (123/171 evictions are same-task self-churn, era-1-born slots suffered only 4 cross-era evictions vs era-0's 31, and the budget arm holds task-1 content BETTER than baseline at every snapshot, 0.566 vs 0.535 final). Task 1 acquires fine (A[1,1] 0.929/0.881) and its tokens are the stream's most novel (never-novel 0.06 vs 0.12–0.23) — it is the hardest task to RETAIN: 42 of the budget's 51 task-1 errors (45/45 baseline) are queries captured by slots whose content genuinely belongs to other classes, the K=40 interference floor that made 0.464 the ladder's low before eviction existed. The net −0.071 delta is READOUT GEOMETRY: 9 errors on two slots still holding task-1 content but carrying foreign labels (slot 33: reborn on a task-1 token, correctly relabeled, then out-voted +13 class-5 votes — majority collapse discards its 8 surviving class-2 votes; slot 9: never evicted, zero task-1 votes ever, drifted into class-2 content and wins the argmax by 0.061 over the healthy majority-2 mass-27 slot). Per class, eviction redistributes rather than loses: class 3 recovers 0.152→0.424 on fresh slots while class 2 pays 0.667→0.373. FIX NAMED: none at the mechanism/victim rule; the delta belongs to T1.6 (soft top-m vote / label distributions — its pre-registered prediction (a), now with measured margins 0.061/0.136 as the bar), the floor to T1.5 (capacity), which proceeds with the victim rule as-is and should track the task-1 content census as K grows.** | `phase33f_eviction_ledger.py` — done; verdict recorded (33d reserved for T1.5) |
| 29 — recursive hierarchy | Run the recruit/consolidate primitive one level up: "tokens" are category n-gram signatures, recruitment discovers recurring category-sequence motifs (phrase-level states). Certified by the same level-agnostic MI-vs-permutation-null machinery from phase 24; oracle eval only against shallow-parse chunk boundaries. Pre-registered risk: at k≈6 there are only ~36 category bigrams — the honest negative is "level 2 learns nothing beyond the level-1 transition matrix," which would push toward hierarchy-aware k-selection. This is the direct answer to the strongest external critique (no structure above the category FSM). | planned |
| 33g — representation-width byte reduction (T1.8; opened+run 2026-08-06) | 33d localized the gate's cost-branch failure as REPRESENTATION WIDTH (complex128 `xi` + dense K×K P; at matched memory count K=120 vs 120 prototypes, ACC −0.018 at 7.8× the bytes). Cut the bytes without touching behavior: `organism_compress.py` — (i) complex64 `xi`, (ii) CSR transition matrix above a count floor, (iii) optional low-rank `xi` factorization — plus E3 **schema v2** (compressed saves; v1 files still load; the uncompressed round-trip stays bitwise). Storage engineering only: compression re-encodes a finished state, nothing re-attributes occurrences to slots, so phase 9's closed negative is out of scope and stays closed. Compute width is pinned at complex128 on BOTH backends by a dtype guard in `Organism.perceive` (the numba kernel already promoted its inputs; without the guard a narrowed store would have made the numpy path compute at a different width — a latent divergence this target closed). Scored on 33c/33d's protocol verbatim, compressing at every task boundary and carrying the quantized store forward (the deployment persistence path), with an eval-only arm to separate storage loss from compounded loss. **Measured (every 33d anchor EXACT in uncompressed mode: 0.712/0.735/0.837/0.854/0.900): 3.85× fewer bytes at ZERO accuracy cost — K=160 goes 371.2KB → 96.4KB with max \|Δ task-accuracy\| = 0.0000 at every swept K, store-mode and eval-only identical, and paired reseeds s=0–4 giving drift of exactly 0.000 on all five. Prediction (a) HELD more strongly than written (predicted "within tolerance", measured identically zero — capture margins at ~0.99 overlap sit orders of magnitude above float32 eps). Prediction (b) HELD at 15.5× and turned out LOSSLESS: P is 6.1% dense at K=160 (1571/25600), so CSR at floor 0 stores exactly the nonzeros and reconstructs bit-for-bit; no count floor needed. Prediction (c)'s negative did not fire (0.900 still above the 0.872 bar). Prediction (d) NOT MET, and that is the phase's honest negative: only low-rank reaches inside ~2× the bar's bytes, and it does not survive reseeding — seed 0 alone says rank-20 gives 0.887 at 49.4KB (1.61×, which would have met (d)), but paired seeds 0–4 swing rank-20 from −0.052 to +0.033 (rank-16 −0.071/+0.019, rank-40 −0.047/+0.011) while c64+CSR is 0.000 everywhere. Best-of-grid on one seed is exactly T1.6's measured selection bias; LEVER (iii) IS A NEGATIVE at this scale, levers (i)+(ii) carry the whole 3.85×. Recorded seed-0 artifact, not a result: rank-48 scored 0.910 (above full width) and costs more than dense c64 anyway — K=160 slots span an N=64 field, so factors only pay below r≈45. Cost branch: still NOT met, but repriced — 33d's cost-matched point K=24/0.644 becomes K=48/0.728 inside the same 30.7KB, per-point cost falls 412.4→107.1 KB/ACC-pt at K=160, and the K=40 compressed arm (23.7KB, 33.3 KB/ACC-pt) is the first organism configuration under BOTH the bar's byte budget and its cost-per-point — at 0.712, not a bar-clearing arm. 0.900 now fits inside 1.08× replay's ~89.6KB footprint (33d spent 12.1× the bar; now 3.14×).** Integrity note kept with the numbers: P pruning above a count floor is accuracy-free on this protocol at EVERY floor tested only because the label readout never queries the graph — the reported graph-fidelity columns are the real price (floor>5 at K=160 keeps 81% of the mass but empties 31% of live rows of their next hops), and phase 30's consumers do use the graph. | `phase33g_representation_width.py` + `organism_compress.py` — done; pinned in harness §10, equivalence §7, E3 v1/v2 |
| 34 — capacity/quality scaling sweep (compute-efficiency track, opened 2026-07-16) | Owner's question: can the oscillator field reach LLM-level quality without LLM-level compute? Holds field size N=128 fixed and sweeps vocabulary 50→800 words (phase 5's cyclic grammar), per-word training exposure held constant so any quality drop is attributable to capacity, not under-training, against a bigram frequency-table baseline. **Measured: organism grammaticality collapses 0.994→0.284 as vocabulary grows 16×, while wall-clock cost still grows 6× over the same range; the bigram baseline holds ~0.85–0.87 flat throughout at up to ~900× less compute at 800 words.** Named finding: **Attractor Crowding Collapse** — a fixed-dimensional store of K word-attractors gets more confusable as K grows (measured nearest-neighbor embedding similarity 0.213→0.284, 50→800 words) and FLAT recall's one-shot K-way selection amplifies that into cascading, compounding errors (unlike the bigram baseline, which resamples independently each step). Confirms, as a controlled sweep, what phases 20 (real-text collapse at scale) and 33 (K=40 slot flooding) already hinted: capacity/selection, not per-step compute, is the binding constraint on this architecture as currently built. | `phase34_capacity_scaling.py` — done; names the Attractor Crowding Collapse finding |
| 35 — hierarchical (category-then-word) recall | Direct test of a fix for phase 34's Attractor Crowding Collapse: route generation through a cheap category-level choice first (flat K_cat=5 competition regardless of vocabulary), then resolve to the specific word-attractor only within the winning category (~K/5 candidates instead of all K). Inference-time only — same learned `org.Pn` Hebbian weights and trained attractors, only the selection procedure changes. **Measured: hierarchical recall holds ~0.84–0.85 grammaticality flat from 50→800 words (vs. flat recall's 0.994→0.284), recovering 98% of the flat-vs-oracle gap at 800 words (0.284→0.845, oracle 0.854).** At small vocabulary (50–100 words) hierarchical recall costs a small accuracy tax vs. flat recall (0.994→0.839 at 50 words) — the crossover where hierarchy starts winning is between 100 and 200 words. Confirms selection-time crowding, not the stored attractors themselves, is a load-bearing cause of the collapse — a fixable inference-time problem, not a fundamental ceiling. **Pre-registered caveat: category membership was read from the corpus generator's ground truth (an oracle), not discovered unsupervised** — `discover_categories_v2` (`polysemy_organism.py`) exists for that and is the honest next step before this counts as a general-purpose fix rather than a proof of mechanism. | `phase35_hierarchical_recall.py` — done; mechanism confirmed, oracle caveat open |
| 36 — unsupervised hierarchical recall (closes phase 35's oracle caveat; opened 2026-08-04) | Replace phase 35's ground-truth category labels with `discover_categories_v2` (PPMI-transformed transition profiles, phase 24's distinctness k-selection) and re-run the 50→800 sweep with the same scorers. Pre-registered predictions: (a) if discovered categories are ~phase-24-quality (V-measure ≈ 0.55 vs oracle), hierarchical recall should retain most of the flat-vs-oracle gap recovery, degraded in proportion to category impurity; (b) the honest negative is that impure categories mis-route resolution and hierarchical recall loses to flat below some purity floor — measure that floor. Then fold in phase 33's two product-critical fixes (slot-budget/eviction via the pool-mode use-it-or-lose-it machinery; online per-slot label evidence as mechanism) and **re-run the phase-33 ladder** — this row is the measured path to lifting the RELEASE HOLD. **Run 2026-08-05 (T1.1, PR #27) — result: PASS, pre-registered band ρ ≥ 0.7 cleared at ρ = 1.02.** Distinctness-argmax selected k=5 — the true category count — at every vocabulary size 50→800 (sharp peak: distinctness ~1.6 at k=5 vs ≤ 0.08 at k ≥ 6), slot-level purity and V-measure 1.000 everywhere, MI-vs-permutation-null certificate z = 132 → 25 624 as slot count grew. Discovered-label routing matches or slightly beats the oracle router at every point (hier_disc 0.849/0.842/0.848/0.861/0.858 vs hier_oracle 0.839/0.849/0.844/0.844/0.845 at 50/100/200/400/800 words; flat 0.994 → 0.284), retaining 96–102% of the flat-vs-oracle gap recovery at ≥ 200 words. In-process reproduction check: phase 35's committed anchors reproduced exactly (flat 0.994/0.284, hier_oracle 0.839/0.845 at 50/800). Prediction (a) confirmed degenerately — impurity was zero, so no degradation was observable; prediction (b)'s purity floor was NOT exercised: discovery was perfect at all tested scales on this synthetic grammar, so the floor lies below purity 1.0 and remains unmeasured — the honest open question for real text, where phase-24 discovery quality is V ≈ 0.55, is whether routing survives that impurity (attach to phase 27/28 corpus work). One library edit en route, values bit-identical: `_silhouette_real` now computes its pairwise-distance matrix in bounded-memory row chunks (the one-shot broadcast needs ~12 GB at slot-level scale n≈900); harness 27/27 on BOTH backends after the edit. Verdict: **phase 35's oracle caveat is CLOSED — hierarchical recall is a general-purpose, label-free, inference-time fix for the Attractor Crowding Collapse.** The remaining half of this row: slot-budget/eviction landed 2026-08-05 (T1.2, `phase33b_slot_budget.py`, harness §9), online label-evidence readout landed 2026-08-05 (T1.3, `label_readout.py`); the ladder re-run (T1.4) is the open piece. | `phase36_unsupervised_hierarchical_recall.py` — done (un-oracle arm); phase-33 fold-in open as T1.2–T1.4 |
| 37 — acceptance-bar calibration at V ≫ N (T2.2; closes phase 26's real-text arm; opened+run 2026-08-06) | Close the gap phase 26 pre-registered and phase 32 left PARTIAL at a modest V=60>N=30: perception's constants (active_bar, fuse_bar, s_hat) need to be measured, label-free, on real text where V=395 ≫ N=50 — rank-saturated, so phase 26's spectral estimator cannot run at all there. Per AGENT_TARGETS T2.2, does NOT invent a third estimator: ports closed PR #19's `_calibrate_bars` — a pairwise-quantile estimator (cross-pattern ceiling from a top-plateau-decontaminated overlap quantile, same-pattern mode from recurrence best-matches) that touches no eigendecomposition at all, so it carries no rank assumption — as a standalone `qcal_calibrate` function (estimator line-for-line; PR #19's `bar_guard` recruit-floor output is dropped — it needed an organism.py hook main does not have) (phase 26's calling convention: feeds active_bar/s_hat/fuse_bar into the existing `perceive()` hooks, zero organism.py/fastpath.py changes). Structurally distinct from phase 32's iterative fixed point (one-shot measurement on a calibration prefix, no fixed point to oscillate around) — the pre-registered hypothesis for why it might sidestep phase 32's documented sigma=0.2 s_cal oscillation (1.7–2.1 vs true 1.2). **Run 2026-08-06 — result: SUCCESS on both pre-registered arms.** Part A (synthetic V=250 words, N=50 dims, 5× oversubscribed, matched to the real corpus's DIM=50): PASS at sigma ∈ {0.0, 0.2, 0.3} — s_use exact match to true s at every sigma (0%, 0%, 0% rel error vs the 40% band — at sigma=0 by genuine estimation; at sigma=0.2/0.3 by construction, since the non-separated fallback returns the caller's known-sigma s_hat); coverage and slot-count deltas both exactly zero at sigma=0.2/0.3, where the non-separated regime correctly falls back to the caller's known-sigma hook per PR #19's own design (its documented behavior for the noise-dominated case) — no oscillation observed, consistent with the one-shot-vs-fixed-point structural argument. Part B (real corpus, 408 464 in-vocab tokens, 395 words, N=DIM=50, phase 23/25 core-arm protocol verbatim, only the bars parameterized): alpha=0.5 qcal lifts coverage **85 → 275/395** (cross_hi=0.867, same_pair=0.935, separated=True, s=0.069, active_bar=0.917, fuse_bar=0.932) — clears the pre-registered ≥230/395 success bar and sits 59 words (27%) above phase 25's 216/395 fully-decorrelated plateau, achieved WITHOUT touching the embedding recipe (α unchanged from phase 23's original 0.5). alpha=0.0 + qcal reaches 289/395, so calibration and decorrelation COMPOSE rather than substitute for each other. Regression harness 31/31 on both backends unaffected (additive script only). Verdict: **the V ≫ N calibration arm is CLOSED — qcal is the working real-text estimator; phase 26's real-text caveat and phase 32's open item are both resolved by this one estimator, no fourth attempt needed.** | `phase37_vn_calibration.py` — done; T2.2 closed |

## Engineering track (parallel; feeds both paths)

| Item | What | Status |
|---|---|---|
| E1 — regression harness | `regression_harness.py`: multi-seed, tolerance-based checks pinning the mechanism's headline behaviors (core perceive/recall/consolidate in plain, confirm, pool, and amb modes; category discovery; predictive-gain margin; since 2026-07-13 also phase 30 symbolic reasoning (§6) and phase 26 percentile bars (§7) — 27 checks total (E3 serialization joined as §8); since 2026-08-05 also §9 slot budget/eviction (T1.2, phase 33b) — 31 checks total). Statistical tolerances, not bitwise equality (GPU reductions will legitimately perturb low-order bits). Every backend/port must pass before it is trusted. Corpus-tier checks (phase 22/23 headline numbers) join the harness once E2 makes them cheap. | `regression_harness.py` |
| E2 — Numba port | JIT the sequential hot loops (perceive, recall/recall2, consolidate inner loops) — they are interpreter-bound, not arithmetic-bound; target 15 min → ~1 min at phase-23 scale. CPU is the right tool: the perceive loop is inherently sequential over the stream. Validated against E1. **Landed 2026-07-13** as `organism_numba.py` (opt-in `NumbaOrganism` backend, op-for-op kernels, 23/23 harness checks, 8× on pool+amb perceive at N=30/K=60 shape, ~2 min projected for phase-23 runs). **Upgraded 2026-07-15** — two sessions built E2 in parallel; the implementations were unified rather than kept side by side: `fastpath.py` is now the single kernel set (BLAS overlap matvec with a maintained conjugate, squared-magnitude argmax — profiling showed the per-slot `np.abs` hypot was half the frame cost — in-place z update, bounded-memory chunked streaming so generator streams never materialize, and consolidate's duplicate scan vectorized to one matmul, which does matter at corpus K≈1580), selected transparently via `Organism(backend="auto"|"numba"|"numpy")` / `DEFAI_BACKEND` env (default auto); `NumbaOrganism` remains as a thin alias so E3's cross-backend pin and existing imports hold. Validation: E1 green on BOTH backends, plus `test_fastpath_equivalence.py` (tier 0: numpy-vs-numba state agreement ~1e-15 on short streams in every mode; recall/recall2 committed hop sequences match exactly — identical RNG consumption order). Measured at true phase-23 corpus shape (`e2_benchmark.py`: K_CAP=1580, DIM=50, 4.9M frames): 18.2 min (numpy, projected) → **1.40 min (numba, measured) = 13.0×** — target met. Honest residual: throughput (58K frames/s) is now bounded by the K×N overlap matvec (memory-bandwidth, ~78K matvec/s at this shape) plus the per-token saccade pass, not the interpreter; the next factor is algorithmic (pruned candidate sets) or E4, not more JIT. Corpus-tier harness checks are now cheap enough to wire in (needs the Gutenberg fetch made reproducible first). | `fastpath.py` — done |
| E3 — state serialization | Save/load of the full organism state (`xi`, `P`, habituation, pool/probation state) with a schema version so stored memories survive mechanism upgrades. Not product polish — an episodic-memory product is *defined* by persistence. Plus: bounded memory (slot cap + eviction policy), deterministic replay. **Landed 2026-07-13: `organism_state.py` — schema-versioned .npz (no pickle), rng generator state included. Pinned guarantees (harness §8): mid-stream save→load→continue is BITWISE identical to never stopping (max dxi = dP = 0); deterministic replay (restored recall reproduces the identical stochastic trajectory); cross-backend restore (reference-saved state continues on the numba backend, capture 0.995). Bounded memory: the slot cap is K by construction and eviction is the mechanism's own use-it-or-lose-it recycling — nothing separate to serialize. Consolidation products (mem/Pn/kept_idx) round-trip so a restored organism can recall() immediately.** (2026-08-05, T1.2: the slot budget gave eviction persistent state after all — org.age staleness clock + org.evictions tallies now round-trip; pre-T1.2 files load with zeroed clocks, an additive migration, schema version unchanged. Pinned in harness §9.) | `organism_state.py` — done |
| E4 — GPU tier (hipfire) | The embarrassingly parallel stages only — permutation nulls first (the statistical backbone, thousands of independent shuffles), then per-word gain sweeps, all-pairs similarity, PPMI/SVD at V≥5–20K. The sequential perceive loop is *not* a GPU target. Windows+AMD reality: Python-side ROCm (numba-hip, CuPy-ROCm) is Linux-only, so the intended shape is a Rust/wgpu (Vulkan/DX12) compute crate with PyO3 bindings — which doubles as the dependency-free native runtime a product fork can ship. Timed to land around Phase 27/28 when the statistics workload grows. | planned |

## Sequencing

```
E1 harness ──┬─────────────► E2 Numba ──► Phase 27 (5M words) ──► Phase 28 ──► Phase 29
             │                   │
Phase 25 ────┴──► Phase 26 ──────┤
                                 ├──► E3 serialization ──► FORK GATE (Path B repo)
                                 └──► E4 GPU tier (by Phase 27/28)
```

Phase 25 and E1 are independent and both unblock everything else; they go
first. Phase 26 consumes Phase 25's diagnosis. E2 consumes E1's guarantees.
The fork gate consumes E1+E2+E3+Phase 26.

## Verification log

- **2026-08-04 (onboarding pass, fresh clone, independent Linux host,
  numpy 2.2.6 / numba 0.66)**: E1 `regression_harness.py` 27/27 PASS on
  both backends (numpy 59.8s, numba 10.2s); `test_fastpath_equivalence.py`
  all green (state agreement ~1e-15, recall sequences exact);
  `e2_benchmark.py` at phase-23 shape: numba 67.3K frames/s, 1.21 min full
  perceive (exceeds the pinned 58K/1.40 min; speedup ratio 6.0× on this
  host — the 13.0× is hardware-relative, pin the absolute numba number);
  phase 35 rows reproduced exactly at 50/100/200/400 words, 800-word row
  not re-run (host OOM at K≈960) — that datapoint rests on the committed
  run. Not re-verified: corpus-scale phases (20–28; Gutenberg fetch out of
  scope) and the phase-33 ladder (torch baselines not installed).
- **2026-08-06 (T1.8 representation-width compression, remote host,
  numpy 2.4.6 / numba 0.66 / sklearn 1.9; torch NOT installed, so the
  ladder's torch arms were not re-run — 33c's committed values stand)**:
  E1 `regression_harness.py` ALL PASS on both backends with the new
  section 10 (numpy 81.2s, numba 13.7s); `test_fastpath_equivalence.py`
  green including the new section 7 (narrowed-store cross-backend
  equivalence: state diff 5.0e-16, transition graph and slot occupancy
  identical, store dtype preserved); `test_label_readout.py` green; E3
  round-trips green for v2 uncompressed (bitwise), v1 backward load
  (bitwise), and compressed (P bitwise, xi at float32 eps); phase 33g
  committed run reproduced every 33d budget-arm anchor EXACTLY
  (0.712/0.735/0.837/0.854/0.900, FORG 0.169 at K=40 and 0.033 at K=160)
  and the prototype bar at 0.872/30.7KB/120 protos.
- **2026-08-05 (T1.4 gate re-test, fresh remote host, numpy 2.4.6 /
  numba 0.66 / torch 2.13-cpu)**: E1 31/31 PASS both backends (numba
  29.0s, numpy 112.2s); `test_label_readout.py` green; the previously
  unverified phase-33 ladder reproduced WITH the torch baselines
  (SGD 0.323, EWC 0.322, replay 0.913, joint 0.974, prototypes 0.872,
  organism 0.665/0.200 — numpy anchors exact); phase 33c committed run:
  all reproduction anchors REPRODUCED (see row 33c).

- **2026-08-05 (T1.7 eviction-ledger diagnostic, remote host, numpy 2.4.6 /
  numba 0.66)**: post-instrumentation (the `evict_debug` hook in
  organism.py/fastpath.py), E1 31/31 PASS both backends (numba 10.9s,
  numpy 61.7s), `test_fastpath_equivalence.py` all green; phase 33e's
  gate: 33c anchors reproduced exactly (0.712/0.169, census [9,7,4,6,14],
  baseline 0.665/0.200), ledger on/off bitwise identical, numpy and
  numba ledgers identical row-for-row (171 rows). Torch arms not re-run
  (organism-only diagnostic; torch not needed).

- **2026-08-06 (T2.2 phase 37, remote host, numpy 2.4.6 / numba 0.66)**:
  E1 31/31 PASS both backends (numpy 81.5s, numba 15.5s) before AND after
  `phase37_vn_calibration.py` landed (additive script only, no
  organism.py/fastpath.py changes); `phase37_vn_calibration.py` itself:
  Part A synthetic gate PASS all sigmas, Part B real corpus (8-book
  Gutenberg fetch, 408 464 in-vocab tokens) qcal coverage 275/395 (alpha=0.5)
  and 289/395 (alpha=0.0) — see row 37.

## What the external reviews got right (kept) and wrong (dropped)

Kept: vectorize so the corpus can scale (E2), external baselines (a small
gradient-trained model on the same corpus as an *oracle ceiling*, phase-9B
style — attach to Phase 27), demonstrate downstream utility of polysemy
detection (Phase 28 + wiring into generation), regression/testing discipline
(E1), hierarchy/compositionality above the category FSM (Phase 29).

Dropped, with reasons recorded so they aren't re-derived: SSM/Mamba core
(replaces the research question; gradient-trained), MoE gating (routing here
is information-limited, not cost-limited — phase 18 measured it), EWC/replay
(solves a failure mode this architecture measurably doesn't have),
differentiable external memory (a different project), post-hoc consolidation
clustering (re-opens phase 9's closed negative result: re-sorting provably
inherits online mixing).

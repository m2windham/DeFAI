# DeFAI Roadmap

Adopted 2026-07-10, after a critical review of two external AI audits (an
engineering-focused executive summary and a strategic review). What survived
that review is below; what didn't (SSM/Mamba core swap, MoE gating, EWC,
differentiable-memory rewrite — all gradient-era machinery that would break
the online/no-backprop premise and the measured continual-learning advantage)
is deliberately absent. Full reasoning lives in the session notes and the
review discussion; the standing rules of engagement in `FABLE_HANDOFF.md`
still bind everything here.

## OWNER DECISION RECORD — 2026-08-11: the gate is re-scoped, the RELEASE HOLD lifts

**Decided by the repo owner, 2026-08-11.** The gate adopted 2026-07-14 —
*"at least SOTA / cost-effective or we are not ready"* — is **re-scoped to
the capability axes**. The RELEASE HOLD that has stood since phase 33c
(2026-08-05) is **lifted**. This is the decision 33h specified and left
open, and it is recorded here rather than in a session log because every
public claim this project makes now inherits from it.

**The re-scoped gate, and the measured evidence for each axis.** The gate
is now: *unsupervised representation + structure learning + bitwise
persistence, in one single-pass online mechanism* — a conjunction the
prototype bar does not satisfy on any of its four terms.

| Axis | Evidence | Scope it carries |
|---|---|---|
| **Unsupervised representation** | No labels enter any mechanism (standing rule, enforced by review). The phase-33 readout is **eval-side only** — `label_readout.py`, pinned by `test_label_readout.py`, which reproduces the committed evict=0 anchor ACC 0.665. | The readout is a measuring instrument, not part of the organism. Any claim that quotes ACC is quoting an instrument reading. |
| **Structure learning** | Category validity without geometry (phase 24; class-bigram MI against a label-permutation null). Field-free logic layer doing real work (phases 30/40: kstep, rollout, planning; confidence-weighted planning +1.04 nats/plan over hop-count above a shuffled-shrinkage null). Hierarchical recall un-oracled (phases 35/36). | Phase 40's honest split: the confidence planner beats hop-count, but against phase 30's own MLE planner it clears on selection seeds and **fails held-out**. Quote the hop-count comparison, not the MLE one. |
| **Bitwise persistence** | E3 round-trip green for schema v3 uncompressed **and** compressed, plus v1 + v2 backward load. `SymbolRegistry` gives stable symbol identity across save/restore (T3.3) and survives independently of slots. Bitwise sections in `test_fastpath_equivalence.py` hold to 0.0e+00. | Verified by harness on two hosts (2026-08-09, 2026-08-11). This is the strongest-evidenced axis and the only one that is engineering rather than science. |
| **Single-pass online** | **Audited and survived** (phase 46 / T7.4), which is why this axis is admissible at all. At ONE epoch vs fifteen: coverage is *better* at one pass (327/376 vs 320/376); the category certificate clears its permutation null at z = 9.1 (fables) and z = 32.0 (gutenberg8); polysemy detection moves 50.0 → 51.3 (+3%, inside the ±20% rule). | Two scope sentences travel with these numbers, always: (i) the 1-vs-15 comparison exists only on the 2 431-token fables corpus — the large corpus was audited 1-vs-3; (ii) the detection axis has a wide three-seed spread, so the claim is "not epoch-sensitive at the resolution three seeds resolve", never "epoch-invariant". |

**Why this is defensible and not a concession dressed up.** The audit came
first. Phase 46 was run specifically to test whether the production recipe
violated the axis a re-scoped gate would stand on — a large drop was
pre-registered as a legitimate and important finding — and the axis held on
all three measures. Re-scoping onto an audited axis is a different act from
re-scoping onto a convenient one.

**WHAT THIS DECISION DOES NOT CLAIM.** Every line here is measured, and
none of it is repaired by the re-scope:

- **NOT SOTA on class-incremental accuracy.** ORGANISM+B scores 0.712 against
  the supervised prototype bar's 0.872 (phase 33c). **Experience replay beats
  the organism on BOTH axes and tops the entire ladder** at 0.913/0.105,
  above even the prototype bar. This was a missed pre-registered clause and
  it stays on the record.
- **Cost parity NOT met, and the ≤2× fallback NOT met** as literally written:
  measured floor 76.1 KB/ACC-pt = **2.24×** the bar (2.07× with phase 44's
  narrow CSR). The re-scope **concedes this branch** rather than passing it.
  The reason is arithmetic, not tuning — a prototype is a real N-vector and a
  field memory is a complex one.
- **Equal-accuracy cost-effectiveness is not claimable**, because the
  accuracies are not equal. Unconstrained KB/ACC-pt must never be quoted from
  33h without its scope sentence; it is minimized where accuracy is worst.
- **Task-free operation is NOT a differentiator** (phase 38; N4 edited down,
  not deleted). Removing the boundary annotation costs gradient arms almost
  nothing.
- **E4 DEFERRED** on measurement (phase 42), and **T2.4 stays blocked** — the
  category level of the chunking measure fails its label-permutation null
  (phase 47).

**Standing rule for every public artifact from here.** Any release note,
essay, demo or README sentence states the four capability axes **and** the
"not SOTA / cost branch conceded" line in the same breath. A capability
claim quoted without its benchmark caveat is a misrepresentation of this
decision, not a summary of it.

**Consequences.** T5.1 (release tag + episodic-memory product fork) is
**UNBLOCKED**, and T4.3 (D2/D3) with it. Cutting the tag is a separate
owner action with its own go-ahead — unblocking is not instruction to
release. The engineering gate (E1+E2+E3+phase 26 synthetic) was already
open and remains so.

**Not settled by this decision:** T7.1's storage fork. See the phase-50
target (T7.7) — the re-scope removes the fork's *gate* stakes but not its
architecture stakes, and it is now to be decided on measured merit.
**Settled 2026-08-14 — see the next record.**

---

## OWNER DECISION RECORD — 2026-08-14: the storage fork resolves to (A)

**Decided by the repo owner, 2026-08-14, on T7.7's measurement (ROADMAP
row 50).** Branch **(A) is ratified**: the `real_phase` + narrow-CSR layout
is the project's **deployment persistence encoding**. Complex128 remains the
**compute** width — the E2 contract is untouched.

**What the ruling rests on.** T7.7 ran the fork on the ladder the organism
is actually evaluated on, with FORG reported — the axis phase 43 could not
see and the axis a lossy store should damage first. Every pre-registered
prediction held, and the pre-named mundane account **is** the outcome: the
≤0.005 bound transfers. Worst held-out mean dACC **−0.0012** (band ±0.005),
worst held-out mean dFORG **+0.0034** (band ±0.010); the kill line
(dFORG ≥ +0.020 / dACC ≤ −0.020) was never approached, at both K, both
decoders, in store mode, with bootstrap CIs. Cost: **39.07 KB/ACC-pt =
1.148×** the bar, against (B)'s 70.30 = 2.066×.

**The three things that made this ratifiable rather than merely attractive:**
1. **The measurement was ordered before the choice.** T7.1 priced the fork
   and refused to choose; the owner refused to choose blind. The number that
   decided it did not exist until it was asked for.
2. **The run killed its own best headline.** A seed-0 probe cell showed the
   organism above replay; held out it reversed on all five seeds (mean
   −0.0215). Replay still tops the ladder at 0.9187/0.0834. A run that
   surrenders its most flattering number is a run whose other numbers can be
   trusted.
3. **The one adverse signal was reported, not rounded away** — dFORG +0.0034
   at K=112/argmax, 6× inside the kill line, not replicating at calib-b8 or
   K=160. It is recorded as **the number to watch**, and any future change to
   the persistence layout re-measures it.

**What (A) actually forfeits, corrected.** Only T7.6 arms **(a)** and **(b)**
— and both already had to clear phase 43's R ≈ 0.06 readout-blindness bar and
phase 16's 9–56-step collapse, so what is spent is an **option that was
already priced against two measured obstacles**. Arms **(c) dual time
constant** and **(d) graph order** are fork-independent and survive
untouched, including the cheapest measured route to sequence memory. The
original fork text overstated the loss; that overstatement is corrected here
and in `FABLE_HANDOFF.md`.

**Reversibility is asymmetric in (A)'s favour, and this is why "one-way
door" was the wrong frame.** (B)→(A) is a re-compression of stored state
available at any time. (A)→(B) loses only what the imaginary channel has
accumulated — and phase 43 measured that nothing accumulates at the
committed parameters (median R 2.6e-04, gauge phase, no committed
complex-input pipeline in a 54-file census). The door is a valve, and it
only shuts if the channel ever starts carrying something.

**What this ruling does NOT do.** It does not flip a default. `real_phase`
stays **default-OFF in code** until **T7.8** lands it with full anchor
re-verification — flipping a persistence default changes every E3 round-trip
and every byte number, and that is a change this project pins rather than
assumes. **Until T7.8 lands, no artifact may quote 1.148× as "the current
default"** — only as *the ratified layout*. It also does not touch the gate:
the cost branch stays conceded, and the 2026-08-11 caveat line is unchanged.

**Consequences.** T7.8 opens (execute the flip). T7.6 unblocks **for arms
(c) and (d) only**; arms (a)/(b) are closed by this ruling, and phase 48 is
released for the surviving arms. N8 is edited down — not deleted — in
`NOVELTY.md`.

---

## Strategic structure: Path A main, product forks downstream

- **This repository stays Path A**: the research line. One continuously
  running oscillator field that perceives, remembers, learns world structure,
  and generates — online, label-free, no gradients inside the mechanism.
  All discovery happens here, phase scripts remain the lab notebook.
- **Path B and later products are hard forks, not branches.** First fork: a
  deployable episodic-memory module (online ingestion, measured ~15%-forgetting
  continual learning, persistent state). Later forks: agent products built on
  capabilities as they are discovered. Each fork is a separate product
  repository.
- **Discoveries flow downstream through a versioned engine, never through
  git merges.** A result graduates from main to forks only when it has
  (1) a phase script, (2) pinned numbers in the regression harness, and
  (3) a release tag. Forks upgrade by bumping the engine version.
- **Fork gate for Path B** (event-driven, not a date): E1 + E2 + E3 done and
  Phase 26 landed — the earliest moment the engine is fast, persistent,
  calibration-robust, and regression-guarded.
  **Engineering gate OPEN as of 2026-07-13**: E1 (27 pinned checks), Phase 26
  (synthetic gate), E2 (unified fastpath, 13.0× at phase-23 corpus shape,
  both backends green), E3 (bitwise-lossless persistence) are all landed.
- **RELEASE HOLD — owner decision, 2026-07-15**: the engineering gate is
  necessary but NOT sufficient. Phase 33's readiness benchmark returned an
  honest NOT-SOTA verdict (organism ACC 0.665 vs 0.872 for a supervised
  prototype baseline on class-incremental split-digits), and the owner's
  standing bar is "at least SOTA / cost-effective or we are not ready."
  **No release tag is to be cut and no Path B repository created until the
  phase-33 gate passes.** The measured paths to closing the gap are already
  in phase 33's findings: (1) task-1 slot flooding at K=40 forces later
  classes to learn by slot drift — a slot-budget/eviction policy is needed
  (the pool-mode use-it-or-lose-it machinery is the natural home); (2) the
  frozen-label readout artifact (fixed in-phase with online label evidence)
  must become mechanism, not evaluation patch. These are product-critical
  fixes for the episodic-memory module itself, not benchmark tuning. Re-run
  the phase-33 ladder after each; the gate re-opens when the organism is at
  least SOTA or demonstrably cost-effective at equal accuracy.
  **Gate re-test run 2026-08-05 (T1.4, phase 33c — row 33c): both fixes
  folded in; organism+budget ACC 0.712 / FORG 0.169 vs the 0.872 bar
  (and replay at 0.913/0.105 tops the ladder). NOT SOTA, and the
  equal-accuracy cost-effectiveness branch is not claimable. The hold
  STANDS pending the owner's decision.**
  **Follow-on measurement 2026-08-05 (T1.5, phase 33d — row 33d): the
  remaining gap is mostly CAPACITY, not mechanism — at K=160 (evict=250,
  a protocol variation; the ladder pins K=40) the organism crosses the
  bar at ACC 0.900 / FORG 0.033, spending 12.1× the bar's state bytes.
  The cost-effectiveness branch stays unmet at every swept K (the bar
  delivers more accuracy per byte everywhere; cost-matched K=24 gives
  0.644 vs 0.872). The decision input is now priced: SOTA accuracy is
  buyable with capacity at a ~12× byte premium.**
  **Follow-on measurement 2026-08-06 (T1.8, phase 33g — row 33g): that
  premium was mostly representation width, and it is now cut. Lossless
  CSR transitions + a complex64 memory store give 3.85× fewer bytes at
  ZERO accuracy cost (max |delta task-accuracy| = 0.0000 across the K
  sweep and across five paired reseeds), so K=160's 0.900 costs 96.4KB
  instead of 371.2KB — 3.14× the bar rather than 12.1×, and inside
  1.08× replay's own footprint. The cost branch is still NOT met (the
  bar delivers 35.2 KB/ACC-point; the bar-crossing organism arm 107.1),
  and the lever that would have met it — low-rank factorization — is a
  measured negative: it does not survive reseeding. The hold STANDS;
  what changed is the price, not the verdict.**
  **Cost branch pegged as follow-up 2026-08-06: T1.9 (AGENT_TARGETS,
  phase 33h) — close the remaining ~3× KB-per-ACC-point gap (107.1 vs
  the bar's 35.2) via the compressed cost curve's knee, readout
  richness, and boundary consolidation, or measure the floor as the
  owner's decision input for re-scoping the gate.**
  **FLOOR MEASURED 2026-08-07 (T1.9, phase 33h — row 33h): the cost
  branch is NOT closable by storage engineering, and the hold's second
  branch now has a number. All three levers were worked and reseed-
  verified with the bar recomputed per seed: the compressed curve's knee
  (K re-chosen on 17 points), readout richness (T1.6's null turns out to
  be BOUNDED, not general — five decoders survive at K=120 that fail at
  K=40), and boundary consolidation (a measured negative — the slot bank
  is already near-orthogonal at `consolidate()`'s own 0.8 threshold, so
  every folded byte costs accuracy). Per-point cost fell 3.15× → **2.24×
  the bar** (76.1 vs 34.0 KB/ACC-pt at ACC 0.903 vs the bar's 0.865),
  measured on HELD-OUT seeds that took no part in choosing the decoder or
  K — the cheaper 1.96× point sign-flipped out of sample and was recorded
  rather than banked, per 33g's low-rank precedent. **The floor has a
  reason and it is arithmetic: a prototype is a REAL float32 N-vector,
  a field memory is a COMPLEX one — 2.03× per stored memory before the
  graph, at parity slot count (112 slots to clear what 115 prototypes
  clear). The ≤2× target was missed by 9.9KB against a transition matrix
  costing 10.5KB.** The complex state is the premise, not an
  implementation detail, and 33g already took the lossless width. **The
  hold STANDS, and the owner's decision is now fully specified: the cost
  branch as literally written has a ~2.2× floor; re-scoping the gate to
  the capability axes — unsupervised representation + structure learning
  + bitwise persistence in one single-pass online mechanism, none of
  which the prototype bar does at all — is the decision the data
  supports.** Recorded with its caveat, not claimed: the K=112 arm is now
  cheaper per accuracy point than 33c's replay (76.1 vs 98.1) at nearly
  its accuracy, but replay's number is a single-seed torch run not
  re-measured here.

## Demonstration & outreach track (decided 2026-07-14; feeds Path B adoption and sustainability)

Four public-demonstration concepts were triaged. All are honest by
construction — each renders capabilities that already have pinned harness
numbers; nothing is staged.

| # | Concept | Disposition |
|---|---|---|
| D1 — "It learns you" | Interactive page where the ORGANISM ingests the visitor's own interaction stream live: memories crystallize from their behavior in ~2 minutes, then it starts predicting their next move. E3 persistence makes "it will remember you if you come back" literally true. The interactive front door to D3. | **Earmarked** — first interactive demo to build |
| D2 — "It remembers becoming itself" | Feed the organism its own repo history (commits, 30 phases of lab notebook) as a perceptual stream; render its learned map of the research process that produced it. | **Marketing** — launch essay / announcement material |
| D3 — The living organism | One continuous organism life on a server, ingesting a public stream, its memories/world-graph/anomalies on a public page, E3 snapshots making the life unbroken across restarts ("born <date>, has never forgotten"). Donations = keeping it alive. | **Public-facing usage scenario** — with planned pivots of the ingestion feed: codebase/commit feeds, API/event feeds, etc. The feed adapter is the only new engineering; the organism needs nothing new |
| D4 — Self-lesion, live | Ablate the perceptual field on stage; reasoning continues (the phase-30/aphasia property, performed); restore from disk intact. | **Research first** — run as a proper phase (pre-registered lesion protocol + measured verdicts) at the appropriate point, demo second |

## Research track (this repo)

| Phase | Question | Status |
|---|---|---|
| 25 — decorrelation α-sweep | Is embedding correlation the whole cause of the pool-mode perception collapse (85/395 at scale)? Sweep the PPMI-SVD weighting `emb = U·S^α`, α ∈ {0.5, 0.25, 0}; report the >0.7-neighbor fraction, transitive-component count, and core-arm coverage per α. **Run 2026-07-10 on the live 547K-word corpus — result: PARTIAL, not full closure.** Neighbor fraction fell monotonically (23% → 13% → 9%) and core-arm coverage more than doubled (85 → 168 → 216 / 395, a 2.5× recovery) as α dropped to 0 — embedding correlation is confirmed as a real, measured cause. But even fully decorrelated, coverage plateaus at 216/395 (55%), well short of both full vocabulary and the plain-recipe arm's 378/395 (phase 23) — a large residual gap that decorrelation alone does not close. Verdict: correlation is *a* cause, not *the* cause; the acceptance-bar constants (0.7 fusion, 0.6 active_bar, the 0.8/√ anneal) are very likely an independent second cause, exactly as pre-registered for this outcome. Feeds directly into Phase 26. | `phase25_decorrelation_sweep.py` — done |
| 26 — percentile acceptance bars | Replace the absolute perception constants (0.7 fusion, 0.6 active_bar, the 0.8/√ anneal) with quantiles of the measured similarity distribution, generalizing the existing `s_hat` hook. Must reproduce phases 14/17/18 on synthetic near-orthogonal data before being believed on real text. Closes handoff open thread #1 as a mechanism and makes perception embedding-source-agnostic (a Path B requirement — products won't control their input embeddings). **Run 2026-07-13 — result: PASS on the synthetic gate, with one documented estimator lesson and one pre-registered real-text caveat.** The obvious same-word estimator (median best-match overlap in a settled-token buffer) fails beyond σ\*: best-of-n matching is fluctuation-dominated exactly where the bars matter (measured m1=0.585 at σ=0.3, truth 0.27) — the same single-comparison insufficiency that forced phase 17's pooling. The working estimator pools spectrally: with vocabulary rank < N the bottom covariance eigenvalues are pure noise floor; the estimator's finite-sample bias is removed by simulating pure noise at the same (n, N) — self-calibration, no data oracle. Measured s_cal 1.29/2.69 vs true 1.20/2.70; calibrated bars reproduce oracle coverage/junk at σ = 0, 0.2, 0.3 (0.96/0.37 vs oracle 1.00/0.34 at σ=0.3, inside tolerance). Ablation: calibrated s_hat is the load-bearing quantity — cal-shat alone matches cal-full, so the midpoint active_bar placement is redundant once s is measured (the oracle bar is itself s-derived). Cross-word distribution comes free and label-free from consecutive settled tokens (a saccade means the word changed). Caveats, pre-registered: gate mode at σ=0 trades junk (0.19 vs 0.00) for coverage (1.00 vs 0.88); the spectral floor needs vocabulary rank < N, so the real-text port (V ≫ N) must re-derive that arm. Candidate for that re-derivation: PR #19 (closed 2026-07-15 as superseded by this row's landed implementation) prototyped an in-stream `qcal` calibration whose pairwise-quantile estimator (cross-pattern ceiling from a top-plateau-decontaminated overlap quantile, same-pattern mode from recurrence best-matches) does NOT need the rank < N assumption, and its unfinished Part B was exactly the real-text run — start from that branch's `_calibrate_bars` before inventing a third estimator. | `phase26_percentile_bars.py` — done (synthetic gate); pinned in harness §7 |
| 27 — 5M-word scale run (T2.1) | 50–100 Gutenberg books, with phase 24's MI-vs-null + distinctness criteria wired into stage B. Pre-registered: does category structure sharpen (MI z up, distinctness-selected k stable or gracefully growing), and do the polysemy detections hold under tighter nulls? Depends on E2 for runtime. **Run started 2026-08-06** on a 42-book, 5.22M-raw-word corpus (title-verified at fetch to guard against ebook-id mismatch; 42 books chosen over 50–100 shorter ones to hit the 5M-word target, since the phase-23 8-book set averaged ~68K words/book). MIN_COUNT scaled proportionally (150→1500) to hold vocabulary in phase-23/24's computational regime (354 words) while giving ~8.8× more occurrences per word — the intended scale lever for both stage-B statistics and stage-C nulls. Stage A deliberately runs only phase 23's measured-winning arm (recruit=0.75, 3 epochs), not a 3-arm resweep — scope control, not part of either pre-registered question. Oracle POS V-measure cross-check dropped for runtime (nltk tagging at 5M tokens was the single slowest stage at 547K already); mechanism-only questions (P1, P2) are unaffected. **Run completed 2026-08-07** (fresh fetch, all 42 titles verified in committed code; stage A 949s/3 epochs on a 4-core host, 598 slots, coverage 328/354; full run 1281s): **P1 NOT CONFIRMED — the pre-registered falsification fired.** Distinctness-argmax selected **k=2**, a collapse to k≤3 (one of the frozen clause's two named failure modes): distinctness 0.491 at k=2 vs 0.242 at k=3 and ~0.11–0.19 everywhere else — at this vocabulary the parsimony criterion sees one dominant binary split. The z-clause fails too: z=61 at the selected k vs phase 24's 65, and z is DOWN at every comparable k (36 at k=4, 21 at k=6, 17 at k=12) because the null's MI floor rises with k faster than the measured MI. VALIDITY itself still holds overwhelmingly (MI clears its permutation null by 17–61 sigma at every k — the categories are real; they did not *sharpen*). Confound recorded, not hidden behind: scaling MIN_COUNT 150→1500 holds V in the same computational regime (354 words) but *changes the vocabulary composition* — only words with ≥1500 occurrences survive, a function-word-heavy population — and the 42-book set spans genres and translations the 8-book set did not; the scale lever moved the corpus family along with the statistics. Disentangling composition-shift from scale (e.g. same 42 books, phase-23's absolute vocab) is the natural follow-up; the prediction as frozen is falsified. **P2 CONFIRMED, with an instrument caveat.** 'right' clears its own per-word permutation null at n=3340: gain 0.008 vs null p99 0.001, margin 0.007, ABOVE — survival under ~5.4× the occurrences and a proportionally tighter null, the frozen operationalization. Caveat: the conditioning variable is the collapsed k=2 category assignment (conditional entropies capped at 1 bit), so absolute gains are not comparable to phase 21's 0.070@547K; the null is computed under the same k, so the survival verdict is internally consistent. Honest full ranking: 205/354 candidates clear their own p99 nulls (top margins: long 0.090, soon 0.055, d 0.044, hope 0.042, or 0.034 — 58% prevalence, consistent with phase 28's measured finding that this gain statistic conflates POS-level context-sensitivity with lexical polysemy). Stage D at k=2 still beats random (corpus-bigram hit 0.787 vs 0.586; category log-lik/hop −0.780 vs −1.036 random, −1.000 uniform). Verdict: **P1 falsified as pre-registered — distinctness-based k-selection collapses in this scale/vocab regime; P2 holds.** | `phase27_5m_word_scale_run.py` — done (P1 not confirmed, P2 confirmed) |
| 28 — polysemy vs grammatical context-sensitivity | The disentangling test: cluster each detected word's occurrences by context signature (prev/next category), ask whether the clusters land in *different* induced categories (lexical polysemy) or the same category with shifted successors (grammatical context-sensitivity). Evaluated against gold-POS occurrence entropy — oracle for evaluation only, per standing rules. Closes handoff threads: the phase-23 conflation residual *and* the POS precision/recall thread, in one run. Scope caveat pre-registered: same-POS polysemes (bank/bank) will be classed as context-sensitivity; the claim narrows to POS-level multi-role detection. **Run 2026-08-05/06 on a fresh 547K-word fetch of the phase-23 corpus (T2.3) — result: the decomposition works, the caveat holds exactly as predicted.** Re-ran phase 23's stage A and stage-C gain code verbatim, but with stage B swapped to `discover_categories_v2` on slot-level counts (the standing phase-21 category method; slot-level silhouette 0.006) rather than phase 23's word-level B2 k-means — so stages C/D condition on a DIFFERENT category variable than phase 23's own stage C did (coverage 378/395, k=3 categories). The 119-word gain-detected list is therefore the same population as phase 23's ~114 (handoff note) but not a bit-exact reproduction: the drift plausibly reflects the stage-B method swap (a deliberate choice, not noise) plus corpus-refetch variation. Multi-slot words take their highest-index slot's category (last-wins; documented in-script as arbitrary). The new test partitions each word's occurrences by predecessor category (the gain test's own conditioning variable, reused rather than re-derived) and compares each bucket's dominant successor category — a bigram-level proxy: occurrence-to-category routing itself is never computed, and the polysemy/context call has no measured null of its own (MIN_BUCKET=15 / DOM_MARGIN chosen in-phase, not pre-registered; a bootstrap on chance cross-bucket conflicts is the open follow-up): 18/119 (15%) words show a cross-bucket conflict (different dominant successor category) and are classed **polysemy** (long, fast, beautiful, big, red, best, rest, tell, ...); 101/119 (85%) keep one dominant successor category throughout and are classed **context-sensitivity**. Gold-POS (nltk, eval-only, never fed back): polysemy-classified words carry higher mean gold-POS entropy than context-sensitivity-classified words (0.533 vs 0.309 bits) — the intended split direction — but the separation is soft, not clean: several polysemy verdicts (big, last, body) turn out gold-monosemous (entropy 0, tagger-consistent single POS), i.e. real predecessor/successor-category conflicts that gold POS doesn't corroborate (context-sensitivity within one POS can still flip which coarse category dominates, since k=3 categories are coarser than 4-way universal POS). Whole-detector precision/recall against a minority-POS-tag gold-multirole definition: precision 0.336, recall 0.284 (tp=40/119 detected, 141/395 gold-positive) — expected and consistent with the pre-registered scope caveat, since the gain test's target (successor-distribution shift) is a broader, genuinely different quantity than gold POS-tag switching, not a proxy for it. 'right' (phase 21's headline word) lands in context-sensitivity here (gold-entropy 1.667, four POS tags) — a direct, measured instance of the pre-registered caveat: a real multi-POS word whose senses don't split into different k=3 categories. Verdict: **the disentangling test is a working decomposition of phase 23's conflated gain statistic, and the pre-registered scope caveat is now a measured property of the method (soft gold-POS separation, not zero, not clean) rather than an assumption.** | `phase28_polysemy_vs_context_sensitivity.py` — done |
| 30 — symbolic reasoning on the logic layer (out-of-sequence; spun off the 2026-07 logic/language architecture split, PR #20) | With transition learning extracted into a decoupled `TransitionGraph` behind a confidence-gated `EventBoundary` (the MIT/McGovern language/logic separation applied to the pipeline), test the decoupling's prediction: reasoning should run on the graph alone, field absent. Three ops added to the logic layer — `kstep` (multi-step inference), `rollout` (field-free imagination), `next_hops`/`plan` + `recall_directed` (planning; logic proposes the next hop, the field renders). **Run 2026-07-11 on a 6-regime hub-and-branch world (a real fork, so planning has a decision), truth used for eval only: k-step inference corr 0.995–0.999 vs permutation-null 99th pct ~0.45; rollout bigram corr 0.999 vs field recall 0.939 at ~20× speed; directed recall reaches every goal at exactly true shortest-path length (1.8 hops, 100%) vs 23.2 hops undirected.** Refactor itself is behavior-identical (demo bitwise, harness values unchanged). Next step designed but deferred: stable symbol IDs decoupled from slot indices (downstream scripts index org.P by slot). | `phase30_symbolic_reasoning.py` — done; pinned in harness §6 |
| 31 — lesion study (from demo triage D4) | Pre-registered self-lesion protocol: ablate the perceptual field (zero/noise the input coupling, damage xi) at graded severities and measure what survives on the logic layer — kstep inference, rollout structure, plan/goal-reaching — vs matched perception-side metrics, plus recovery after E3 restore. The quantitative version of the aphasia property phase 30 demonstrated qualitatively. Timing: at an appropriate point after Phase 27/28; the public demo version comes only after the phase script pins the numbers. | planned |
| 32 — self-consistent calibration at V ≥ N | Close phase 26's pre-registered caveat: the spectral estimator needs noise-only eigendirections, absent on rank-saturated (real-text) data. Method: fixed-point self-calibration — guess s, place bars, run the organism's own pooling, measure settled-token-vs-denoised-slot overlaps, invert the phase-17 overlap law, iterate; escalate on measurement failure (bars too tight = guess too low, itself a signal). **Run 2026-07-14 at V=60 > N=30 — result: PARTIAL.** Engages at all σ (the spectral method cannot run at all here); s_cal 0.20/1.74/3.48 vs true 0.30/1.20/2.70; calibrated-bar coverage within 0.06 of oracle everywhere — but misses the pre-registered bands (30% rel err, 0.05 coverage delta) at σ=0.2 (45%/0.06). Upward bias from contaminated slots persists through per-slot/upper-quartile refinements; a 1.7–2.1 oscillation at σ=0.2 is documented for the next attempt. The V ≥ N arm stays open. | `phase32_selfconsistent_calibration.py` — partial |
| 33 — industry-standard readiness benchmark | Owner's gate (2026-07-14): "at least SOTA / cost-effective or we are not ready" — measure it. Class-incremental split-digits, standard metrics (ACC/FORG), standard ladder (naive SGD, EWC, replay, joint oracle, growing prototypes, organism-on-numba). **Measured: organism ACC 0.665 / FORG 0.200 — 2× the gradient arms (SGD/EWC 0.32) so the continual-retention claim survives, but a supervised prototype baseline wins the raw benchmark outright (0.872, faster, smaller): the organism is NOT SOTA on class-incremental accuracy.** Two mechanism findings: task-1 slot flooding at K=40 makes later classes learn by slot drift (a slow forgetting channel, the FORG 0.20); frozen-label readout mis-scores drifted slots (ACC 0.25 artifact — fixed with online per-slot label evidence). Surviving differentiated claims: unsupervised representation + structure learning + bitwise persistence in one single-pass online mechanism. Public claims must be scoped to those, not benchmark wins. | `phase33_industry_baselines.py` — done; gate verdict recorded. Readout promoted to `label_readout.py` (T1.3, 2026-08-05): eval-side module, phase script consumes it bit-identically, pinned by `test_label_readout.py` |
| 33b — slot-budget/eviction under recruitment pressure (T1.2; closes phase 33's mechanism finding #1; opened+run 2026-08-05) | Stop task-1 slot flooding as MECHANISM, no task/label information anywhere: `perceive(evict=E)` makes the pool-mode use-it-or-lose-it staleness clock persistent organism state (`org.age`, E3-serialized; evict=0 reproduces prior behavior exactly) and adds eviction UNDER RECRUITMENT PRESSURE — a token more novel than 0.8× the recruit floor (the pool-mode weak-tail fraction: recruiting out of a freed slot costs more novelty than into a free one) with no free slot reclaims the least-established stale slot (argmin lifetime `count` among slots idle > E frames — consolidate's junk signal) through the existing recycle path (graph.retire + boundary.invalidate). **Measured on phase 33's exact organism arm (stream reproduced verbatim; baseline arm re-lands ACC 0.665 / FORG 0.200 and the flood: 40/40 slots after task 1, zero fresh recruits ever after). At the pre-registered window E=2000 the mechanism recruits fresh slots for later classes (14–29/task) but ACC FALLS to 0.511 (FORG 0.513) — the pre-registered recency risk materialized; negative recorded. The diagnosis is the row's finding: THE STALE POOL MUST CONTAIN THE PRESENT, OR EVICTION EATS THE PAST — with a long window only previous eras' slots are ever stale during a task, so count-ordering has nothing to choose from and the budget degenerates to a recency flush (era census [0,2,3,6,29]); with a short window the stream's own junk recruits go stale mid-task and enter the pool, count-ordering recycles the stream's own churn first, and established old-era prototypes survive. Sweep: every window in 100–750 beats baseline on BOTH axes; primary E=250 (post-hoc, mid-plateau, ~3× the measured live-slot revisit interval): ACC 0.712, FORG 0.169, era census [9,7,4,6,14], task-0 final accuracy 0.939 vs baseline's 0.732. E3 mid-run save→load→continue bitwise (dxi=dP=dage=0).** Pinned: harness §9 (flooded-bank two-world recovery is evict-load-bearing + bitwise clock round-trip), equivalence check 6 (plain and pool modes, both backends). T1.4 re-runs the full ladder once T1.1/T1.3 land. | `phase33b_slot_budget.py` — done; pinned in harness §9 |
| 33c — gate re-test: full ladder re-run (T1.4; the RELEASE HOLD's re-test; run 2026-08-05) | The complete phase-33 ladder, torch arms included, with the two hold-named fixes folded into the organism arm as mechanism: T1.2's slot budget (`perceive(evict=250)`, phase 33b's primary window) + T1.3's `LabelEvidenceReadout` with eviction invalidations forwarded at phase-script level; the evict=0 arm kept as reproduction anchor. T1.1 (hierarchical recall) is generation-side and has no role in this classification protocol — recorded in the docstring. **Measured: every numpy anchor reproduced EXACTLY — prototypes 0.872/0.021 (120 protos), organism evict=0 0.665/0.200 incl. the 40/40 flood with zero later recruits, ORGANISM+budget 0.712/0.169 with fresh recruits 11–14/later task and census [9,7,4,6,14]. Torch arms now on record: SGD 0.323/0.842, EWC 0.322/0.844, replay 0.913/0.105, joint\* 0.974/0.000. Gate verdict, honest: 0.712 vs the 0.872 supervised prototype bar — NOT SOTA; T1.2 closed 23% of the gap (0.207→0.160), cut FORG to 0.169, lifted task-0 final accuracy 0.732→0.939 (task 1 gives a little back, 0.464→0.393). Pre-registered clause missed, recorded: experience replay beats the organism on BOTH axes and tops the entire ladder, above even the prototype bar. Equal-accuracy cost-effectiveness not claimable (accuracy is not equal). Surviving claims stay scoped to the capability flags — unsupervised representation + structure learning + bitwise persistence in one single-pass online mechanism, now without the flooding ceiling.** OWNER DECISION POINT: bar not met; the RELEASE HOLD stands unless the owner re-scopes the gate to the cost/capability axes. | `phase33c_gate_retest.py` — done; gate verdict recorded |
| 33d — cost-matched capacity sweep (T1.5; prices the post-33c gap; opened+run 2026-08-05) | Is the 0.160 gate gap (0.712 vs the 0.872 prototype bar at the ladder's pinned K=40) a capacity artifact? Sweep K ∈ {40, 60, 80, 120, 160} at evict=250 + LabelEvidenceReadout on phase 33c's stream/scorers VERBATIM (the 33c module is imported, not copied; protocol-level only, no organism.py changes), plus evict=0 twins at every K (the staleness re-check: E=250 was tuned at K=40) and a cost-matched point — the largest K whose measured state fits the bar's 30.7KB (K=24). **Measured (anchors all EXACT: prototypes 0.872/120 protos, K=40 arms 0.665/0.200 and 0.712/0.169 census [9,7,4,6,14]): ACC rises monotonically — 0.712/0.735/0.837/0.854/0.900 — and CROSSES the bar at K=160 (0.900, FORG 0.033), so the gap IS mostly capacity; prediction (b)'s plateau never appeared (0.854 at K=120 is below 0.87 but still rising). The flood dissolves at K≥60 (task 0 recruits 53-54 slots) yet the bank still fills by tasks 1-3 at every K and eviction stays load-bearing (budget arm beats its evict=0 twin everywhere except K=120's −0.005: +0.238 at K=60, +0.076 at 80, +0.046 at 160; the evict=0 ladder is itself non-monotone, 0.497 at K=60 with census [53,7,0,0,0]); E=250 transfers across the sweep (window contingency untriggered). The K=40 task-1 regression (0.393, T1.7) heals with capacity: 0.726/0.881/0.905/0.964. Cost branch NOT met anywhere: bar 35.2 KB/ACC-pt vs organism 45.9→412.4 rising in K; cost-matched K=24 gives 0.644 vs 0.872; matched slot count (K=120 vs 120 protos) nearly matches accuracy (0.854 vs 0.872) at 7.8× the bytes — the byte gap is representation width (complex128 xi + dense K×K P), not memory count. Replay (committed 0.913 at ~89.6KB) still tops the ladder.** Verdict: raw-accuracy branch met only as a protocol variation (K=160, 12.1× bytes); cost branch unmet at every K; the pinned-K=40 ladder verdict (NOT SOTA) stands. Byte-efficiency levers named for any follow-on: representation width and P sparsity; T1.6 remains open as an independent readout lever. | `phase33d_capacity_sweep.py` — done; gap priced |
| 33e — readout geometry: eval-side decoder comparison (T1.6; opened+run 2026-08-05) | Does the 0.160 gate gap live partly in `LabelEvidenceReadout`'s blunt decoder (argmax-overlap slot -> majority label) rather than in memory content? Added richer EVAL-SIDE decoders over the SAME organism state (`label_readout.py`; labels still enter at the readout only, all decoders bitwise non-mutating, and every one reduces EXACTLY to argmax at m=1 — pinned in `test_label_readout.py` §4): `soft` (evidence-weighted vote over the top-m overlapping slots), `dist` (per-slot label DISTRIBUTIONS, count-normalized), `calib` (softmax(beta·overlap)-weighted distributions). Scored on the 33c protocol, both organism arms, each trained once and scored by all 15 configs (prediction is non-mutating, so this is exact). **Measured: argmax anchors EXACT (evict=0 0.665/0.200; evict=250 0.712/0.169). The seed-0 sweep looked positive on the budget arm — five configs cleared the pre-registered +0.02 threshold (calib-b8 +0.063 → ACC 0.775, dist-m2 +0.051, dist-m3, calib-b32, soft-m40) — and dead-null on the flooded evict=0 arm (best alternative +0.000, wide routing destructive to −0.443): geometry only helps where the slot population is healthy/mixed-era, never where it is drift-dominated. The pre-registered reseeding supplement then returned the honest negative: under full-protocol reseeds calib-b8 goes +0.063/+0.030/+0.088/−0.031/+0.029 (mean +0.036, SIGN FLIP at seed 3) and dist-m2 mean +0.027 with the same flip — at seed 3 argmax itself lands strongest (0.813; per-seed range 0.692–0.813), i.e. the gain is ANTI-CORRELATED with argmax's own strength: distribution decoders recover label mass when the evidence lands messy and give it back when it lands clean. Per the pre-registered survival rule (mean > +0.02 AND no sign flip) the claim does not survive: THE NULL STANDS — no eval-side decoder moves ACC by > 0.02 robustly; the memory content (K=40 = 4 slots/class vs the prototype bar's 120), not readout geometry, is the limit, and the lever is T1.5/mechanism, exactly as pre-registered. argmax stays the default (a flip would silently re-score the committed ladder).** Null pinned under CI: `test_label_readout.py` §5 (committed evict=0 protocol: best alternative ≤ argmax + 0.02, plus the 0.665 anchor tolerance-banded). | `phase33e_readout_geometry.py` — done; null pinned in test §5 |
| 33f — eviction ledger: the task-1 regression diagnostic (T1.7; run 2026-08-05) | Phase 33c's budget arm lifted task-0 retention 0.732→0.939 but dropped task-1 final accuracy 0.464→0.393 — explain it, discriminating three pre-registered hypotheses (H1 task-1 slots evicted disproportionately → count-normalized victim choice; H2 labels stale in the readout → invalidation scope; H3 intrinsically hardest → no fix). Instrumentation: a debug-only eviction ledger, `perceive(evict_debug=...)` in organism.py + fastpath.py, both backends — each pressure eviction logs (frame, slot, count, age) before mutating; era attribution reconstructed script-side so the mechanism stays task-blind. Equivalence proven, gate-style: all 33c anchors EXACT with the ledger attached, ledger on/off bitwise identical (state + accuracy matrix), numpy/numba ledgers identical row-for-row (171 evictions), harness 31/31 both backends + equivalence suite green with the flag off. **Verdict: H3 — the regression is NOT an eviction pathology; H1 and H2 ABSENT; the victim-choice rule is exonerated (123/171 evictions are same-task self-churn, era-1-born slots suffered only 4 cross-era evictions vs era-0's 31, and the budget arm holds task-1 content BETTER than baseline at every snapshot, 0.566 vs 0.535 final). Task 1 acquires fine (A[1,1] 0.929/0.881) and its tokens are the stream's most novel (never-novel 0.06 vs 0.12–0.23) — it is the hardest task to RETAIN: 42 of the budget's 51 task-1 errors (45/45 baseline) are queries captured by slots whose content genuinely belongs to other classes, the K=40 interference floor that made 0.464 the ladder's low before eviction existed. The net −0.071 delta is READOUT GEOMETRY: 9 errors on two slots still holding task-1 content but carrying foreign labels (slot 33: reborn on a task-1 token, correctly relabeled, then out-voted +13 class-5 votes — majority collapse discards its 8 surviving class-2 votes; slot 9: never evicted, zero task-1 votes ever, drifted into class-2 content and wins the argmax by 0.061 over the healthy majority-2 mass-27 slot). Per class, eviction redistributes rather than loses: class 3 recovers 0.152→0.424 on fresh slots while class 2 pays 0.667→0.373. FIX NAMED: none at the mechanism/victim rule; the delta belongs to T1.6 (soft top-m vote / label distributions — its pre-registered prediction (a), now with measured margins 0.061/0.136 as the bar), the floor to T1.5 (capacity), which proceeds with the victim rule as-is and should track the task-1 content census as K grows.** | `phase33f_eviction_ledger.py` — done; verdict recorded (33d reserved for T1.5) |
| 40 — logic-layer depth and optimization (T6.3; consolidation posture; opened+run 2026-08-08) | Phase 30 proved the logic layer reasons with the field absent, then stopped at three ops. Take the field-free graph deeper on T6.3's four axes, each against a null: (i) planning under uncertainty — Wilson lower bounds on edge probability instead of the point estimate, plus a `PathReport` carrying path reliability, weakest link and that link's observation count; (ii) compositional goals — negation by deleting symbols from the planner's graph, conjunction by exhausting goal orders over cached shortest-path trees; (iii) temporal abstraction — macro-edges in both the `fold` (contract pass-throughs) and overlay (mined vs observed) readings; (iv) sparse-P ports of next_hops/rollout/kstep. Library work is additive: `TransitionGraph.dijkstra` (one algorithm, several notions of "best"), `.confidence`, `.edge_quality`, `.path_report`, `.plan_reliable`, `.plan_visit`, `.passthrough_census`, `.contracted`, plus `MacroGraph` and `SparseTransitions`; **`next_hops` was refactored onto the shared Dijkstra and is pinned BITWISE identical to its pre-phase-40 body** (harness §12 keeps the pre-refactor code as the reference), and no existing op's behavior changed. **Measured — (a) CONFIRMED, (b) SPLIT, (c) CONFIRMED AS THE NEGATIVE with its condition measured.** (a) On a 120-symbol Zipf world at 2000 observed transitions (75/120 symbols seen ≤3 times, their edges over-stating true probability by 4.6×), confidence-weighted planning beats hop-count planning by **+1.04 nats/plan of true log-reliability, 0/5 sign flips**, above a shuffled-shrinkage permutation null (+0.0565 vs −0.0078; the null keeps the point estimates and permutes only the confidence correction, so it tests the count→trust correspondence rather than the weights), and reproduces on HELD-OUT seeds 5–9 at the pre-registered alpha=0.05 (+1.19, 0/5 flips). **The sharper question was NOT pre-registered and returns a bounded answer**: against the MLE planner phase 30 already had, the two disagree on 13.7% of plans and confidence wins **+0.379 nats when they disagree** at 2000 observations, decaying monotonically to **−0.074 at 60 000** — an under-evidence correction, not a better routing rule. On the selection seeds it clears T1.6's survival bar at budgets 1000/2000, and **on held-out seeds 5–9 it does not** (+0.345 with 2/5 flips at 1000; +0.012 with 3/5 at 2000), so it is recorded as a direction and NOT banked — 33g's low-rank precedent, applied to our own result. The durable half of (i) is the report, not the router: over 129 disagreeing pairs at seed 0 the arms rest on comparably observed edges (weakest link seen 12 vs 13 times), which a hop count shows and `PathReport` does. (b) Equality clause **MET** — sparse `next_hops` and `rollout` are BITWISE identical to dense including the RNG stream (row sums of integer counts are order-independent, so summing only the nonzeros lands on the same float; the argmin settle order is kept deliberately, since a heap would reorder ties), `kstep` to 1e-17 (skipping zeros reorders a float reduction: tolerance, not bit, per the standing port rule). Speed clause **NOT MET as written**: next_hops crosses 2× at **K=800**, not K=112 (1.0× at 112, 1.0× at 160, 1.4× at 400, 2.6× at 800, **6.1× at 1580**, at out-degree 10 so density runs 8.8%→0.6% across that range) — the dense op is O(K²) in cheap vectorized work, so sparsity only pays once the K² logarithms dominate. Two measured negatives kept with the numbers: **sparse full `kstep` is 5–10× SLOWER than dense** (0.1–0.2× across the sweep) because a dense K×K output is BLAS's home ground, and a non-equivalent `rollout_pruned` that samples only among successors buys just 1.6× while changing the trajectory — priced and not adopted; sparse `rollout` itself is 0.8–1.4×, since bitwise equality requires densifying the row so `rng.choice` consumes the stream identically, leaving only the O(K²) normalize to save. The real optimization win is **`kstep_row`, 4×→356× from K=112 to K=1580**: "where does this symbol land in k hops" is the query a planner issues, and the dense layer cannot ask it without building all K². Timings are best-of-3 after warm-up on a shared 4-core container — the crossover K is the portable claim, not the absolute milliseconds. (c) The pre-registered negative fired and its condition is now measured. Mined first-order macros move plan reliability by **exactly 0.0** (forced: a macro's quality IS the product of its own edges, so Dijkstra already pays it along the expanded route) and buy only 0.74 fewer decisions per plan at 1.1× the planning time; the pass-through census finds essentially nothing to contract at these degrees (0–14 of K, and 0/160 on the 33g-shaped graph), so the `fold` reading would delete structure rather than compress it — 33h's "price the fold before folding" precedent. But **the boundary is an ORDER, not a graph size**: observed trigram macros collapse to |Δ|≤4e-04 on a first-order control world, as they must, and gain **+0.443 nats on a genuinely second-order world (0/5 flips; held-out seeds 5–9 +0.581, 0/5 flips, SURVIVES)** — macros pay exactly when the world carries structure a first-order transition matrix cannot represent. Composition is certified, not asserted: 0/6 avoidance violations, optimality gap vs exhaustive search 4e-16 (negation) and 0.0 (conjunction, including a 3-goal case) on a 12-symbol world, and a random walker that reaches the goal trespasses on the forbidden symbol 76% of the time where the planner never does. Scope kept honest: the real organism graphs measured here (30/26/58 kept memories) show successors-per-symbol FALLING 5.1→1.8 as the world grows, which is phase 34's Attractor Crowding starving coverage rather than a graph property, so the timing sweep is anchored on 33g's committed 6.1%-at-K=160 (~10 successors/symbol) instead of extrapolating from them. | `phase40_logic_depth.py` — done; pinned in harness §12 (12 checks) |
| 29 — recursive hierarchy | Run the recruit/consolidate primitive one level up: "tokens" are category n-gram signatures, recruitment discovers recurring category-sequence motifs (phrase-level states). Certified by the same level-agnostic MI-vs-permutation-null machinery from phase 24; oracle eval only against shallow-parse chunk boundaries. Pre-registered risk: at k≈6 there are only ~36 category bigrams — the honest negative is "level 2 learns nothing beyond the level-1 transition matrix," which would push toward hierarchy-aware k-selection. This is the direct answer to the strongest external critique (no structure above the category FSM). | planned |
| 33g — representation-width byte reduction (T1.8; opened+run 2026-08-06) | 33d localized the gate's cost-branch failure as REPRESENTATION WIDTH (complex128 `xi` + dense K×K P; at matched memory count K=120 vs 120 prototypes, ACC −0.018 at 7.8× the bytes). Cut the bytes without touching behavior: `organism_compress.py` — (i) complex64 `xi`, (ii) CSR transition matrix above a count floor, (iii) optional low-rank `xi` factorization — plus E3 **schema v2** (compressed saves; v1 files still load; the uncompressed round-trip stays bitwise). Storage engineering only: compression re-encodes a finished state, nothing re-attributes occurrences to slots, so phase 9's closed negative is out of scope and stays closed. Compute width is pinned at complex128 on BOTH backends by a dtype guard in `Organism.perceive` (the numba kernel already promoted its inputs; without the guard a narrowed store would have made the numpy path compute at a different width — a latent divergence this target closed). Scored on 33c/33d's protocol verbatim, compressing at every task boundary and carrying the quantized store forward (the deployment persistence path), with an eval-only arm to separate storage loss from compounded loss. **Measured (every 33d anchor EXACT in uncompressed mode: 0.712/0.735/0.837/0.854/0.900): 3.85× fewer bytes at ZERO accuracy cost — K=160 goes 371.2KB → 96.4KB with max \|Δ task-accuracy\| = 0.0000 at every swept K, store-mode and eval-only identical, and paired reseeds s=0–4 giving drift of exactly 0.000 on all five. Prediction (a) HELD more strongly than written (predicted "within tolerance", measured identically zero — capture margins at ~0.99 overlap sit orders of magnitude above float32 eps). Prediction (b) HELD at 15.5× and turned out LOSSLESS: P is 6.1% dense at K=160 (1571/25600), so CSR at floor 0 stores exactly the nonzeros and reconstructs bit-for-bit; no count floor needed. Prediction (c)'s negative did not fire (0.900 still above the 0.872 bar). Prediction (d) NOT MET, and that is the phase's honest negative: only low-rank reaches inside ~2× the bar's bytes, and it does not survive reseeding — seed 0 alone says rank-20 gives 0.887 at 49.4KB (1.61×, which would have met (d)), but paired seeds 0–4 swing rank-20 from −0.052 to +0.033 (rank-16 −0.071/+0.019, rank-40 −0.047/+0.011) while c64+CSR is 0.000 everywhere. Best-of-grid on one seed is exactly T1.6's measured selection bias; LEVER (iii) IS A NEGATIVE at this scale, levers (i)+(ii) carry the whole 3.85×. Recorded seed-0 artifact, not a result: rank-48 scored 0.910 (above full width) and costs more than dense c64 anyway — K=160 slots span an N=64 field, so factors only pay below r≈45. Cost branch: still NOT met, but repriced — 33d's cost-matched point K=24/0.644 becomes K=48/0.728 inside the same 30.7KB, per-point cost falls 412.4→107.1 KB/ACC-pt at K=160, and the K=40 compressed arm (23.7KB, 33.3 KB/ACC-pt) is the first organism configuration under BOTH the bar's byte budget and its cost-per-point — at 0.712, not a bar-clearing arm. 0.900 now fits inside 1.08× replay's ~89.6KB footprint (33d spent 12.1× the bar; now 3.14×).** Integrity note kept with the numbers: P pruning above a count floor is accuracy-free on this protocol at EVERY floor tested only because the label readout never queries the graph — the reported graph-fidelity columns are the real price (floor>5 at K=160 keeps 81% of the mass but empties 31% of live rows of their next hops), and phase 30's consumers do use the graph. | `phase33g_representation_width.py` + `organism_compress.py` — done; pinned in harness §10, equivalence §7, E3 v1/v2 |
| 33h — cost-branch follow-up: KB/ACC-point parity or its floor (T1.9; opened+run 2026-08-07) | 33g repriced the gate's cost branch without meeting it (bar-crossing arm K=160 at 96.4KB = 107.1 KB/ACC-pt vs the bar's 35.2, a ~3× per-point gap). Close it or measure its floor as the owner's decision input. Levers in T1.9's priority order: (i) re-find the accuracy-per-byte-optimal K on the COMPRESSED curve (33d chose K uncompressed); (ii) readout richness at fixed K (T1.6's two recovery targets, eval-side, zero bytes); (iii) slot-count reduction by folding near-duplicate slots with `consolidate()`'s own rule at task boundaries (fold along an existing identity — `graph.merge` + count fold + `readout.remap`, `xi` never re-averaged — so phase 9's re-attribution negative stays closed). Width NOT retried (33g's c64+CSR is already lossless-to-3.85×) and low-rank NOT retried (33g measured it a reseeding negative). All arms paired s=0–4 with **the prototype bar recomputed on each seed's own split** — a fixed seed-0 bar against reseeded organism arms is the unpaired comparison T1.6/T1.8 were burned by. Protocol-level only: no library file touched. **Measured (anchors all EXACT at seed 0: bar 0.872/30.7KB/120 protos, 33d's ladder 0.712/0.837/0.854/0.900 at K=40/80/120/160, 33g's 96.4KB compressed store; live-slot byte accounting verified equal to 33g's allocated accounting on every non-folding arm). VERDICT: parity NOT MET, T1.9's ≤2× fallback NOT MET; measured floor 76.1 KB/ACC-pt = 2.24× the bar at ACC 0.903 (K=112 + `calib-b8`), on HELD-OUT seeds s=5–9. Per-point cost fell 3.15× → 2.24× (−29%), all of it from a K re-choice plus a free eval-side decoder. Lever (i): prediction (a) held in its first clause, FAILED in its second and instructively — the knee is at K=24 (23.9 KB/ACC-pt = 0.70× the bar) and the organism is cheaper per point than the bar at every K ≤ 40, but only because the metric is minimized where accuracy is worst (K=24 scores 0.583). The gate says "cost-effective AT EQUAL ACCURACY", so only the constrained number answers it; unconstrained KB/ACC-pt must never be quoted from this phase without that sentence. Constrained, lever (i) alone lands at K=160/110.3/3.25× — the finer 17-point grid bought resolution, not cost. Lever (ii): the phase's real finding — T1.6's null is BOUNDED, not overturned. It reproduces at low capacity (K=24: nothing survives, best +0.009) but at K=120 five decoders clear the survival rule on all five seeds (dist-m2/m3/m5, calib-b8/b32; calib-b8 +0.053 [+0.012, +0.081]) via exactly the mechanism T1.6 named and could not exercise — per-slot label DISTRIBUTIONS instead of majority collapse, which only has mass to recover once slots are plentiful enough to split it. Composed with lever (i) at zero byte cost, the cheapest at-bar arm went K=160/3.25× → K=96/1.94× on the selection seeds — **and then the pre-registered held-out rule took most of it back, which is why it exists**: on fresh seeds s=5–9 K=96's gain SIGN-FLIPS (+0.023 mean, min −0.004), so its 1.96× is recorded and NOT banked (a 14-decoder × 3-K grid filtered on the seeds that measure it is still selection — 33g's low-rank arm is the standing example); K=112 survives out of sample (+0.026 [+0.009, +0.050], positive on every held-out seed) and is the verdict point. Lever (iii): prediction (c) HELD and the redundancy census called it before a fold ran — at `consolidate()`'s own 0.8 threshold the bank is already near-orthogonal (8 duplicate pairs out of 12 720 at K=160; 13/160 slots have any partner; median pairwise overlap 0.169). Nothing to fold for free, so every byte costs accuracy, and the sweep prices it badly at the end that matters: th ≤ 0.60 (160 → 132 live) costs −0.147 ACC and leaves the bar far behind, while th = 0.85 frees 0.2 slots and moves 3.22× vs 3.25× — noise. No fold arm anywhere is both at/above the bar and cheaper per point than not folding; store-mode compounds worse than eval-only, as expected (th=0.70: −0.057 vs −0.045).** THE REASON THE FLOOR IS WHERE IT IS, and it is arithmetic rather than a tuning failure: a prototype is a REAL float32 N-vector (256 B), a field memory is a COMPLEX one (512 B) + 8 B meta = **2.03× per stored memory before the graph**, and slot efficiency is at parity (112 slots to clear what 115 prototypes clear), so nothing buys it back. The floor decomposes exactly — 68.7KB/29.4KB = 2.34× bytes × (0.865/0.903) = 2.24×, of which 2.03 × (112/115) = 1.98× is complex-vs-real and 10.5KB/29.4KB = 0.36× is the CSR graph. Parity would need the bar cleared on ≤ 57 slots (K=56 measures 0.800); the ≤ 2× fallback needs ≤ 114 slots with a FREE graph — K=112 overshoots the 58.8KB budget by 9.9KB against a graph costing 10.5KB, i.e. **the fallback was missed by almost exactly the size of the transition matrix**. Scope variation on record, NOT banked as a lever: this benchmark's readout never queries the graph, so a graph-free store would be 58.2KB at 0.903 = 64.5 KB/ACC-pt = 1.90×, which WOULD meet the fallback — honest only for a product that never reasons, since the graph IS the logic layer (phase 30 kstep/rollout/plan, phases 35/36 routing) and underpins every capability a re-scoped gate would point at; quoting 1.90× unqualified is the artifact 33g refused to bank on P pruning. Direction worth noting with its caveat: the K=112 arm (0.903 at 76.1 KB/ACC-pt) is now CHEAPER PER ACCURACY POINT than 33c's replay (0.913 at ~89.6KB = 98.1, 2.89×), having been 3.4× dearer at 33d — but replay's numbers are 33c's single-seed committed torch run, not reseeded and not re-measured here (no torch on this host), so it is a direction, not a claim. **Owner decision input: the cost branch is not closable by storage engineering — 33g took the lossless width, 33h took the free readout and the right K, and the residue is the complex-vs-real factor, which is the premise rather than an implementation. The branch as literally written has a measured floor of ~2.2×; re-scoping the gate to the capability axes (unsupervised representation + structure learning + bitwise persistence in one single-pass online mechanism, none of which the prototype bar does at all) is a decision the data now supports rather than merely permits.** | `phase33h_cost_frontier.py` — done; floor measured, RELEASE HOLD stands pending the owner's re-scope decision |
| 34 — capacity/quality scaling sweep (compute-efficiency track, opened 2026-07-16) | Owner's question: can the oscillator field reach LLM-level quality without LLM-level compute? Holds field size N=128 fixed and sweeps vocabulary 50→800 words (phase 5's cyclic grammar), per-word training exposure held constant so any quality drop is attributable to capacity, not under-training, against a bigram frequency-table baseline. **Measured: organism grammaticality collapses 0.994→0.284 as vocabulary grows 16×, while wall-clock cost still grows 6× over the same range; the bigram baseline holds ~0.85–0.87 flat throughout at up to ~900× less compute at 800 words.** Named finding: **Attractor Crowding Collapse** — a fixed-dimensional store of K word-attractors gets more confusable as K grows (measured nearest-neighbor embedding similarity 0.213→0.284, 50→800 words) and FLAT recall's one-shot K-way selection amplifies that into cascading, compounding errors (unlike the bigram baseline, which resamples independently each step). Confirms, as a controlled sweep, what phases 20 (real-text collapse at scale) and 33 (K=40 slot flooding) already hinted: capacity/selection, not per-step compute, is the binding constraint on this architecture as currently built. | `phase34_capacity_scaling.py` — done; names the Attractor Crowding Collapse finding |
| 35 — hierarchical (category-then-word) recall | Direct test of a fix for phase 34's Attractor Crowding Collapse: route generation through a cheap category-level choice first (flat K_cat=5 competition regardless of vocabulary), then resolve to the specific word-attractor only within the winning category (~K/5 candidates instead of all K). Inference-time only — same learned `org.Pn` Hebbian weights and trained attractors, only the selection procedure changes. **Measured: hierarchical recall holds ~0.84–0.85 grammaticality flat from 50→800 words (vs. flat recall's 0.994→0.284), recovering 98% of the flat-vs-oracle gap at 800 words (0.284→0.845, oracle 0.854).** At small vocabulary (50–100 words) hierarchical recall costs a small accuracy tax vs. flat recall (0.994→0.839 at 50 words) — the crossover where hierarchy starts winning is between 100 and 200 words. Confirms selection-time crowding, not the stored attractors themselves, is a load-bearing cause of the collapse — a fixable inference-time problem, not a fundamental ceiling. **Pre-registered caveat: category membership was read from the corpus generator's ground truth (an oracle), not discovered unsupervised** — `discover_categories_v2` (`polysemy_organism.py`) exists for that and is the honest next step before this counts as a general-purpose fix rather than a proof of mechanism. | `phase35_hierarchical_recall.py` — done; mechanism confirmed, oracle caveat open |
| 36 — unsupervised hierarchical recall (closes phase 35's oracle caveat; opened 2026-08-04) | Replace phase 35's ground-truth category labels with `discover_categories_v2` (PPMI-transformed transition profiles, phase 24's distinctness k-selection) and re-run the 50→800 sweep with the same scorers. Pre-registered predictions: (a) if discovered categories are ~phase-24-quality (V-measure ≈ 0.55 vs oracle), hierarchical recall should retain most of the flat-vs-oracle gap recovery, degraded in proportion to category impurity; (b) the honest negative is that impure categories mis-route resolution and hierarchical recall loses to flat below some purity floor — measure that floor. Then fold in phase 33's two product-critical fixes (slot-budget/eviction via the pool-mode use-it-or-lose-it machinery; online per-slot label evidence as mechanism) and **re-run the phase-33 ladder** — this row is the measured path to lifting the RELEASE HOLD. **Run 2026-08-05 (T1.1, PR #27) — result: PASS, pre-registered band ρ ≥ 0.7 cleared at ρ = 1.02.** Distinctness-argmax selected k=5 — the true category count — at every vocabulary size 50→800 (sharp peak: distinctness ~1.6 at k=5 vs ≤ 0.08 at k ≥ 6), slot-level purity and V-measure 1.000 everywhere, MI-vs-permutation-null certificate z = 132 → 25 624 as slot count grew. Discovered-label routing matches or slightly beats the oracle router at every point (hier_disc 0.849/0.842/0.848/0.861/0.858 vs hier_oracle 0.839/0.849/0.844/0.844/0.845 at 50/100/200/400/800 words; flat 0.994 → 0.284), retaining 96–102% of the flat-vs-oracle gap recovery at ≥ 200 words. In-process reproduction check: phase 35's committed anchors reproduced exactly (flat 0.994/0.284, hier_oracle 0.839/0.845 at 50/800). Prediction (a) confirmed degenerately — impurity was zero, so no degradation was observable; prediction (b)'s purity floor was NOT exercised: discovery was perfect at all tested scales on this synthetic grammar, so the floor lies below purity 1.0 and remains unmeasured — the honest open question for real text, where phase-24 discovery quality is V ≈ 0.55, is whether routing survives that impurity (attach to phase 27/28 corpus work). One library edit en route, values bit-identical: `_silhouette_real` now computes its pairwise-distance matrix in bounded-memory row chunks (the one-shot broadcast needs ~12 GB at slot-level scale n≈900); harness 27/27 on BOTH backends after the edit. Verdict: **phase 35's oracle caveat is CLOSED — hierarchical recall is a general-purpose, label-free, inference-time fix for the Attractor Crowding Collapse.** The remaining half of this row: slot-budget/eviction landed 2026-08-05 (T1.2, `phase33b_slot_budget.py`, harness §9), online label-evidence readout landed 2026-08-05 (T1.3, `label_readout.py`); the ladder re-run (T1.4) is the open piece. | `phase36_unsupervised_hierarchical_recall.py` — done (un-oracle arm); phase-33 fold-in open as T1.2–T1.4 |
| 37 — acceptance-bar calibration at V ≫ N (T2.2; closes phase 26's real-text arm; opened+run 2026-08-06) | Close the gap phase 26 pre-registered and phase 32 left PARTIAL at a modest V=60>N=30: perception's constants (active_bar, fuse_bar, s_hat) need to be measured, label-free, on real text where V=395 ≫ N=50 — rank-saturated, so phase 26's spectral estimator cannot run at all there. Per AGENT_TARGETS T2.2, does NOT invent a third estimator: ports closed PR #19's `_calibrate_bars` — a pairwise-quantile estimator (cross-pattern ceiling from a top-plateau-decontaminated overlap quantile, same-pattern mode from recurrence best-matches) that touches no eigendecomposition at all, so it carries no rank assumption — as a standalone `qcal_calibrate` function (estimator line-for-line; PR #19's `bar_guard` recruit-floor output is dropped — it needed an organism.py hook main does not have) (phase 26's calling convention: feeds active_bar/s_hat/fuse_bar into the existing `perceive()` hooks, zero organism.py/fastpath.py changes). Structurally distinct from phase 32's iterative fixed point (one-shot measurement on a calibration prefix, no fixed point to oscillate around) — the pre-registered hypothesis for why it might sidestep phase 32's documented sigma=0.2 s_cal oscillation (1.7–2.1 vs true 1.2). **Run 2026-08-06 — result: SUCCESS on both pre-registered arms.** Part A (synthetic V=250 words, N=50 dims, 5× oversubscribed, matched to the real corpus's DIM=50): PASS at sigma ∈ {0.0, 0.2, 0.3} — s_use exact match to true s at every sigma (0%, 0%, 0% rel error vs the 40% band — at sigma=0 by genuine estimation; at sigma=0.2/0.3 by construction, since the non-separated fallback returns the caller's known-sigma s_hat); coverage and slot-count deltas both exactly zero at sigma=0.2/0.3, where the non-separated regime correctly falls back to the caller's known-sigma hook per PR #19's own design (its documented behavior for the noise-dominated case) — no oscillation observed, consistent with the one-shot-vs-fixed-point structural argument. Part B (real corpus, 408 464 in-vocab tokens, 395 words, N=DIM=50, phase 23/25 core-arm protocol verbatim, only the bars parameterized): alpha=0.5 qcal lifts coverage **85 → 275/395** (cross_hi=0.867, same_pair=0.935, separated=True, s=0.069, active_bar=0.917, fuse_bar=0.932) — clears the pre-registered ≥230/395 success bar and sits 59 words (27%) above phase 25's 216/395 fully-decorrelated plateau, achieved WITHOUT touching the embedding recipe (α unchanged from phase 23's original 0.5). alpha=0.0 + qcal reaches 289/395, so calibration and decorrelation COMPOSE rather than substitute for each other. Regression harness 31/31 on both backends unaffected (additive script only). Verdict: **the V ≫ N calibration arm is CLOSED — qcal is the working real-text estimator; phase 26's real-text caveat and phase 32's open item are both resolved by this one estimator, no fourth attempt needed.** | `phase37_vn_calibration.py` — done; T2.2 closed |
| 41 — polysemy: act on detection (T6.4; closes the README's "detects ... now needs the field mechanism to act on it" thread; opened+run 2026-08-08) | Close the loop the project has carried since phase 12: let the **detector drive the field**. Per-word predictive gain (phase 12's Myhill-Nerode statistic) over UNSUPERVISED categories (`discover_categories_v2`, the standing method) gates phase 10's context-primed splitting -- a word may recruit a second sense-slot only if its gain clears **its own** permutation null at **its own** sample size (phase 21/27's committed operationalization) -- then the resulting slots are scored on successor distinctness and on DOWNSTREAM UTILITY. Run on the phase-8/10 synthetic dual-role corpus (29 words, 3 genuinely dual: fish/duck/bear -- same surface form, same embedding, two grammatical roles), 12 000-token train / 4 000-token held-out, seeds s=0-4 paired, every constant taken from a committed phase (no grid searched, so the held-out-seed rule is not triggered). Phase 10's twice-confirmed negative is respected, not re-tested: **every arm runs at g_mem = 0.0** -- perception reads the field, it does not bend it. Four arms, identical class/dynamics/routing, differing only in which words may split and what decides where an occurrence goes: `unsplit` (no splitting), `split` (detected words, context-primed routing), **`matched` -- the mandatory control T1.5 forced**, holding EXACTLY the split arm's per-word slot count for every word but routing occurrences AT RANDOM, and `randword` (|D| randomly chosen UNDETECTED words allowed to split, a diagnostic on whether the DETECTOR is load-bearing). The control skips consolidation by design and the reason is stated in the script: phase 10's consolidate merges slots whose context residuals agree, i.e. it is a duplicate detector, so applied to a random partition it would undo that partition and hand the control LESS capacity than the arm it controls for. **THE MISSES FIRST. (i) GENERATION DOES NOT IMPROVE — both generation metrics fail the pre-registered survival rule by SIGN FLIP: corpus-bigram hit +0.001 [-0.002 … +0.011] and modal-role grammaticality **-0.005** [+0.021 … -0.053]. Splitting buys prediction, not generation, and that half of prediction (b) is falsified. (ii) On word-level likelihood the split arm beats its matched control (+0.145 nats/token, positive on all five seeds) but is WORSE THAN THE UNSPLIT ORGANISM (-0.081): fragmenting a word across many slots makes each slot's next-word estimate sparser than the pooled one, so the split's information does not pay for its own fragmentation on this axis. (iii) Held-out next-WORD accuracy is SATURATED and cannot discriminate -- the eval-only Bayes ceiling is 0.144 and the matched control already sits at 0.132, so all arms live inside 0.012; split takes +0.007 (60% of that headroom) and is correctly recorded as BELOW THRESHOLD rather than as a null. (iv) The +0.02 survival threshold is T1.6's, calibrated on an ACCURACY; applying it to a log-likelihood is not the same bar, so the verdict below rests on next-category accuracy, the metric the threshold was designed for, and the ll/token survival is reported as corroboration, not as a second independent win. (v) FRAGMENTATION IS WORSE UNDER GATING, not better: split dual words carry a mean of 11.3 slots (mean role-purity 0.87) where ungated phase 10 carried 3-4 -- gating hands the whole free-slot budget to three words, so phase 10's open 'exact 2/1 slot structure' problem is amplified by this phase, not closed. (vi) The detector false-positived on 3 of 26 controls at ONE seed of five (wolf +0.011, sheep +0.001, swim +0.001 -- all marginal, [3,0,0,0,0] across seeds), consistent with p99's ~0.26 expected rate but on the high side. **WHAT HELD. Prediction (a) CONFIRMED, 15/15**: every split dual word's sense-slots have successor-category distributions distinct from a same-word permutation null at the observed group sizes (mean pairwise TV 0.304-0.420 vs p99 0.107-0.127, 200 draws) -- and, as an internal validation the phase did not have to pass, **0/3 of the detector's own false positives clear that test**, so the distinctness test rejects exactly what the detector should not have flagged (their slots are 100% single-role, real splits but not sense splits). Detection itself: 3/3 dual words at EVERY seed, margins 0.34-0.42 bits against p99 nulls of 0.006-0.008 (a ~50x margin), on categories the organism discovered for itself (k=3 selected at every seed, silhouette 0.596-0.657, coverage 27-29/29, recovering ANIMAL/ACTION/OBJECT). Sense structure, eval-only: 15/15 dual words cover BOTH true roles. **Prediction (b) SURVIVES on held-out next-CATEGORY accuracy: +0.063 over matched capacity, [+0.046, +0.077, +0.079, +0.064, +0.050] -- positive on every seed, clearing both clauses of the survival rule, and worth 56% of the 0.114 headroom between the matched control (0.672) and the eval-only oracle ceiling (0.786).** The capacity match is EXACT at every seed (split == matched at 59/64/65/57/59 live slots; unsplit 29), which is what makes that number attributable to the split's information rather than to slots. **The confound was real but small on this protocol and only the control could have shown that**: matched (0.672) beats unsplit (0.662) by just +0.010, so of the +0.073 split-vs-unsplit delta about 0.010 is capacity and 0.063 is the split -- T1.5's warning was still worth paying for, since without the control the whole +0.073 would have been unattributable. The `randword` diagnostic closes the loop from the other side: allowing |D| randomly chosen UNDETECTED words to split yields almost no extra slots (26-31 live vs unsplit's 29 -- the residual gate itself declines to split them) and lands on unsplit's numbers (next-category 0.660 vs 0.662), so the DETECTOR, not the splitting machinery, is what selects where capacity is worth spending. Phase 10's own anchor was re-run on this host and reproduces exactly at thr=0.65 / g_mem=0.0: 3/3 roles covered, 0/3 exact 2-way splits, 10/10 dual-word slots grammatically correct, 60/60/61 slots, STABLE across seeds 0/1/2. **SCOPE SENTENCE, and it is load-bearing: every number here is on a SYNTHETIC corpus where polysemy is LEXICAL BY CONSTRUCTION.** Phase 28 measured that on real text this same gain statistic conflates lexical polysemy with grammatical context-sensitivity (only ~15% of detections were lexical at POS level, precision 0.336 / recall 0.284 against gold multi-role), so NONE of this transfers to real text without re-derivation -- 'detection drives useful splitting' is established as a MECHANISM here, not as a real-text capability. Verdict: **the loop closes, and it closes asymmetrically -- detection-driven splitting is a measurable functional gain for next-category prediction at matched capacity, and is NOT one for generation or for word-level likelihood.** Prediction (c)'s blanket honest-negative ('flat at matched capacity, a representational nicety') therefore does NOT fire, but neither does the strong reading of (b): the capability is narrower than the thread's framing assumed. | `phase41_detection_driven_split.py` — done; pinned in harness §13 |

| 42 — performance re-baseline & profile (T6.5; opened+run 2026-08-08) | Every performance number on record predated compression (33g), the symbol registry (T3.3), and the 5M-word corpus (phase 27). Re-measure `e2_benchmark`'s shapes and the compressed/CSR store, harness wall-clock on both backends (27 → 65 checks), phase-27 stage timings, and — never done before — **where the frame budget actually goes**; output a shortlist ranked by measured cost, which is the E4 go/no-go input. Protocol-level only: no library file touched. Phase 27 was re-run end-to-end for real stage timings (42 books re-fetched, all titles verified; **every committed anchor exact** — 5 224 034 raw / 3 577 289 in-vocab / V=354, 598 memories, coverage 328/354, distinctness 0.491 at k=2, MIz 61/50/36/21/17, 'right' n=3340 gain 0.008 vs p99 0.001, 205/354 above null; 1184s total vs the committed run's 1281s). **THE MISSES FIRST. (a) MISSED AS WRITTEN.** The pinned 58K frames/s was not reached: four full-load runs at the phase-23 shape gave 43.8/50.1/51.4/54.3K (median ~51K; 1.50–1.86 min for the 3-epoch load vs the pinned 1.40 min). The pre-registered branch demanded localization rather than a hardware excuse, so a same-host, interleaved, six-round A/B ran three trees on identical work — current, pre-T3.3 (`7e0b639~1`), pre-33g (`92e9cc9~1`): medians **49.8 / 50.1 / 50.6K, indistinguishable**. The T3.3 registry ON costs **−2.4%** (median, base `Organism` both arms) against a **17%** within-arm spread; a complex64 store costs **+3.6%** against a **4%** spread — both INSIDE the noise. So there is no tree-attributable regression, and the sharper finding is methodological: this host spans **43.7–58.2K on bit-identical work**, a ±20% band that STRADDLES the pin. **A single absolute throughput number cannot function as a regression detector** — the 2026-08-04 advice to "pin the absolute numba number" does not survive contact with host variance; the same-session A/B does. **(b) FALSIFIED → E4 DEFER.** Permutation nulls are **265s of 1184s = 22.4%**, against the pre-registered ≥50% GO bar: stage B 163s (the 169s observed minus 5.8s of directly measured k-means+silhouette at the real 328×656 profile shape) + stage C 102s (~100% nulls; the independent cost model put it at 90–97s). Observed stage split: corpus+embeddings 20s (1.7%), **stage A perceive 785s (66.3%)**, stage A consolidate+`coverage_map` 105s (8.9%), stage B 169s (14.3%), stage C 102s (8.6%), stage D 3s. Amdahl ceiling with nulls made **free** is **1.29×** on TOTAL corpus-tier wall-clock; E4's full named scope (nulls + all-pairs similarity, i.e. `coverage_map`) reaches 31.3% and **1.45×**. Scope sentence that must travel with those numbers: they are ceilings on TOTAL time, not speedups of the null stage. **(c) CONFIRMED**, same-host rather than cross-host: the 27-check tree (`15cfa65`) vs the 65-check tree costs numpy 82.2s → 101.0s (1.23×) and numba 12.4–13.5s → 12.5–12.7s (~1.00×) for **2.41×** the checks; T1.8's + T3.3's 34 new checks add 8.5s numpy / 0.7s numba. Sublinear — no harness tiering needed. **THE PROFILE (the part that had never been done).** At K=1580/N=50 the frame is 14 614 ns: **overlap matvec 10 927 (75%)**, free-slot scan + refine 2 135 (15%), argmax 1 441 (10%), fused z update 111 (0.8%); frame = 1.23 matvec-equivalents, and K×rate flattens 18.5M (K=40) → 89.5M (K=1580) → 102.2M (K=3000), i.e. asymptotically 1/K. The 2026-07 hypothesis SURVIVES — the matvec is still the bound — but the E2 row's *reason* needs correcting: at these shapes the operand is 1.2 MB and therefore **cache-resident, so the bound is cache bandwidth (84–94 GB/s effective), not DRAM**. On the numpy reference path `.conj()` alone is 3.12s of 7.09s (44%), inside `overlaps()` — the fastpath already fixed this by maintaining `xic`; the reference path never did. **CSR/compression at corpus K:** P is 3.64% dense at K=1580 (90 914/2 496 400), so CSR is **18.2× smaller and lossless** (19 503 KB → 1 072 KB) and the full compressed store is 15.43× (20 762 KB → 1 346 KB) in 20 ms — but CSR is **storage only**: perceive writes P in place (`P[bprev,k] += 1`, row/column zeroing on retire), so the kernel needs it dense. `consolidate()` at K=1580 is 18 ms and `recall` runs at 220K steps/s — neither is a bottleneck. **THE SHORTLIST IS PRICED, NOT ARGUED**, and its top two items are algorithmic, CPU-only, exact, and delete most of E4's own premise: stage B's null is exactly `G^T W G` over precomputed word-pair counts W (**bit-identical** contingency tables, max diff 0.0; 282.6s → 0.18s for 11 k × 500), and stage C's null is a **multivariate-hypergeometric** draw of the same table — O(k²) instead of O(n), because permuting `pred` against a fixed `succ` re-partitions the succ multiset into fixed-size buckets (p99 agreement to 3 significant figures at n = 3 340 / 37 495 / 259 324; **4.0× / 41.3× / 255×**). Together these take the 265s of null work under 10s. Third: narrowing the matvec operand to complex64 buys **1.48–1.77×** on the matvec — priced only, NOT proposed, since compute width is pinned at complex128 on purpose by `Organism.perceive`'s dtype guard and taking it would move every committed number. Verdict: **E4 DEFER — recorded as the useful answer, not a failure.** The GPU tier was scoped for a workload that is 22.4% of the bill and that two exact CPU reformulations can shrink by ~30×; after them nulls would be under 1% and E4's premise would be gone. Revisit only if a future phase makes nulls dominant again (a much larger V, or per-occurrence rather than per-word tests). | `phase42_perf_rebaseline.py` — done; E4 deferred with numbers |

| 43 — phase-channel audit and the storage fork (T7.1; owner work order; opened+run 2026-08-09) | T1.9 closed the cost branch with "nothing left to engineer", which is established for **lossless width narrowing** (33g) but never was for a **representation change**. The floor's dominant term says a field memory is a COMPLEX N-vector where a prototype is a REAL one (2.03× per stored memory). Do the stored vectors actually USE the imaginary channel? Metric: `R(v) = min_θ ‖Im(e^{−iθ}v)‖²/‖v‖² = (1 − |Σvᵢ²|/Σ|vᵢ|²)/2` — zero exactly when v is a global rotation of a real vector, 0.5 for an isotropic complex one (`organism_compress.real_phase_residual`; the `real_phase` CompressionSpec lever stores the layout, default OFF). **THE MISSES FIRST. (1) The work order's own mechanistic account is FALSIFIED, and this is the phase's main finding.** It held that `dz = 1j*omega*z + g_in*(x−z)` is a content-blind global rotation, so for real inputs the reachable set is *exactly* {e^{iθ}·real} and the emptiness is STRUCTURAL. Deriving the loop instead of describing it: `normalize()` rescales by a real factor, so the direction obeys `z_t ~ a z_{t−1} + b x_t` with `a = 1 − g_in·dt + i·omega·dt`, i.e. `z_t ~ Σ_j b a^j x_{t−j}` — a **phasor sum whose per-lag coefficients have different arguments**. The reachable set is the complex span of recent inputs and collapses to {rotation of real} only as omega→0 or as settling completes within a token. Measured (digits, K=112, seed 0), median R: omega=0 gives **exactly 0.000e+00 at every hold**; then R ∝ omega² — 2.58e-05 / 2.55e-04 / 2.76e-03 / 1.26e-02 at omega = 0.05 / 0.15 / 0.5 / 1.0 (hold=8), reaching **0.33 by omega≥4**, and it PEAKS at shallow settling (hold=2–4, e.g. 1.36e-02 at omega=0.5/hold=2 vs 2.76e-03 at hold=8). The channel is empty by **parameter regime** (omega/g_in = 0.0375, hold=8 ≈ 2× the 1/(g_in·dt) time constant), not by construction. A two-token closed form `u = a^hold·x_prev + (1−a^hold)·x` predicts measured R to within 1.15–1.35× for omega ≤ 0.5 at hold ≥ 4 and breaks down at hold ≤ 2 or omega ≥ 2 (it is a 2-token truncation of a 4–5-token mixture) — reported as a bound with its range of validity, not a law. Consequence for T7.6: **content-dependent phase is not a new mechanism, it is a parameter setting** — perceive already computes a phasor sum. **(2) The behavior null is WEAKER than it looks, and the pre-registered positive control is what showed it.** Substituting the de-rotated real part costs `dACC = +0.0000` eval-only on **all ten seeds** — but `LabelEvidenceReadout` scores `np.abs(...)`, so M3 asked whether the metric can see the channel at all. Trained on a synthetic genuinely-complex stream: at median R = 5.96e-02 the substitution still costs **exactly 0.0000**, and only at R = 0.462 does it cost −0.1256. **The readout's resolution floor is R ≈ 0.06, and the measured store sits ~230× inside it.** The evidence for emptiness is therefore the residual measurement, NOT the accuracy null; anyone quoting `dACC = 0` without this paragraph is quoting a metric operating below its resolution. **(3) The work order's sandbox arithmetic is CONTRADICTED.** It anchored the new floor at "~1.02× and the total → ~1.13×". 1.02× is the *memory term alone*; the total also carries 33h's graph term, which this layout does not touch. Derived **before** measuring (33h's process rule): memory 1.047 × (112/115) = 1.020, graph 10.46/29.44 = 0.355, byte ratio 1.375 × (0.865/0.903) = **1.32×**. Measured on held-out seeds: **1.318×** (selection 1.323×). The derivation is right and the anchor is wrong. **(4) Parity is still out of reach and this lever cannot get there** — parity needs a byte ratio ≤ 1.044 and the memory term alone is 1.020, leaving ≤ 0.7 KB for a graph costing 10.5 KB. **(5)** One slot per text corpus sits at R ≈ 0.40 (the max), i.e. a recruited-but-never-refined slot still carrying its complex initialization. **WHAT HELD.** P1: median R = **2.58e-04** (s=0–4) and **2.55e-04** (held-out s=5–9) over 560 live slots each, 92.4% below 1e-3 and 99.8% below 1e-2 — reported as a distribution, not a mean. Cross-corpus, at the phase 19/20 recipe: fables 413 live slots median 1.65e-04 with **410/413 below 1e-3** (the sandbox anchor was 416/418 — it reproduces), gutenberg8 1200 live slots median 1.17e-04 with 1199/1200 below 1e-3. P3: store-mode `dACC` held-out mean −0.0005, range [−0.0050, +0.0025], max |Δ| 0.0050 inside the pre-registered 0.010 band and well inside 33e's 0.02 claim threshold. P4: rotating every slot by an independent random phase moves ACC by **exactly 0** and leaves slot-argmax agreement 1.0000 — **the per-slot phase is GAUGE** for every `np.abs` consumer, so the layout's 4-byte scalar buys E3 bitwise round-trip rather than behavior. 33h's anchors reproduce exactly: c64+CSR **76.10 KB/ACC-pt = 2.236×** on held-out seeds (33h: 76.1, 2.24×), decomposing memory 1.981 + graph 0.356 = 2.337 × 0.958. **M1, the mundane account, is neither confirmed nor dismissed — it is measured and left standing.** A census of every `perceive(` call site (54 files) found **no committed pipeline that builds a complex-valued stream**; every one casts real embeddings. So the emptiness cannot be attributed to the mechanism over the input class from existing pipelines alone. Constructing the missing input class shows the width IS usable (median R 0.462 at imag=1.0), which is precisely what branch (A) would forfeit. **THE FORK, PRICED AND NOT CHOSEN — this is the deliverable.** **(A) SPEND IT**: real+phase store = **44.83 KB/ACC-pt = 1.32× the bar, the ≤2× fallback MET**, at a measured behavior cost of ≤0.005 held-out; permanently forfeits the imaginary channel — N8's binding code, the path-encoding T7.5/T7.6 would need, and any future complex-input pipeline. **(B) CASH IT IN**: keep the width, floor stands at **76.10 = 2.24×**, and the option-(a) gate re-scope is forced — but the width becomes usable by parameter change rather than new mechanism, and T7.6(b) now has a measured bar to clear: any phase-carrying arm must move the readout past its R ≈ 0.06 resolution floor or it will score as a null no matter what the dynamics do. Harness §15 (10 checks) pins the metric's two analytic fixed points, the omega=0 exactness, the falsification banded so a reversal must be earned, the identity `layout loss = sqrt(R)`, and gauge invariance. | `phase43_phase_channel_audit.py`, `text_recipe.py`, `organism_compress.py` (additive, default-off) — done; **the fork is with the owner** |

| 44 — sparse P as the default path (T7.2; owner work order; opened+run 2026-08-09) | The work order puts this second because "the graph contributes 0.36× to the cost floor" and 33g measured P at ~6% density; T6.3 had already built `SparseTransitions` and moved `next_hops` onto a shared Dijkstra with the dense body pinned bitwise. **THE MISSES FIRST, AND THE FIRST ONE IS A PREMISE. (1) One premise of the ask was already spent, and the phase says so before it measures anything else**: 33h's 0.36× graph term is **not a dense number waiting to be sparsified — it IS the CSR number** (33g's headline spec is c64 + CSR at floor 0, and 33h priced the floor with it). At the floor arm a dense float64 P is 100 352 B and a dense float32 P is 50 176 B against a measured graph term of **10 116 B** (nnz 1208, density 0.0963). **CSR-as-the-default-storage-path therefore moves the cost floor by exactly 0.000×**, and the phase had to look where the project had never priced: the CSR encoding's own dtypes, and the COMPUTE default. **(2) The work order's own mundane account FIRED — density is not scale-free.** Measured at fixed K by scaling observations 16×: digits K=112 goes 0.0988 → 0.1500 → 0.2188 → 0.2857 → **0.3740 (3.79×)**, gutenberg8 K=1200 goes 0.00766 → 0.0116 → 0.0185 → 0.0284 → **0.0466 (6.08×)**. Both far exceed the pre-registered ≤2× saturation rule, so **P3 MISSED as written**: growth is sublinear (≈ n^0.5 on text, ≈ log n on digits) but does **not** saturate inside 16×. The consequence is a scope rule, and it applies retroactively: **every CSR byte number in this project needs an observation count attached** — phase 42's "3.64% dense at K=1580" is a snapshot at one observation count, not an endpoint. What survives is the break-even: narrow CSR (4 B/nonzero) beats a dense uint16 store (2 B/entry) up to density 0.5, and the digits protocol at 16× observations is already at 0.374, i.e. within striking distance on the small-K arm. **(3) P4's crossover HELD but only just, and the honest reading is that sparse loses at every shape this project actually reasons at.** Including the O(K²) build, `next_hops` dense/sparse is 0.72× at K=112, 0.82× at 200, 0.95× at 400, **1.15× at 500** (the crossover, inside the pre-registered [200, 600]), 1.28×/1.58×/1.73× at 600/700/800 and 2.84× at 1580. `rollout` is a NEGATIVE at every K (0.74–0.91×) and full `kstep` is a strong negative (0.08–0.23×), both reproducing T6.3. `kstep_row` wins from K≈200 (1.18× → 3.45×). **WHAT HELD. P2 landed on its derivation to the byte.** Two lossless narrowings the project had never taken — int16 CSR indices/indptr where the shapes provably fit, and unsigned-integer counts where they round-trip exactly, both guarded the way 33g guards float32 counts — were **derived first** as `4·nnz + 2·(K+1) = 5058 B` and **measured at exactly 5058 B**, reconstructing P identically. The floor, held-out seeds 5–9, against the pre-registrations in brackets: c64+CSR **76.10 KB/ACC-pt = 2.236×** [2.24], c64 + narrow CSR **70.30 = 2.066×** [2.07], real+phase + CSR **44.83 = 1.318×** [1.32], real+phase + narrow CSR **39.04 = 1.147×** [1.15]. **The pre-registered decision rule fired the way it was written: the ≤2× fallback is still MISSED by graph engineering alone, at 2.066×.** That settles a structural question — **the fallback is decided by the complex-vs-real term and never by the graph**, because the entire graph term is 0.36× and the gap to close is 0.24×. It also reconciles T7.1's contradiction with the work order: **1.147× is the sandbox's unreproduced "~1.13×"**, so the sandbox was almost certainly pricing a narrowed graph as well as a real store and reported only the store. **THE DEFAULT CHANGE, AND WHAT IT DELIBERATELY DOES NOT DO.** `TransitionGraph.next_hops` gains `sparse=None` auto-dispatch on `SPARSE_MIN_K = 800` **and on integer counts** — T6.3's own condition for bitwise identity, so under `p_decay > 0` the default stays dense rather than silently becoming a tolerance. The threshold is set at T6.3's ≥2× point rather than at the measured 500 break-even, on purpose: phase 42 measured ±20% wall-clock variance on bit-identical work on this host, so a break-even-tuned constant would not be portable. The consequence is stated rather than hidden — **the dispatch is INERT on every committed benchmark** (phases 30/40 reason at K ≤ 60, the corpus tier at K ≤ 1580 never calls `next_hops`), so this default change cannot move an anchor and does not. `rollout` and `kstep` keep their dense defaults, exactly where T6.3 measured negatives. Harness §16 (10 checks) pins the dense/sparse bitwise agreement, both dispatch conditions separately, narrow-CSR losslessness and its guard declining non-integer counts, and — banded so the claim must be re-earned — the FALSIFIED scale-free reading of density. | `phase44_sparse_graph_default.py`, `organism.py` (`next_hops` dispatch), `organism_compress.py` (`p_narrow`, default-off) — done; floor unmoved by the graph, **narrow CSR available and priced** |

| 45 — settling-depth sweep (T7.3; owner work order; opened+run 2026-08-09) | Every committed text recipe holds each token for 8 frames at `g_in·dt = 0.25`, about twice the field's own time constant `1/(g_in·dt) = 4`. Sweep the knob against slot coverage, phase-24 category validity, and a path-sensitivity index `PSI = (ceiling − suffix)/(ceiling − floor)`, where CEILING is two runs of the same token sequence from DIFFERENT random initial states, SUFFIX is two sequences sharing only the last token, FLOOR is two with different last tokens — all in the organism's own `|⟨a,b⟩|/N`. The field recursion does not read the memory bank, so the end state is computed directly and **checked against `Organism.perceive`'s own `org.z` rather than assumed** (max |Δz| = 9.2e-10). **THE MISSES FIRST. (1) A PROCESS MISS, recorded rather than hidden**: this phase's predictions were authored before its first run but committed after it (the docstring is unchanged from the version that produced that run and the run's output was preserved, but B.6 asks for the commit to land first, and it did not). Phases 43, 44, 46 and 47 all pre-registered in the required order. **(2) The mundane account is FALSIFIED, and the way it fails re-frames the whole question.** M3 held that `1/(g_in·dt) ≈ 4` frames explains everything, there is no window, only an EMA, and hold≈3 "works" solely because settling is incomplete — i.e. the state is NOISY, not path-aware. The pre-registered discriminator was reproducibility across initial conditions, and it says the opposite: **1-NN prefix decoding at a FIXED final token is 1.000 at every hold from 1 to 12** against a null p99 of 0.083, with ceiling ≥ 0.99 from hold=2 up. The prefix information is not noise, and it is not confined to a window — **it is present and perfectly decodable even at hold=12, where PSI is 0.0031**. (3) So the work order's framing of "a window at hold≈3" is wrong in an instructive way: what the sweep actually moves is not whether prefix information EXISTS but its MAGNITUDE in similarity units — signal `ceiling − suffix` = 0.375 / 0.406 / 0.307 / 0.207 / 0.078 / **0.027** / 0.003 at hold 1/2/3/4/6/8/12 — and **every consumer inside the mechanism is a THRESHOLD on that similarity** (`recruit` 0.75, `active_bar` 0.6, `fuse_bar` 0.7, `merge_thresh` 0.8). Path sensitivity is a property of the CONSUMER, not of the state. Quantified rather than asserted: with added state noise the decode holds to σ=0.03 at hold=8 (0.996) and collapses to 0.275 at σ=0.1 for hold=12, so the usable margin tracks the signal. (4) One disagreement with the work order's own flag: it says `merge_thresh = 0.8` sits below suffix "from hold=4 upward". **Measured, the crossing is between hold=4 (suffix 0.7931) and hold=6 (0.9219)** — at hold=4 the threshold is still above suffix. The flag itself stands and is now dated: from hold≈5 up, distinct suffix-sharing sequence states would be merged by `consolidate()`'s default, and that threshold remains **untested for that use** because nothing in the repo consolidates sequence states today. **WHAT HELD.** P1 exactly: at hold=8 ceiling **1.0000**, suffix **0.9735**, PSI **0.0302** — a pure last-token attractor that has forgotten its own initial condition, against the sandbox's 1.0000/0.980. P2: PSI(hold=3)/PSI(hold=8) = **21.7×**, far past the pre-registered 5×. The other knob reproduces the same curve at FIXED frame count — at hold=8, `g_in·dt` = 0.05/0.10/0.15/0.25/0.40/0.60 gives PSI 3.09/0.774/0.255/0.030/0.0008/0.0000 — so this is settling depth and not exposure length. Sandbox agreement is good on suffix at hold=3 (0.693 vs its 0.703) and poor on ceiling (0.9998 vs its 0.945); the likeliest cause is sequence sampling (random vocabulary draws here vs corpus windows there) and the disagreement is recorded, not reconciled. **P4 HELD, AND THE TRADE IS STEEP — this is the priced frontier the target asked for.** Word coverage on the phase-19 recipe: 128 / 199 / 265 / 301 / 320 / **327** / 321 of 376 at hold 1/2/3/4/6/8/12. Category validity (phase 24's class-bigram MI against a label-permutation null) z = 1.3 / 2.1 / 2.6 / 6.8 / 10.5 / **9.5** / 9.3 — **at hold ≤ 3 the discovered categories do not clear their own null at all**. So the "window" costs 62 words of coverage and the category certificate outright. There is no free lunch and hold=8 is not over-settling. **Consequence for T7.6, and it is the useful one**: (c) the dual-time-constant arm is the only one of the four that buys path sensitivity without paying this bill, because it adds a SECOND state at a slower `g_in` instead of moving the one state everything else reads — and phase 43 already priced a second O(N) state at ~0.001× of the K×N store. | `phase45_settling_depth.py` — done; the window is real but it is a consumer-resolution question, and it costs the category certificate |

| 47 — macro-recruit pre-gates (T7.5; owner work order; opened+run 2026-08-09) | Three questions before any chunk mechanism is built: does a principled chunk-SELECTION rule beat the obvious ones, does a chunk INVENTORY saturate, and is there anything to chunk at the CATEGORY level — which is phase 29 / T2.4's pre-registered "level 2 learns nothing" risk one level up. A chunk is a bigram given its own symbol; the measure is a two-part description length in nats (merge non-overlapping left-to-right BPE-style, score the rewritten stream under an add-α first-order Markov model, add the table cost), so both arms are valid codes for the SAME original stream. Two method changes from the sandbox, both tightening: selection is a search over ~10⁵ candidates, so **every arm selects on a training half and is scored on a disjoint held-out half over 5 folds** (the sandbox scored in-sample), and categories come from **`discover_categories_v2` at slot level** rather than a word-level PPMI k-means proxy. **THE MISSES FIRST, AND THERE ARE FOUR. (1) P2 FAILED AND ITS PRE-REGISTERED CONSEQUENCE WAS HONORED**: under the predecessor-permutation null the FREQUENCY arm still gained **+2286 nats**, so by the rule written before the run "the measure is broken and nothing else in the phase counts". Diagnosis: merging a frequent bigram shortens the stream and enlarges the symbol table, and an add-α model refitted on the longer table fits its own data better — a length/estimation effect present with **no sequential structure at all**, and frequency-selection maximizes exactly it. Sections 1–5's raw numbers are therefore reported as diagnostics of the measure, not as pre-gate answers. **(2) P1 MISSED on the pre-registered statistic**: held out, plain frequency beats expected-total-gain at every M (M=64: 2519.8 vs 1589.9 vs divergence −2221.4), with 5/5 sign flips against total-gain — the exact reverse of the sandbox's in-sample +2908 / +1740 / +880. The M1 check clears total-gain of being "frequency wearing a hat" (selection overlap 0.55–0.62), so the two rules genuinely differ and the raw statistic genuinely prefers frequency. **(3) P3 MISSED — the sandbox's own UNK confound does NOT reproduce under held-out scoring.** The UNK *share* reproduces exactly (**0.253**, sandbox 0.253) and 21.2% of selected chunks contain UNK, but the gain ratio is **1.15×**, not the predicted ≥2×, against the sandbox's ~4× (+2908 → +748). The scope sentence that must travel with any word-level number: the payoff is **0.93% of total cross-entropy** under either OOV policy. `oov="drop"` remains the reported default. **(4) P5 MISSED AND P6 FIRED — this is the phase's most consequential result and it goes against the sandbox's strongest claim.** The category arm clears the stream-permutation null comfortably (810.3 vs 350.4 nats) **and FAILS the label-permutation null outright — 810.3 measured against a null of 1279.8**, i.e. a *random* partition at fixed category sizes compresses the stream BETTER than the discovered one, on 5/5 folds. That is precisely the artifact M5 named: any clustering of a Zipfian stream manufactures apparent second-order structure, and only the label-permutation control kills it. Mechanistically it is not surprising in hindsight — `discover_categories_v2` groups distributionally SIMILAR words, which flattens the category-level bigram, while a random partition leaves categories whose bigrams are dominated by a few frequent members. **Pre-registered consequence, applied: phase 29's "level 2 learns nothing" FIRES on this corpus, T2.4 stays blocked on its own honest negative, and no chunk mechanism should be built at the category level.** Category-level allocation is 12.66 nats/slot against word-level 24.84 — the reverse of the sandbox's ~+91 vs +11.7. **WHAT HELD. P4 HELD and its mundane account was dismissed by the measurement**: held-out gain peaks at **M=64** (955 → 1407 → 1590 → 1016 → −1419 → −7123 at M=16…512), a marginal block goes negative from M=128, and it does so **even with the table cost removed** (2547 → 1642 → −1001), so the reversal is language, not accounting. An unbounded chunk gate loses information, and the curve supplies the stopping rule phase 41 lacked. **P7 HELD — the repair, committed before the run that tested it.** The statistic that answers the question asked is the NULL-CORRECTED gain (`real − permuted`, matched per fold, rule and M), because the length/estimation term is common to both and cancels. Being post-hoc, it was given what a post-hoc repair needs: the corpus was halved, the repair exercised on selection folds, and the ordering **confirmed on folds no selection had touched**. On the confirmation half, expected-total-gain beats frequency-only on **every fold at M=32/64/128** (M=64: 1984.2 vs 51.1 corrected; 0/5 flips), and M7's discriminator says the correction does real work rather than shifting a constant (null spread across rules 2994 vs real spread 2419 at M=64). **So the sandbox's chunk-selection claim survives — but only on a repaired measure, only at the word level, and only worth ~1% of cross-entropy.** | `phase47_macro_recruit_pregates.py`, `text_recipe.py` — done; **T2.4 stays blocked**, word-level selection rule survives on the corrected statistic |

| 46 — single-pass audit (T7.4; owner work order; opened+run 2026-08-09) | "Single-pass online" is the first capability axis 33h's owner-decision paragraph names for a re-scoped gate — but phase 19's recipe, inherited by 20/21/22/23, runs **fifteen epochs** over the same corpus. A re-scoped gate must not rest on an axis the production recipe violates. Re-measure the three headline axes at ONE epoch against the multi-epoch numbers, paired seeds, on both corpora: word coverage (phase 19's own metric and consolidate settings), category validity (phase 24's class-bigram MI against a label-permutation null), polysemy detection (phase 12/21's predictive split gain against phase 21's directly measured noise floor). **Explicitly an audit and not an improvement target** — a large drop was pre-registered as a legitimate and important finding. **THE MISS FIRST, AND IT IS THE PHASE'S HEADLINE. P1 MISSED — and in the direction that makes the claim stronger, not weaker.** The prediction was that one epoch loses ≥10 percentage points of coverage, on the stated mechanism that `recruit` is a similarity floor and rare words need repeated exposure to escape absorption. Measured on fables, 1 epoch vs 15: **327/376 → 320/376**, a coverage *drop* of 7 words from the extra fourteen passes, with consolidated memories falling 405 → 356. At scale (gutenberg8, 1 vs 3 epochs — 15 was omitted at 3.3M frames/epoch and that omission is a real limit of this audit): **387.3 → 388.3 of 395**. So the epochs are not buying coverage; they are fusing memories that one pass had already separated. **The mundane account M1 therefore lands, but backwards**: the honest phrase is "single-pass capable, multi-pass tuned" only if "tuned" means the category certificate, because that is the one axis the passes move. **WHAT HELD.** P2 HELD on both corpora: a single pass clears the permutation null at **z = 9.1** (fables) and **z = 32.0** (gutenberg8), strengthening to 12.7 / 38.3 with more passes — categories are read off transition counts, which accumulate at full rate within one pass, exactly the stated reason. P3 HELD: words above the noise floor go **50.0 → 51.3** on gutenberg8 (+3%, inside the ±20% rule), so re-reading a fixed corpus does not manufacture detection signal — the statistic is reading the language, not the organism's convergence, which was the serious failure mode the decision rule was written to catch. **Two scope sentences that must travel with these numbers.** (i) The 1-vs-15 comparison exists only on the 2 431-token fables corpus; the large corpus was audited 1-vs-3. (ii) The detection axis has a wide three-seed spread (39/39/71 at one pass, 28/62/64 at three), so the correct statement is "not epoch-sensitive at the resolution three seeds resolve", not "epoch-invariant" — and on fables the axis is not measurable at all (one candidate word clears the n≥100 bar in 2 431 tokens, and zero clear the floor at every epoch count), which is phase 19's own n=200–500 finding reappearing rather than a null result. **NOVELTY updated in the same commit**, per the pre-registered rule: the one-sentence claim's "single-pass" clause now carries the audit, its numbers, and both scope sentences. | `phase46_single_pass_audit.py`, `text_recipe.py` — done; **single-pass survives on every axis measured**, multi-pass buys only the category certificate |
| 39 — retention mechanism study (T6.2; consolidation posture; opened+run 2026-08-11) | 33b found a slot budget and a law for it, but swept only ONE of its knobs, so "retention" was an emergent property of one operating point rather than a tunable one. Factorize it: eviction window E, victim rule (argmin-count vs count-normalized-by-era vs a RANDOM-eviction control), `p_decay`, probation/confirm, slot headroom K — one factor at a time off a fixed reference cell (K=40, E=250, victim=count, confirm=0), paired seeds s=0–4, held out s=5–9, baseline reseeded on the same split, exact paired sign-flip nulls. **SCOPE, CARRIED BY EVERY NUMBER IN THIS ROW: these are retention knobs AT 33c's BLOCKEDNESS. Phase 38 measured that the forgetting here is produced by SEQUENTIAL BLOCKING; 33c is one fully-blocked point on that axis and T6.6 (phase 49) is what varies it.** New hook, minimal and default-off: `perceive(evict_victim=...)` in organism.py + fastpath.py (`'count'` default; `'rate'` = argmin count/tenure, the LABEL-FREE reading of "count-normalized-by-era" — era is a slot's own occupancy tenure, never a task index; `'random'` = the control arm), plus `org.tenure` and `org.evict_rng` round-tripping additively in E3 (no schema bump). **THE MISSES FIRST. (1) PREDICTION (a) IS NOT SUPPORTED: the victim rule does NOT matter less than the window. Pooled n=10 FORG spread of the level means — full range window 0.4244 vs rule 0.4386; settings-only 0.4244 vs 0.4386; with each axis's own catastrophic setting dropped, window 0.0554 vs rule 0.0536, a 0.0018 gap that is rounding, not a ranking. The two knobs have indistinguishable leverage, and 33b's "the window is the load-bearing knob" was an artifact of never having varied the other one. (2) `p_decay` (F3) WAS UNMEASURABLE ON THIS PROTOCOL — a gap in the target's factor table, not a null. Every level returns byte-identical ACC/FORG/task-0; the parameter is wired (graph mass 2782 → 93.6 at p_decay=0.01) but touches ONLY the transition graph, leaving `xi` bitwise unchanged (max|dxi| = 0.0e+00), and 33c's readout decodes from `xi` overlaps alone and never consults the graph. Measuring transition decay as a retention factor needs a metric that consumes P (next-symbol prediction, planning); deliberately NOT patched by swapping protocols mid-study. (3) The confirm/probation win below is GRID-SELECTED and is quoted at its held-out value only. WHAT HELD. (c)-strong CONFIRMED and it is the phase's main result: random eviction underperforms argmin-count at EVERY window — dACC −0.279/−0.282/−0.327/−0.339 and dFORG +0.457/+0.439/+0.465/+0.446 at E=100/250/500/750, 10/10 sign census, exact paired p=0.0020 (the n=10 floor) on both axes. The simplification branch of (c) does NOT fire: the victim rule is doing real work. (b) HOLDS, harder than written — count-normalized-by-era shows no advantage anywhere and is affirmatively WORSE on FORG at all four windows (+0.040 p=0.0098, +0.054 p=0.0059, +0.060 p=0.0020, +0.035 p=0.0391), with task-0 final falling 0.90 → 0.70; 33f's refusal to indicate H1 is vindicated by direct test. ALL THREE PRE-REGISTERED MUNDANE ACCOUNTS WERE REJECTED, so none of this is true by construction: the rules diverge at the first or second eviction and reach different eviction totals (E=250: count 171, rate 110, random 98), 'rate' really reorders the pool (victim mean lifetime count 57.0 vs count's 24.9), and — the hardest one — random evicts memories 4–15× BETTER ESTABLISHED than argmin-count does (victim lifetime count 217.1 vs 14.4 at E=100; 229.4 vs 24.9 at E=250; 212.5 vs 44.5 at E=500; 233.8 vs 57.6 at E=750), so the stale pool has large spread and argmin-count is exploiting it. THE LAW GAINS A SECOND CLAUSE. 33b: "the stale pool must contain the present, or eviction eats the past." Phase 39 adds: **AND THE POOL MUST BE ORDERED BY HOW ESTABLISHED ITS MEMBERS ARE, OR EVICTION EATS THE PAST JUST AS FAST.** Sharp form — **a MIS-SET budget is worse than NO budget, and this is symmetric across both knobs**: no budget (E=0) ACC 0.551 / FORG 0.291; window mis-set (E=2000) 0.439 / 0.577; victim mis-set (random at E=250) 0.449 / 0.591; both set well (E=250, count) 0.730 / 0.152. E=0 is not the worst cell on either axis. WHAT IS ACTUALLY TUNABLE (pooled n=10, paired, exact null, sign-consistent on the held-out seeds): slot headroom K is the largest single gain and roughly monotone — K=96 dFORG −0.111 / dACC +0.127 (10/10, p=0.0020), K=72 −0.100 / +0.109 (10/10) — though K=56 wobbles out of sample (2+/3−), so monotonicity is a trend and is not pinned; and probation/confirm, phase 14's provisional-slot machinery which 33c's own recipe leaves OFF at confirm=0, is a real retention knob (confirm=3/probation=1000: dFORG −0.062 10/10 p=0.0020, dACC +0.056 p=0.0059) — NOT pre-registered, found by the sweep, so quoted held-out and not offered as a tuned recommendation. No torch on the run host: the ladder's four MLP arms are quoted from 33c's committed run and were not re-measured.** Pinned: harness §18 (13 checks, both backends) — the phase-39 sign, the pool-spread discriminator, default-path neutrality (omitting `evict_victim` == `'count'`, EXACT 0.0), tenure inertness off the rate rule, E3 round-trip for all three rules, and a typo'd rule raising instead of silently running the default. The branch's DEFAULT path is proven **bitwise identical to origin/main on both backends**, so no committed anchor could drift. | `phase39_retention_factors.py` — done; pinned in harness §18 |


| 50 — fork decision measurement: the (A) store on the ladder (T7.7; owner ruling 2026-08-11 "measure before choosing"; opened+run 2026-08-13/14) | T7.1 priced the storage fork and refused to choose; the owner declined to choose blind — nobody had run the (A) `real_phase` store and the accuracy arm in the same experiment, phase 43's ≤0.005 bound was final-ACC-only on the cost-frontier protocol, and FORG (where a lossy store should hurt first) was never reported for the layout. Protocol: 33h's `run_arm` VERBATIM in store mode (compress at every task boundary, carry forward), K=112 (33h's floor arm) and K=160 (33d's bar-crossing arm), arms c64 (33g headline spec) vs rp (`real_phase`), argmax + calib-b8 scored off the same trained organism per arm, paired s=0–4, held out s=5–9, prototype bar recomputed on each seed's own split, replay reseeded per split (torch 2.13-cpu), bootstrap CIs (B=20000) on paired deltas, every byte number under CSR and narrow CSR with observation counts attached (phase 44's rule). Pre-registration disclosed that the mandated timing probe exposed five seed-0 cells before commit; all confirmations ride on held-out seeds and rp-at-K=160, which the probe never touched. **THE MISSES AND CAVEATS FIRST. (1) The probe's most interesting observation DIED on held-out seeds, and it is the run's best advertisement for the discipline**: seed 0 showed the K=160 c64 calib arm ABOVE replay (0.9301 vs 0.9100); held out, replay wins on ALL FIVE seeds (organism − replay: −0.0012 to −0.0461, mean −0.0215). Replay still tops the ladder (held-out 0.9187/0.0834; FORG differs from 33c's committed 0.105 by rng provenance, disclosed in-run). (2) **The largest fork delta observed is the one the charter said to look for**: dFORG argmax at K=112 held-out **+0.0034 [+0.0000, +0.0089]** — rp forgets marginally more, sign consistent with "lossy store hurts FORG first", but 6× inside the pre-registered kill line, inside the 0.010 band, and it does NOT replicate at calib-b8 (−0.0006) or at K=160 (−0.0050, rp *better*). Recorded as the number to watch. (3) K=160 at calib-b8 (held-out 0.8972) is NOT a better operating point than K=112 (0.9029) — 33h's knee logic holds; K=160 is here as the fork test at 33d's bar-crossing capacity. **EVERY PRE-REGISTERED PREDICTION HELD.** P1 anchors: bar s0 0.872/120 protos/30720 B exact; K=160 c64 argmax s0 0.9002 (33d/33g's 0.900); K=112 held-out floors c64+CSR **76.10 KB/ACC-pt = 2.236×** and c64+narrow **70.30 = 2.066×** — phase 44's committed values to the second decimal; rp **44.88 = 1.319×** / rp+narrow **39.07 = 1.148×** vs 44.83/39.04, drift +0.05/+0.03 KB/pt inside the registered attribution band (43/44 priced the layout on the uncompressed arm's accuracy; this phase prices it as deployed). **P2 — THE BOUND TRANSFERS, which is M1, the pre-registered mundane account, and it is the phase's answer**: worst held-out mean dACC **−0.0012** (band ±0.005), worst held-out mean dFORG **+0.0034** (band ±0.010), kill line (dFORG ≥ +0.02 / dACC ≤ −0.02, the pre-named "not worth taking at any saving" threshold = 33e's claim bar) never approached — both K, both decoders, store mode. Held-out calib-b8 deltas: K=112 dACC −0.0005 [−0.0030, +0.0015], dFORG −0.0006 [−0.0019, +0.0000]; K=160 dACC +0.0019 [+0.0000, +0.0057], dFORG −0.0018 [−0.0071, +0.0017] — at K=160 rp is nominally better on both axes, quoted as inside-noise, not a win. P3: the (A) discount is **K-independent** — rp/c64 price ratio 0.590 (K=112) / 0.581 (K=160) vs the derived 0.58 ± 0.02, because it lives entirely in the xi term. P4: replay reseeded held-out 0.9187 ∈ 0.91 ± 0.02. **THE COST TABLE, held-out, bar recomputed per seed (34.03 KB/ACC-pt), every byte number at frames=33600 with sum(P)/nnz/density attached**: K=112 — c64+CSR 68.70KB = 76.10 = 2.236×, c64+narrow 63.47KB = 70.30 = 2.066×, rp+CSR 40.49KB = 44.88 = 1.319×, rp+narrow 35.25KB = **39.07 = 1.148×** (nnz ≈1251, sum(P) ≈4556, density 0.100); K=160 — c64+CSR 96.45KB = 107.52 = 3.160×, c64+narrow 89.83KB = 100.13 = 2.943×, rp+CSR 56.15KB = 62.46 = 1.836×, rp+narrow 49.51KB = 55.08 = 1.619× (nnz ≈1576, sum(P) ≈4892, density 0.062); replay 89.6KB at 0.9187 = 97.65 = 2.870× (33h's byte convention). Scope: KB/ACC-pt at unequal accuracy answers a narrower question than the gate's conceded cost branch; these are architecture-choice numbers. **OWNER DECISION ARTIFACT — THE FORK, MEASURED, WITH THE RECOMMENDATION T7.7 REQUIRES. RECOMMEND (A)**: adopt the `real_phase` + narrow-CSR layout as the DEPLOYMENT PERSISTENCE encoding — 39.07 KB/ACC-pt = 1.148× the bar against (B)'s 70.30 = 2.066×, at a measured behavior cost indistinguishable from zero on the ladder (±0.005 ACC / ±0.010 FORG held-out, both K, both decoders) — keeping complex128 as the COMPUTE width (E2 contract untouched) and `real_phase` DEFAULT-OFF in code until the owner flips it. The mandated scoping note: T7.6's arms (c) dual time constant and (d) graph order are fork-independent by T7.6's own text, so (A) forfeits ONLY arms (a)+(b) — and those carry phase 43's measured R ≈ 0.06 readout-blindness bar and phase 16's measured 9–56-step superposition collapse; the fork text as written overstates what (A) forfeits, and the cheapest measured route to sequence memory (T7.6c) survives (A) untouched. Reversibility is asymmetric in (A)'s favor as long as the channel stays empty: (B)→(A) is a re-compression of stored state at any time, (A)→(B) loses only what the imaginary channel accumulated — and phase 43 measured that nothing accumulates at the committed parameters (median R 2.6e-04, gauge phase) with no committed complex-input pipeline in the repo (54-file census). What (A) actually spends today is an OPTION priced by the R ≈ 0.06 bar T7.6(a)/(b) would have to clear anyway. NOVELTY deliberately untouched: no claim changes until the owner rules — N8 already carries phase 43's measured emptiness, and a recommendation is not a ruling. Pinned: harness §19 (4 checks, both backends) — store-mode fork-equivalence bands and the (A)/(B) byte-ratio arithmetic at toy scale, so neither can drift silently. Run artifacts: `phase50_results_{bar,K112,K112_resume,K160,replay,report}.txt` + `phase50_cells.json` were written to the RUNNING SESSION'S working tree and **never committed**; that container was ephemeral, so **they no longer exist** (owner audit, 2026-08-14 — `.gitignore` did not exclude them, they were simply not added). Nothing is lost scientifically: every number above is printed by the run that measured it and `phase50_fork_ladder.py` reproduces them. Standing lesson, and it is now a rule: **an outcome block may not cite evidence that lives only in a working tree** — commit the artifact or cite the script that regenerates it. | `phase50_fork_ladder.py` — done; **the fork is measured and back with the owner, recommendation (A)**; phase 48 stays reserved until the ruling |

## Engineering track (parallel; feeds both paths)

| Item | What | Status |
|---|---|---|
| E1 — regression harness | `regression_harness.py`: multi-seed, tolerance-based checks pinning the mechanism's headline behaviors (core perceive/recall/consolidate in plain, confirm, pool, and amb modes; category discovery; predictive-gain margin; since 2026-07-13 also phase 30 symbolic reasoning (§6) and phase 26 percentile bars (§7) — 27 checks total (E3 serialization joined as §8); since 2026-08-05 also §9 slot budget/eviction (T1.2, phase 33b); since 2026-08-06 §10 store compression (T1.8); since 2026-08-07 §11 stable symbol registry (T3.3); since 2026-08-08 §12 logic-layer depth (T6.3, phase 40 — the shared-Dijkstra refactor pinned BITWISE against the pre-refactor body kept in the phase script, the confidence-bound invariants, the compositional planners certified against brute-force optimal walks, sparse-port equality, and the first-order macro null); since 2026-08-08 §13 detection-driven sense splitting (T6.4, phase 41 — the matched-capacity invariant is pinned exact, since the whole comparison rests on it); since 2026-08-08 §14 task-free continual learning (T6.1, phase 38 — the three conditions' structural invariants plus the exact A-vs-B boundary-signal invariance, and the two falsified predictions banded negative so a reversal must be earned); since 2026-08-09 also §15 phase channel + real+phase layout (T7.1, phase 43 — the residual metric's fixed points and the `loss = sqrt(residual)` identity pinned exact, gauge invariance pinned exact, and the falsified structural account banded POSITIVE at omega=2 so a re-claim must be earned); since 2026-08-09 also §16 sparse-P default path (T7.2, phase 44 — dense/sparse bitwise agreement, BOTH dispatch conditions pinned separately, narrow CSR losslessness and its guard, and the falsified scale-free density reading banded) and §17 settling depth + chunk statistics (T7.3/T7.5, phases 45/47 — the vectorized field recursion checked against `Organism.perceive`'s own z, the last-token attractor and the prefix signal's magnitude banded, and phase 47's raw chunk-gain statistic pinned POSITIVE on an i.i.d. stream because it FAILED its own null, alongside the clustering artifact the category arm died on) — 119 checks total). Statistical tolerances, not bitwise equality (GPU reductions will legitimately perturb low-order bits). Every backend/port must pass before it is trusted. Corpus-tier checks (phase 22/23 headline numbers) join the harness once E2 makes them cheap. | `regression_harness.py` |
| E2 — Numba port | JIT the sequential hot loops (perceive, recall/recall2, consolidate inner loops) — they are interpreter-bound, not arithmetic-bound; target 15 min → ~1 min at phase-23 scale. CPU is the right tool: the perceive loop is inherently sequential over the stream. Validated against E1. **Landed 2026-07-13** as `organism_numba.py` (opt-in `NumbaOrganism` backend, op-for-op kernels, 23/23 harness checks, 8× on pool+amb perceive at N=30/K=60 shape, ~2 min projected for phase-23 runs). **Upgraded 2026-07-15** — two sessions built E2 in parallel; the implementations were unified rather than kept side by side: `fastpath.py` is now the single kernel set (BLAS overlap matvec with a maintained conjugate, squared-magnitude argmax — profiling showed the per-slot `np.abs` hypot was half the frame cost — in-place z update, bounded-memory chunked streaming so generator streams never materialize, and consolidate's duplicate scan vectorized to one matmul, which does matter at corpus K≈1580), selected transparently via `Organism(backend="auto"|"numba"|"numpy")` / `DEFAI_BACKEND` env (default auto); `NumbaOrganism` remains as a thin alias so E3's cross-backend pin and existing imports hold. Validation: E1 green on BOTH backends, plus `test_fastpath_equivalence.py` (tier 0: numpy-vs-numba state agreement ~1e-15 on short streams in every mode; recall/recall2 committed hop sequences match exactly — identical RNG consumption order). Measured at true phase-23 corpus shape (`e2_benchmark.py`: K_CAP=1580, DIM=50, 4.9M frames): 18.2 min (numpy, projected) → **1.40 min (numba, measured) = 13.0×** — target met. Honest residual: throughput (58K frames/s) is now bounded by the K×N overlap matvec (memory-bandwidth, ~78K matvec/s at this shape) plus the per-token saccade pass, not the interpreter; the next factor is algorithmic (pruned candidate sets) or E4, not more JIT. Corpus-tier harness checks are now cheap enough to wire in (needs the Gutenberg fetch made reproducible first). **Re-baselined 2026-08-08 (T6.5, phase 42): the matvec bound HOLDS and is now measured rather than inferred — 10 927 ns of a 14 614 ns frame (75%) at K=1580/N=50, with K×rate flattening toward 1/K by K=3000. Two corrections to this row, both from measurement. (i) The parenthetical "memory-bandwidth" is wrong at these shapes: the operand is 1.2 MB and cache-resident, so the bound is CACHE bandwidth (84–94 GB/s effective) — which is why halving operand width to complex64 buys 1.48–1.77× on the matvec (priced in phase 42, NOT taken: compute width is pinned at complex128 by `Organism.perceive`'s dtype guard). (ii) The "58K frames/s" figure should not be read as a portable pin. On a 2026-08-08 4-core host, bit-identical work spans 43.7–58.2K (±20%), straddling it; a same-host interleaved A/B against pre-33g and pre-T3.3 trees found no tree-attributable difference (medians 49.8/50.1/50.6K), and the T3.3 registry (−2.4%) and a complex64 store (+3.6%) are both inside that noise. Detect throughput regressions with a same-session A/B, not by comparing to this number.** | `fastpath.py` — done |
| E3 — state serialization | Save/load of the full organism state (`xi`, `P`, habituation, pool/probation state) with a schema version so stored memories survive mechanism upgrades. Not product polish — an episodic-memory product is *defined* by persistence. Plus: bounded memory (slot cap + eviction policy), deterministic replay. **Landed 2026-07-13: `organism_state.py` — schema-versioned .npz (no pickle), rng generator state included. Pinned guarantees (harness §8): mid-stream save→load→continue is BITWISE identical to never stopping (max dxi = dP = 0); deterministic replay (restored recall reproduces the identical stochastic trajectory); cross-backend restore (reference-saved state continues on the numba backend, capture 0.995). Bounded memory: the slot cap is K by construction and eviction is the mechanism's own use-it-or-lose-it recycling — nothing separate to serialize. Consolidation products (mem/Pn/kept_idx) round-trip so a restored organism can recall() immediately.** (2026-08-05, T1.2: the slot budget gave eviction persistent state after all — org.age staleness clock + org.evictions tallies now round-trip; pre-T1.2 files load with zeroed clocks, an additive migration, schema version unchanged. Pinned in harness §9.) (2026-08-07, T3.3: **schema v3** adds the optional symbol registry — slot→symbol bindings, the ID counter, alias chains, tombstones, and the consolidation view, all bitwise. v3 without a registry is byte-for-byte the v2 layout; v1 **and** v2 files still load, into an organism with no registry — the state a pre-T3.3 organism had. `save_state_v1`/`save_state_v2` write the older layouts so backward load is tested against real files. Pinned in harness §11.) | `organism_state.py` — done |
| E4 — GPU tier (hipfire) | The embarrassingly parallel stages only — permutation nulls first (the statistical backbone, thousands of independent shuffles), then per-word gain sweeps, all-pairs similarity, PPMI/SVD at V≥5–20K. The sequential perceive loop is *not* a GPU target. Windows+AMD reality: Python-side ROCm (numba-hip, CuPy-ROCm) is Linux-only, so the intended shape is a Rust/wgpu (Vulkan/DX12) compute crate with PyO3 bindings — which doubles as the dependency-free native runtime a product fork can ship. Timed to land around Phase 27/28 when the statistics workload grows. **DEFERRED 2026-08-08 on measurement (T6.5, phase 42), which is the answer the target pre-registered as equally useful.** Phase 27 has now run, so the premise is testable instead of assumed, and it does not hold: permutation nulls are **265s of 1184s = 22.4%** of corpus-tier wall-clock, not the ≥50% that would have justified the tier. Making nulls FREE caps total speedup at **1.29×**; adding this row's other named target (all-pairs similarity — `coverage_map`, 105s) reaches 31.3% and **1.45×**. The bill is stage A perceive at **785s = 66.3%**, which this row correctly excludes as a GPU target. Worse for the premise: phase 42 priced two EXACT CPU reformulations of the null workload itself — stage B's table is `G^T W G` over precomputed word-pair counts (bit-identical, 282.6s → 0.18s) and stage C's is a multivariate-hypergeometric draw (O(k²) not O(n), 4–255× by word size) — which together take the null bill under 10s, i.e. below 1% of the run. **Do those first; they are days of CPU work against a Rust/wgpu crate, and they remove the reason to build it.** Re-open only if a future phase makes nulls dominant again (much larger V, or per-occurrence rather than per-word tests) — and re-measure before committing, per this row's own history. | deferred on measurement (phase 42); revisit only if nulls become dominant |
| E5 — stable symbol registry | **T3.3, landed 2026-08-07: `SymbolRegistry` in `organism.py`, driven entirely from the `EventBoundary` seam.** The problem it closes: a slot index is storage, not identity. Fusion moves a memory off the slot it was born on and leaves that slot to be recycled onto an unrelated pattern, so anything a caller recorded as "slot 7" quietly starts naming a different word — measured, harness §11: in the oversubscribed regime 6 of 9 epoch-A slots came to hold a different word, and in the fusion regime every epoch-A ID that got aliased had its recorded slot re-let to someone else. Design: an ID is minted the first time a slot crosses the boundary (`commit`), moves with its memory or is aliased on fusion (`remap`), and is tombstoned and never reissued on recycling (`invalidate`) — the three notifications the boundary already emitted, so there are **no new call sites in the perceive loop** and perception cannot rewrite identity behind the boundary's back. Consolidation is a *view* (`mem_row` maps a symbol to its row of `org.mem`/`org.Pn`), never an identity mutation, because callers snapshot/restore raw counts around it. Opt-in (`Organism(symbols=True)`) and **observational**: registry-on state is bitwise identical to registry-off on both backends, pinned. The JIT backend maintains the slot→symbol array inside the kernel and journals alias/tombstone events for replay, so both backends emit the same identity events in the same order (`test_fastpath_equivalence.py` §8). Serialized under E3 schema v3. Honest scope: identity is **lineage, not content** — pool-mode refinement keeps adapting a mature trace on an ~1/eta-visit window, so a long-lived memory can come to sit on a different word; that happens at the same rate whether it is reached by ID or by slot (measured 17/25 vs 16/25, harness §11), and the registry does not and must not pretend the mechanism is frozen. Migration proof: `phase33f_eviction_ledger.py` — its script-side birth-era replay (which rested on a prose ordering argument) is reproduced by the registry on **40/40 slots on both backends**, with 171 tombstones = 171 ledger rows and 211 mints = K=40 + 171 rebirths, and every committed number of that phase unchanged. | `organism.py`, `organism_state.py` — done |

## Sequencing

```
E1 harness ──┬─────────────► E2 Numba ──► Phase 27 (5M words) ──► Phase 28 ──► Phase 29
             │                   │
Phase 25 ────┴──► Phase 26 ──────┤
                                 ├──► E3 serialization ──► FORK GATE (Path B repo)
                                 └──► E4 GPU tier (by Phase 27/28)
```

Phase 25 and E1 are independent and both unblock everything else; they go
first. Phase 26 consumes Phase 25's diagnosis. E2 consumes E1's guarantees.
The fork gate consumes E1+E2+E3+Phase 26.

## Consolidation phase — owner posture, 2026-08-07

**Refine and enhance before pushing.** The gate sequence (T1.1–T1.9) is
exhausted and the decision is fully specified with measured floors; frontier
scale work (phases 27/28) has landed. Rather than open new frontier work,
the next block deepens what the organism is measurably differentiated on
and re-baselines performance against the shapes compression (33g) and the
symbol registry (T3.3) changed. Phases 38–42 are specified as targets
T6.1–T6.5 in `AGENT_TARGETS.md`, with phase 31 (perception ablation)
re-scoped and unblocked:

| Phase | Track | Question |
|---|---|---|
| 38 (T6.1) | Continual learning | Does the retention advantage hold on a **boundary-free** stream — the condition gradient arms need and the organism does not? **Run 2026-08-08 (`phase38_task_free_continual.py`, 5 paired seeds, torch 2.13.0+cpu; phase 33c re-run first and reproducing EXACTLY on this host, torch arms included). RESULT: BOTH HEADLINE PREDICTIONS FALSIFIED, and the target's own premise is what failed.** Design isolates the variable across three conditions sharing identical pacing (20 chunks), optimizer-step budget (20×75 = 33c's 5×300) and organism frame count: **A BOUNDARIED** (hard switches, gradient arms told), **B HARD-UNTOLD** (same stream, EWC on a misaligned period-5 schedule, reservoir replay), **C TASK-FREE** (gradual drift + interleaved re-emergence + unannounced novelty). **(a) FALSIFIED on 0/5 seeds, in the opposite direction**: the gradient arms did not degrade without boundaries, they improved enormously — mlp-seq 0.296→0.924 (+0.629), mlp-ewc 0.318→0.910 (+0.592) — while ORGANISM+B moved +0.003. **(b) FALSIFIED with the sign reversed**: recruitment is *lower* at unannounced-novelty chunks (pooled −1.39 slots/chunk, z = −1.53 vs 2000 permutations, p = 0.94, per-seed sign positive 0/5) — under gradual drift novelty is smeared by construction, so there is no spike to find and the organism does not compute the signal EWC is handed. **(c) collapse clause did not fire** (organism ΔACC +0.003 > −0.10). **The decomposition is the finding**, and is why condition B was worth building: A→B removes the boundary SIGNAL with the stream fixed and costs the gradient arms essentially nothing (mlp-seq +0.000, mlp-ewc **+0.056** — it *gains* from a misaligned schedule vs being told the true task ends — mlp-replay −0.005, all organism arms exactly +0.000); B→C unblocks the STREAM and that is where everything moves (mlp-seq +0.629, mlp-ewc +0.536, ORGANISM+B +0.003). So catastrophic forgetting is produced by **sequential blocking**, not by the absence of the annotation: a boundary is merely the label on the blocking, and the label is nearly worthless once the blocking is there. Any drifting, re-emerging stream is *interleaved*, which is the textbook cure — a confound in the target's framing, not in the measurement. **What survives, pinned exact**: ORGANISM+B is bitwise invariant between A and B (max &#124;ΔACC&#124; = 0.0e+00 across seeds) because it consumes no boundary information; mlp-seq and prototypes are exactly invariant too, which is the internal-validity check that the plumbing measures what it claims. Invariance is a real architectural property that is simply not worth much here, since what it is invariant to costs the competition almost nothing either. **Dropped**: "task-free operation is a differentiator" is not claimable from this protocol (N4 edited down, not deleted). In condition C, replay reaches 0.968/0.031 against ORGANISM+B's 0.759/0.140 while storing 200 raw labelled samples. Phase 33c's boundaried verdict is untouched. Opens the question this phase's own design exposes: not "what happens without boundaries" but *how blocked a stream must be before gradient methods need the annotation at all* — a sweep over blocking, left open rather than smuggled in. | `phase38_task_free_continual.py` — done (both predictions falsified; harness §14) |
| 39 (T6.2) | Continual learning | What actually protects old memories? Factorize window / victim rule / decay / headroom against a random-eviction control. **DONE 2026-08-11 — see row 39: (a) NOT SUPPORTED (the victim rule has the same leverage as the window, not less), (b) holds and the era-normalized rule is affirmatively WORSE, (c)-strong CONFIRMED (random eviction loses at every window, 10/10 seeds, exact paired p=0.0020). `p_decay` was UNMEASURABLE on this protocol — recorded as a gap, not a null.** |
| 40 (T6.3) | Logic layer | Confidence-weighted planning, compositional goals, macro-edges, and sparse-P speedup — reasoning depth on the field-free graph. **DONE 2026-08-08 — see row 40: (a) confirmed, (b) split (bitwise yes, 2× crosses at K=800 not 112), (c) the negative fired and its condition is an ORDER, not a graph size.** |
| 41 (T6.4) | Polysemy | Close the loop: detection (phase 12/21/28) drives field splitting (phase 10), scored on **downstream utility at matched capacity**. **DONE 2026-08-08 — the loop closes asymmetrically**: +0.063 held-out next-category accuracy over an exactly matched-capacity control (all 5 seeds positive, 56% of the headroom to the eval-only ceiling), successor distinctness 15/15 vs a same-word null — but generation does NOT improve (both metrics sign-flip) and word-level likelihood loses to the unsplit organism. Synthetic-corpus scope; see row 41. |
| 42 (T6.5) | Engineering | Re-baseline throughput/profile post-33g/T3.3; produce the measured E4 go/no-go. **Run 2026-08-08 — see row 42 below: E4 DEFER, nulls are 22.4% not ≥50%.** |
| 49 (T6.6) | Continual learning | **Opened by the repo owner 2026-08-11**, as the home for the follow-on phase 38 exposed and deliberately refused to smuggle in. Phase 38 decomposed its own result into "boundary signal removed" (worth ~nothing) and "stream unblocked" (worth everything), which means catastrophic forgetting is produced by **sequential blocking** and a boundary is only the annotation on it. So: *how blocked must a stream be before gradient methods need the annotation at all?* Sweep blockedness continuously from i.i.d. interleaved to 33c's hard switches, with the annotation ON and OFF at each level, and report the two crossover curves — where gradient arms begin to collapse, and where the annotation starts being worth more than zero. A monotone curve with no knee is a real outcome and must be pre-registered as such. ORGANISM+B must stay bitwise invariant to the annotation at every level (phase 38's §14 invariance); if it moves, that is a plumbing leak, not a finding. N4 to be rewritten to whatever scope this measures. |
| 31 (T4.1) | Lesion | Perception ablation vs logic survival, now with registry-identity and precision-lesion arms plus bitwise E3 recovery. |

**Category 7 — architecture economics (owner work order, 2026-08-08; phases 43–47 run 2026-08-09).** Targets T7.1–T7.5 in `AGENT_TARGETS.md`. Provenance is load-bearing and is recorded in that file's category header: stages 1–5 came from an exploratory sandbox session that produced no branch and no repo changes, so every sandbox number was treated as an indicative anchor to be re-derived, and four of them did not survive.

| Phase | Track | Question | Outcome |
|---|---|---|---|
| 43 (T7.1) | Cost floor | Does the stored complex vector use its imaginary half, and what does the store cost if it stops paying for one? | **The channel is empty (median residual 2.6e-04) but by PARAMETER REGIME, not construction — the work order's structural account is falsified.** Fork priced at **1.32×** (spend) vs **2.24×** (keep) and handed up unchosen. |
| 44 (T7.2) | Cost floor | Make CSR the default where T6.3 measured a win; re-derive the graph term. | **The graph term was already CSR — the floor moves 0.000×.** Narrow CSR (lossless, guarded) takes it to **2.07× / 1.15×**, still missing the ≤2× fallback, which settles that the fallback is decided by the complex-vs-real term. Density is **not scale-free** (3.8–6.1× over 16× observations). |
| 45 (T7.3) | Sequence | Is there a settling-depth window where the field state carries the prefix? | **Prefix identity is perfectly decodable at EVERY hold** — what the knob moves is its magnitude in similarity units, so path sensitivity is a consumer-threshold question. The window costs 62 words of coverage and the category certificate outright. |
| 46 (T7.4) | Audit | Does "single-pass online" survive when the recipe runs 15 epochs? | **Yes, on all three axes — and coverage is *better* at one pass (327 vs 320).** Multi-pass buys only the category certificate. |
| 47 (T7.5) | Chunking | Three pre-gates before any chunk mechanism is built. | **The raw measure failed its own null and its pre-registered consequence was honored.** Repaired and confirmed on untouched text, the word-level selection rule survives (~1% of cross-entropy); **the category level fails the label-permutation null — T2.4 stays blocked.** |
| 50 (T7.7) | Cost floor | **Owner ruling 2026-08-11 on T7.1's fork: measure before choosing.** Run the (A) `real_phase` store through the **33c ladder** at K=112/160 against the identical c64 arm — paired s=0–4, held out s=5–9, prototype bar recomputed per seed — and report **ACC *and* FORG**, plus KB/ACC-pt with observation counts attached. Phase 43's ≤0.005 behavior bound was measured on the cost-frontier protocol, not the continual ladder, and FORG is where a lossy store should hurt first. The 2026-08-11 re-scope **removed this fork's gate stakes**, so the question is no longer "can (A) clear the gate" but whether (A) holds up where the organism is actually evaluated. Deliver a recommendation this time. Scoping note for the recommendation: T7.6's arms (c) dual time constant and (d) graph order are **fork-independent**, so (A) forfeits only (a)+(b) — and those carry phase 43's R ≈ 0.06 readout-blindness bar and phase 16's 9–56 step collapse. **DONE 2026-08-14 — see row 50: the bound TRANSFERS (worst held-out mean dACC −0.0012, dFORG +0.0034; kill line never approached), every pre-registered prediction held, the probe's "organism above replay at K=160" died on held-out seeds (replay wins 5/5), and the recommendation is (A) — real_phase + narrow CSR as the deployment persistence layout at 39.07 KB/ACC-pt = 1.148× vs (B)'s 2.066×, compute width and default-OFF unchanged.** |
| 48 (T7.6) | Deep arms | Content-dependent phase, phase-aware readout, dual time constant, graph order. | **T7.7 (phase 50) has reported; still owner-decided.** Under the recommended (A), arms (a)+(b) are forfeited (they carry the R ≈ 0.06 readout bar and the 9–56-step collapse) and (c)+(d) survive. Phase number stays reserved and unused until the ruling. |

**Category 8 — agentic language model (owner work order, 2026-08-14).** Targets T8.1–T8.6 in `AGENT_TARGETS.md`. Opened in answer to "what would it take to make this a true agentic LM", and deliberately structured as **gates with kill rules** rather than a build plan, because three of the relevant results are already measured and negative. The governing fact: the logic layer is a **first-order Markov model over slot indices**, phase 40 established that the binding constraint is graph ORDER (dense third-order at K=112 is ~1.4M entries, dead on arrival), and on the phase 34/35 sequence-prediction protocol a **bigram table** held ~0.85–0.87 flat at a fraction of the compute while the organism fell 99%→28% from 50 to 800 words (phase 36's two-stage routing restored ~85%). A first-order transition model over discrete slots is approximately a bigram model, and language modelling is where that ceiling binds hardest. **Owner's standing recommendation, recorded to be argued with rather than rediscovered: the hybrid (T8.6) is the path that ships; T8.1 is the honest test of whether the pure path exists at all.**

| Phase | Track | Question |
|---|---|---|
| 51 (T8.1) | **GATE 0 — context depth** | **The make-or-break; everything downstream assumes it clears.** Can the state carry usable context beyond one step, above a permutation null over shuffled histories at matched marginals? Run the **dual time constant** first (a second field state at slower `g_in`, O(N), ~0.001× of the store, fork-independent — the same arm as T7.6(c), claim one not both); the low-rank/sparse higher-order graph only if that is ambiguous, noting T7.5 already measured the chunk route's category level failing its permutation null. Content-dependent phase and the phase-aware readout are **CLOSED** by the 2026-08-14 fork ruling. **KILL RULE, pre-registered verbatim: if effective context depth does not exceed 2–3 tokens above the null on any surviving mechanism, the architecture is not a language model, Category 8 stops at T8.6, and that is a publishable result rather than a failure.** Report a depth curve, not a number. |
| 52 (T8.2) | GATE 1 — binding | **Blocked on T8.1.** Compositional binding ("X did Y to Z"), required by both language and agency. The architecture's own mechanism (N8) is measured empty, closed by ruling, and carries a **9–56-step collapse** — lifetime, not accuracy, is the binding risk. Any proposal states in advance what it does about lifetime, and is measured against a conventional slot-filling/role-labelling baseline, not against this project's earlier attempts. |
| 53 (T8.3) | GATE 2 — generation | **Blocked on T8.1 + T8.2, and explicitly NOT to be attempted first.** Phase 41 already measured detection-driven splitting making word-level likelihood **worse** than not splitting (−0.081 nats/token) while generation did not improve on either metric; starting here re-derives that negative at full cost. Scored against a conventional LM baseline at matched compute, perplexity reported, or it does not count. |
| 54 (T8.4) | GATE 3 — scale | 10³ → 10⁵ vocabulary, two orders of magnitude past anything measured. N3 (Attractor Crowding Collapse) is named and fixed label-free and phase 36's hierarchical routing holds ~85% to 800 items; whether that survives 100× is a **research** question, not an engineering one. Independent of Gates 0–2, so it can run in parallel and is informative whichever way Gate 0 goes. |
| 55 (T8.5) | GATE 4 — agency | **The architecture's most natural extension, and independent of Gates 0–3.** Model-based planning over a learned transition graph is standard model-based RL and the pieces exist (`phase15_action_conditioned.py`; phases 30/40's `kstep`/`rollout`/`plan`/`plan_reliable`/`plan_visit`). Missing: **goal conditioning** and **outcome-driven credit assignment** — the agent plans but never learns whether the plan worked. Measured against a tabular model-based RL baseline on a task with a real reward signal; report **regret**, not success rate. Should probably run before T8.2/T8.3 regardless of Gate 0. |
| — (T8.6) | **HYBRID — recommended** | No phase number; engineering. Expose the organism as the **persistent episodic memory and world model** for an agent whose language faculty is a conventional LM. Plays to every axis with evidence (continual retention without replay or boundaries, exact resume, a queryable transition graph over the agent's own history) and avoids every measured weakness. **Overlaps T5.1's product fork — coordinate, do not duplicate.** Bar for landing: the memory's contribution is **ablated and measured**, not merely present; an unablated integration is a demo, not a result. |

Two standing lessons, paid for in T1.6/T1.8/T1.9, bind all of them: reseed
every arm **and** hold out fresh seeds for any grid-selected claim; reseed
the baseline on the same split.

## Verification log

- **2026-08-14 (T7.7 / phase 50, `claude/t7-7-fork-ladder`; remote 2-core
  host with ERRATIC wall-clock — identical cells took 1.3–97s, timings are
  not comparable across this log; Python 3.11.15, numpy 2.4.4 / numba 0.67.0 /
  scipy 1.17.1 / sklearn 1.8.0, **torch 2.13.0+cpu present** — the four MLP-era
  version pins differ from both prior verification hosts and are stated per
  the standing rule)**: arrival baseline at `aaf4845` **132/132 ALL PASS both
  backends** (numpy 189.2s, numba 68.6s) before anything was touched. Phase 50
  run in five parts with per-part stdout preserved (`--run bar / k112 / k160 /
  replay / report`; a session reset mid-K=112 cost nothing — the cell cache
  resumed). Anchors re-run and EXACT: bar seed 0 0.872/120 protos/30720 B;
  K=160 c64 argmax seed 0 0.9002 vs the committed 0.900; K=112 held-out floors
  76.10 KB/ACC-pt = 2.236× (c64+CSR) and 70.30 = 2.066× (narrow) reproducing
  phase 44 to the second decimal; rp floors 44.88/39.07 vs 44.83/39.04 with
  the drift inside the pre-registered attribution band (store-mode vs
  uncompressed accuracy construction). After harness §19 landed: **136/136
  ALL PASS both backends** (numpy 103.0s, numba 40.7s). Auxiliary suites not
  re-run (no kernel, readout, or state-schema change; §19 is additive and
  protocol-level). Torch arms measured fresh this time (replay reseeded
  s=0–9); 33c's committed replay 0.913/0.105 was NOT disturbed — the
  per-seed arm is a new measurement, rng provenance disclosed in-run.

- **2026-08-11 (T6.2 / phase 39, `claude/phase-39-retention-mechanism-r7k2qm`;
  remote 4-core host, Python 3.11, numpy 2.4.6 / numba 0.66.0 / scipy 1.17.1 /
  sklearn 1.9.0, **no torch** — the same four versions the owner's second-host
  pass resolved to)**: arrival baseline **119/119 ALL PASS both backends**
  (numpy 112.2s, numba 39.7s) before anything was touched, reproducing the
  committed anchor. After T6.2's `evict_victim` hook and harness §18:
  **132/132 both backends** (numpy 104.4s, numba 35.5s). The hook's DEFAULT
  path was proven **bitwise identical to `origin/main` on both backends** by
  running the same stimulus under both trees and comparing `xi`/`P`/`age`/
  `count`/`evictions`/`z` — all 0.0 — which is why no committed anchor could
  drift. Re-run and EXACT: `phase33c_gate_retest.py` all five reproduction
  anchors (prototypes 0.872; organism evict=0 0.665/0.200; ORGANISM+B
  0.712/0.169; the 40/40 flood; fresh recruits every later task);
  `phase33b_slot_budget.py`'s full window sweep and its (a)–(d) verdict;
  `phase33f_eviction_ledger.py`'s 171 evictions, 123/171 same-task self-churn
  and H3 verdict; `test_fastpath_equivalence.py` green;
  `test_label_readout.py` green including the committed evict=0 anchor ACC
  0.665. E3 round-trip re-verified for all three victim rules (numba
  0.0e+00 exactly; the numpy backend's ~2e-15 on this stimulus was reproduced
  on `origin/main` and predates T6.2). **No torch on this host**, so the
  ladder's four MLP arms were NOT re-measured and are quoted from 33c's
  committed run throughout.

- **2026-08-11 (repo owner, independent re-verification of PR #49 —
  no code changed; remote 4-core host, Python 3.11, numpy 2.4.6 / numba
  0.66.0 / scipy 1.17.1 / sklearn 1.9.0, **no torch**)**: the merged tree
  at `a341cc5` was checked out on a **different host from the one that ran
  Category 7**, the scientific stack installed from `requirements.txt`
  (which resolved to the same four versions), and the full suite re-run
  from cold. **119/119 ALL PASS both backends** (numpy 111.5s, numba
  39.1s), `test_fastpath_equivalence.py` green, `test_label_readout.py`
  green — including its reproduction of the committed evict=0 anchor
  ACC 0.665. Category 7's verification claims therefore reproduce on a
  second host rather than resting on the running agent's own report.
  Timings are faster than Category 7's (174.3s/54.5s) and that is host
  speed, not a behavior change: every check is inside its band and the
  bitwise sections are bitwise. Scope: this re-verifies the **harness**,
  not the phase scripts' committed runs — no phase 43–47 script was
  re-executed end-to-end here, and with no torch on this host every torch
  arm on record remains a committed number.

- **2026-08-09 (Category 7 / T7.1–T7.5, architecture economics; remote
  4-core host, Python 3.11.15, numpy 2.4.6 / numba 0.66.0 / scipy 1.17.1 /
  sklearn 1.9.0; **no torch on this host**, so every torch arm on record is
  a committed number here and is quoted as such)**: baseline on arrival at
  `c9240f0`, E1 `regression_harness.py` **92/92 ALL PASS both backends**
  (numpy 174.8s, numba 57.6s) before anything was touched.
- **2026-08-09 (T7.1, phase 43)**: after §15, **102/102 both backends**
  (numpy 173.0s, numba 52.0s). 33h's floor arm re-measured end-to-end on
  this host and the committed anchors reproduce EXACTLY — c64+CSR
  **76.10 KB/ACC-pt = 2.236×** on held-out seeds 5–9 against 33h's 76.1 /
  2.24×, with the term decomposition landing at memory 1.981 + graph 0.356
  = 2.337 × 0.958. The eight-book phase-20 corpus was re-fetched and all
  eight Gutenberg titles verified against pinned ebook ids.
- **2026-08-09 (T7.2–T7.5, phases 44–47, final tree)**: after §16 and §17,
  **119/119 ALL PASS both backends** (numpy 174.3s, numba 54.5s);
  `test_fastpath_equivalence.py` green (a kernel-adjacent default changed:
  `next_hops` now dispatches to the CSR port above `SPARSE_MIN_K`);
  `test_label_readout.py` green. Every byte anchor is untouched by
  construction — both new compression levers (`real_phase`, `p_narrow`) are
  default-OFF, and the `next_hops` dispatch is bitwise on integer counts
  and inert at every K the committed benchmarks reason at (phases 30/40 run
  at K ≤ 60, crossover measured at K=500, threshold set at 800). Corpus
  work re-fetched and title-verified on every load. **Process miss on
  record**: phase 45's predictions were authored before its first run but
  committed after it; phases 43, 44, 46 and 47 pre-registered in the
  required order, and phase 47's post-hoc repair (P7) was itself committed
  before the run that tested it.

- **2026-08-08 (T6.1 phase 38, task-free continual learning; remote 4-core
  host, Python 3.11.15, numpy 2.4.6 / numba 0.66.0 / scipy 1.17.1 /
  sklearn 1.9.0, and — a change from every prior entry — **torch
  2.13.0+cpu IS installed here**, the same version 33c's committed run
  used)**: baseline before any change, E1 `regression_harness.py` **84/84
  ALL PASS both backends** (numpy 155.3s, numba 49.2s). After, with the new
  section 14: **92/92 both backends** (numpy 171.8s, numba 57.6s);
  `test_fastpath_equivalence.py` green; `test_label_readout.py` green.
  **`phase33c_gate_retest.py` re-run end-to-end on this host BEFORE
  designing phase 38, and every committed anchor reproduced EXACTLY,
  torch arms included** — prototypes 0.872, organism evict=0 0.665/0.200
  with the 40/40 flood, ORGANISM+B 0.712/0.169 with fresh recruits every
  later task, mlp-seq 0.323/0.842, mlp-ewc 0.322/0.844, mlp-replay
  0.913/0.105, mlp-joint* 0.974/0.000. That reproduction is what makes the
  phase-38 comparison trustworthy: the boundaried control is not a claim
  from the archive, it was measured here. `harness_probe` returns
  **bit-identical values on both backends** (checked directly: acc_A
  0.6573242034704642, acc_C 0.738481051781757, localization z
  −0.5980373901557183 on both), so section 14's bands are not hiding a
  backend split. No library file was touched by this target — the phase is
  an additive script plus its harness section, so `organism.py`,
  `fastpath.py`, `organism_state.py` and `label_readout.py` are unchanged
  and no committed number can move. Pre-registration was committed and
  pushed before the run that tested it; no grid was searched, so the
  held-out-seed rule is not triggered; all arms reseeded s=0–4 with every
  arm in a seed seeing the identical chunk sequence. One pre-run protocol
  correction, made on first principles before any measurement and recorded
  rather than silently applied: chunk 0 is excluded from the
  novelty-localization analysis, because the organism recruits there for
  having an empty store rather than for detecting anything, and counting it
  would have inflated prediction (b) with a trivial effect — it was
  excluded *before* the prediction was found to fail in the opposite
  direction anyway. Not re-verified: nothing on the language side (phases
  19–28, 37) and no corpus-tier run, none of which this target touches.

- **2026-08-08 (post-merge correction, T6.4 phase 41; same host/versions as
  the T6.4 entry below)**: PR #43 and PR #44 both authored a `section_12` and
  merged the same day, #44 first; the merge resolution correctly gave §12 to
  phase 40 and renumbered phase 41 to **§13**, and the E1 row, AGENT_TARGETS
  header, FABLE_HANDOFF and the harness docstring all landed consistent at
  **84 checks**. Two things the renumber missed, found by running main rather
  than assuming it: (1) `section_13_detection_driven_split` still *printed*
  its old `(12)` label, so a harness run on main emitted **two sections
  labelled (12) and none labelled (13)** — output contradicting every
  document; (2) five cross-references still pointed phase-41 content at §12
  (ROADMAP row 41, NOVELTY ×2, AGENT_TARGETS ×2), which on main resolve to
  phase 40's logic-layer section. Both corrected here. **Main was verified
  green before and after: `regression_harness.py` 84/84 ALL PASS on BOTH
  backends (numpy 93.6s / numba 26.2s at `eaf2216`), with §13's values
  unchanged and identical across backends** — detection 3/3, distinctness
  3/3, capacity gap 0.0000 exact, next-category gain over matched capacity
  +0.0485. The correction is label-and-prose only; no measured number moved.

- **2026-08-08 (merge review for PRs #45, #44, #43 — the whole Category-6
  batch; remote 4-core host, Python 3.11.15, numpy 2.4.6 / numba 0.66.0 /
  scipy 1.17.1 / sklearn 1.9.0; torch NOT installed, so the phase-33
  ladder's torch arms rest on 33c's committed values)**: the three PRs were
  authored concurrently against the same base (`14054db`) and each verified
  itself there, so none of their merged-tree runs was against a tree
  containing the other two. Re-verified here in merge order, each on the
  tree actually being merged. Baseline on arrival at `14054db`: **65/65 both
  backends** (numpy 132.3s, numba 22.8s), matching the recorded state.
  After #45: **65/65 both backends** (numpy 124.7s, numba 17.3s) — it adds
  no harness section. After #44: **77/77 both backends** (numpy 126.4s,
  numba 17.9s). After #43: **84/84 both backends** (numpy 152.3s, numba
  46.7s). `test_fastpath_equivalence.py` and `test_label_readout.py` green
  at every step. All 84 check lines are identical across backends except
  the E3 compressed-file-size ratio (1.3986 numpy vs 1.3994 numba), an
  existing tolerance-band check rather than a behavior difference.
  **The section_12 collision #43 self-flagged was real and was resolved on
  merge**: #44 keeps `section_12_logic_depth`, #43 renumbered to
  `section_13_detection_driven_split`, 84 = 65 + 12 + 7. The renumber is a
  label — every anchor in that section reproduced (dual words detected 3/3,
  control false positives 0, sense-slots recruited 3/3, unsplit max slots
  per word 1.0 exact, distinctness vs same-word null 3/3, split-vs-matched
  live-slot gap 0.0000 exact, held-out next-category gain +0.0485), as did
  #44's twelve (`next_hops` pre- vs post-refactor delta 0.0000, sparse
  next_hops/rollout bitwise, first-order macro gain forced 0.0000).
  **Correction, from the post-merge entry above**: "the renumber is a label"
  was right about the measurements and incomplete about the labels — the
  merge renamed the function but left its `print()` at `(12)` and missed
  five cross-references, so main briefly emitted two `(12)` sections. #47
  fixed both. The lesson is the one that entry states: the renumber was
  performed by the merge, so the renamed section had never actually
  executed in that configuration until it was on main, and reading the
  diff would not have caught it — only running it did.
  Remaining conflicts were doc adjacency, not disagreement in measurement:
  the E1/E2 table rows, the Category-6 table, the verification log itself,
  and the E1 check-count lines in `FABLE_HANDOFF.md` / `AGENT_TARGETS.md`.
  Two process notes recorded rather than smoothed over: (i) none of the
  three PRs pushed its `[claimed: …]` slot to main before starting, which is
  why three sessions ran concurrently without seeing each other and two took
  the same section number — protocol rule 2 is what would have prevented it;
  (ii) the plain-language track (`LAB_NOTES_PLAIN.md`) landed on main
  mid-merge, so #44 and #45 shipped four documentation surfaces rather than
  five — their chapters were written in this commit, not by the authoring
  sessions. Not re-verified: the ladder's torch arms, and no phase script
  was re-executed here (#45 had re-run phase 27 end-to-end with every
  committed anchor exact).

- **2026-08-08 (T6.4 phase 41, detection-driven sense splitting; remote host,
  4 cores, Python 3.11.15, numpy 2.4.6 / numba 0.66.0 / scipy 1.17.1 /
  sklearn 1.9.0; torch NOT installed, so no ladder arm was re-run and 33c's
  committed torch values stand)**: baseline on arrival, before any change,
  E1 `regression_harness.py` ALL PASS both backends — **65 checks**, numpy
  76.0s / numba 14.8s — and `test_fastpath_equivalence.py` green including
  its narrowed-store §7 and symbol-registry §8. After: ALL PASS both backends
  with the new section — **72 checks**, numpy 82.9s / numba 31.2s on the
  branch, and re-run on the MERGED tree (origin/main at 14054db) at numpy
  93.0s / numba 30.7s, 72/72 both times, plus
  `test_fastpath_equivalence.py` 27/27 and `test_label_readout.py` 24/24 on
  the merged tree — and
  the section's measured values are IDENTICAL on the two backends (detection
  3/3, distinctness 3/3, capacity gap 0.0000 exact, next-category gain over
  matched capacity +0.0485 on both). No library file was touched: the phase
  is an additive script plus its harness section, so `organism.py`,
  `fastpath.py`, `organism_state.py` and `label_readout.py` are unchanged and
  their equivalence/round-trip guarantees are untouched. Anchor reproduction:
  `phase10_context_primed_settling.py` re-run end-to-end at its committed
  best config (thr=0.65, g_mem=0.0, seeds 0/1/2) reproduces every committed
  number exactly — 3/3 both-roles-covered, 0/3 exact 2-way splits, 10/10
  dual-word slots grammatically correct on the successor readout, 60/60/61
  total slots, verdict STABLE across seeds. Phase 41's own committed run is
  the 5-seed paired sweep recorded in row 41.

- **2026-08-08 (T6.3 logic-layer depth, phase 40, remote host, numpy 2.4.6 /
  numba 0.66 / sklearn 1.9; torch NOT installed, so the ladder's torch arms
  were not re-run — 33c's committed values stand)**: baseline before the
  change, E1 `regression_harness.py` ALL PASS both backends (numpy 97.9s,
  numba 23.0s, 65 checks). After: ALL PASS both backends with the new
  section 12 — **77 checks**, numpy 89.1s / numba 14.0s;
  `test_fastpath_equivalence.py` green (including its registry sections);
  `test_label_readout.py` green; `organism_state.py` smoke green for v3
  round-trip, v1/v2 backward load and compressed stores.
  **Phase-30 anchors re-checked directly, before and after the
  shared-Dijkstra refactor, on BOTH backends**: on the numpy reference
  backend `phase30_symbolic_reasoning.py` reproduces its committed row
  exactly — k-step corr 0.999/0.998/0.995 vs permutation nulls
  0.453/0.486/0.444, rollout bigram corr 0.999 vs field recall 0.939, and
  directed recall 1.8 hops at 100% vs undirected 23.2 — and the after-run
  differs from the before-run only in wall clock. On numba the graph-side
  anchors are identical and the field-side recall reads 0.942 / 13 675 hops
  vs numpy's 0.939 / 13 689: the known cross-backend recall difference (E2's
  tolerance-band rule), not a phase-40 effect, and present in the before-run
  too. The row's "~20×" rollout-vs-field ratio is a pre-E2 numpy number and
  measures 17–18× on numpy here, 2× on numba — the field side got faster,
  the graph side did not change. Harness §12 additionally pins the refactor
  bitwise against the pre-refactor body, which is the guard that matters
  going forward. One check was rewritten during the run rather than
  weakened: the harness caught `confidence(mode='laplace')` exceeding the
  point estimate, which is correct behavior for a posterior mean and wrong
  only in the docstring that called both modes lower bounds — the bound
  invariant is now pinned for `wilson` alone and the docstring says why.
  **Re-verified on the MERGED tree** (origin/main's NOVELTY.md claims
  register + standing operating protocol merged in, per protocol rule 16):
  `regression_harness.py` **77/77 both backends** (numpy 131.0s, numba
  19.5s — the numpy wall clock is container load, not a behavior change),
  `test_fastpath_equivalence.py` and `test_label_readout.py` green. The
  merge touched only `AGENT_TARGETS.md` (text) and added `NOVELTY.md`; no
  mechanism file was on both sides. NOVELTY N2 updated per rule 14,
  including a correction it needed: N2 described phase 30's planner as
  "Dijkstra over hop counts" when it always minimized −log transition
  probability, which is why phase 40's robust win is over a hop-count
  baseline and only a thin-evidence correction against phase 30's own.

- **2026-08-08 (T6.5 performance re-baseline, phase 42; fresh remote 4-core
  Linux host, numpy 2.4.6 / numba 0.66.0 / scipy 1.17.1 / sklearn 1.9; torch
  NOT installed, so the phase-33 ladder's torch arms rest on 33c's committed
  values)**: baseline before any work, E1 `regression_harness.py` **65/65 ALL
  PASS on both backends** (numpy 101.0s, numba 19.2s cold / 12.9s warm). No
  library file was touched by this target, so no re-verification was owed —
  the harness was nevertheless re-run per-section on both backends as a
  measurement (numpy 87.2s, numba 13.2s in-process) and stayed green.
  `phase27_5m_word_scale_run.py` **re-run end-to-end on a fresh 42-book
  fetch** (all 42 Gutenberg titles verified against their live headers):
  every committed anchor reproduced exactly — 5 224 034 raw / 3 577 289
  in-vocab tokens / V=354 at MIN_COUNT=1500, 598 memories, coverage 328/354,
  distinctness 0.491 at k=2 and 0.242 at k=3, MI z 61/50/36/21/17,
  'right' n=3340 gain 0.008 vs null p99 0.001, 205/354 words above their own
  nulls, stage D bigram hit 0.787 vs 0.586 random — with the same
  pre-registered outcome (P1 not confirmed, P2 confirmed). Total 1184s vs
  the committed run's 1281s. Additionally run on this host for the
  same-host A/B arms: `regression_harness.py` at commit `15cfa65` (27-check
  era) on both backends, ALL PASS (numpy 82.2s, numba 12.4–13.5s warm), and
  the phase-23-shape perceive benchmark at `7e0b639~1` and `92e9cc9~1`.
  No kernel or readout path was modified, but both tier-0 suites were run
  anyway and are green: `test_fastpath_equivalence.py` (11s, including the
  narrowed-store §7 and symbol-registry §8, registry on-vs-off at 0.000e+00
  on both backends) and `test_label_readout.py` (2s). Not re-verified: the
  phase-33 ladder's torch arms (no torch on this host) and the phase-35
  800-word row (memory-limited), both unchanged by this target.

- **2026-08-07 (T3.3 stable symbol registry, remote host, numpy 2.4.6 /
  numba 0.66 / sklearn 1.9; torch NOT installed, so the ladder's torch arms
  were not re-run — 33c's committed values stand)**: baseline before the
  change, E1 `regression_harness.py` ALL PASS both backends (numpy 91.2s,
  numba 19.0s). After: ALL PASS both backends with the new section 11 —
  **65 checks**, numpy 90.9s / numba 15.8s; `test_fastpath_equivalence.py`
  green including its new section 8 (registry cross-backend identity, and
  registry-on vs registry-off bitwise at 0.0e+00 on both backends);
  `test_label_readout.py` green; `organism_state.py` smoke green for v3
  round-trip (exact), v1 and v2 backward load (bitwise), and compressed
  stores. `phase33f_eviction_ledger.py` re-run end-to-end: all five
  original gates reproduce exactly (ACC/FORG 0.712/0.169 and 0.665/0.200,
  census [9,7,4,6,14], ledger on/off bitwise, numpy/numba ledger identical
  at 171 rows), the four new T3.3 migration gates pass, and the entire rest
  of its output is byte-identical to the pre-migration run.

- **2026-08-04 (onboarding pass, fresh clone, independent Linux host,
  numpy 2.2.6 / numba 0.66)**: E1 `regression_harness.py` 27/27 PASS on
  both backends (numpy 59.8s, numba 10.2s); `test_fastpath_equivalence.py`
  all green (state agreement ~1e-15, recall sequences exact);
  `e2_benchmark.py` at phase-23 shape: numba 67.3K frames/s, 1.21 min full
  perceive (exceeds the pinned 58K/1.40 min; speedup ratio 6.0× on this
  host — the 13.0× is hardware-relative, pin the absolute numba number);
  phase 35 rows reproduced exactly at 50/100/200/400 words, 800-word row
  not re-run (host OOM at K≈960) — that datapoint rests on the committed
  run. Not re-verified: corpus-scale phases (20–28; Gutenberg fetch out of
  scope) and the phase-33 ladder (torch baselines not installed).
- **2026-08-07 (T1.9 cost frontier, fresh remote host, numpy 2.4.6 /
  numba 0.66 / sklearn 1.9; torch NOT installed, so the ladder's torch
  arms were not re-run — 33c's committed replay/SGD/EWC/joint values
  stand)**: E1 `regression_harness.py` **45/45 PASS on both backends**
  (numpy 93.8s / 81.1s, numba 19.0s / 13.2s — run before and after the
  phase), `test_fastpath_equivalence.py` green incl. the narrowed-store
  §7, `test_label_readout.py` green. Phase 33h touches NO library file
  (protocol-level only; lever (iii)'s fold lives in the phase script and
  uses `LabelEvidenceReadout`'s public remap/invalidate lifecycle), and
  the run is deterministic — two independent invocations printed
  byte-identical sections. In-phase anchors all EXACT: the prototype bar
  (0.872 / 30.7KB / 120 protos), 33d's ACC ladder at K=40/80/120/160
  (0.712/0.837/0.854/0.900), 33g's 96.4KB compressed K=160 store, and
  33h's live-slot byte accounting verified equal to 33g's allocated
  accounting on every non-folding arm. Not re-verified here: corpus-scale
  phases (20–28) and the torch baselines.
- **2026-08-07 (merge review of the four open PRs — #42/T1.9, #40/T2.1,
  #41/T3.3, and #38 closed as a duplicate of #40; reviewer host, numpy
  2.4.6 / numba 0.66 / scipy 1.17, torch NOT installed)**: each PR
  re-verified on its own tree (harness ALL PASS both backends: 45/45 for
  #42/#40/#38, 65/65 for #41) and then again on the MERGED tree —
  `regression_harness.py` **65/65 PASS both backends** (numpy 72.8s,
  numba 27.9s), `test_fastpath_equivalence.py` green incl. §7 and T3.3's
  new §8 (registry on-vs-off drift 0.0e+00 on both backends),
  `test_label_readout.py` green, `organism_state.py` smoke green (v3
  round-trip exact, v1 and v2 backward load, compressed stores). Claims
  reproduced independently rather than taken on trust: phase 33h re-run
  end-to-end (490s) and every headline number matched — held-out floor
  76.1 KB/ACC-pt = 2.24× at K=112+calib-b8, the knee at K=24/23.9, and
  K=96's out-of-sample sign-flip (min delta −0.004) correctly recorded but
  not banked; `phase33f_eviction_ledger.py` re-run on the T3.3 tree with
  all nine gates passing (0.712/0.169, 0.665/0.200, census [9,7,4,6,14],
  171 ledger rows = 171 tombstones, 211 mints, 40/40 lineage agreement);
  33d's ACC anchors (0.712 at K=40, 0.854 at K=120) and the prototype bar
  (0.872 / 30.7KB / 120) re-checked ON the merged tree, so T3.3's
  organism.py/fastpath.py edits are confirmed not to perturb the phase-33
  protocol. Phase 27's corpus run was NOT re-executed (5M-word fetch), but
  all 42 of its committed ebook-id → title expectations were checked
  against live Gutenberg headers (42/42 match), and PRs #38 and #40
  completed that target independently on two hosts and reported identical
  numbers (k=2, distinctness 0.491, z=61, 'right' n=3340 gain 0.008 vs p99
  0.001, 205/354) — an unplanned but genuine replication.
- **2026-08-06 (T1.8 representation-width compression, remote host,
  numpy 2.4.6 / numba 0.66 / sklearn 1.9; torch NOT installed, so the
  ladder's torch arms were not re-run — 33c's committed values stand)**:
  E1 `regression_harness.py` ALL PASS on both backends with the new
  section 10 (numpy 81.2s, numba 13.7s); `test_fastpath_equivalence.py`
  green including the new section 7 (narrowed-store cross-backend
  equivalence: state diff 5.0e-16, transition graph and slot occupancy
  identical, store dtype preserved); `test_label_readout.py` green; E3
  round-trips green for v2 uncompressed (bitwise), v1 backward load
  (bitwise), and compressed (P bitwise, xi at float32 eps); phase 33g
  committed run reproduced every 33d budget-arm anchor EXACTLY
  (0.712/0.735/0.837/0.854/0.900, FORG 0.169 at K=40 and 0.033 at K=160)
  and the prototype bar at 0.872/30.7KB/120 protos.
- **2026-08-05 (T1.4 gate re-test, fresh remote host, numpy 2.4.6 /
  numba 0.66 / torch 2.13-cpu)**: E1 31/31 PASS both backends (numba
  29.0s, numpy 112.2s); `test_label_readout.py` green; the previously
  unverified phase-33 ladder reproduced WITH the torch baselines
  (SGD 0.323, EWC 0.322, replay 0.913, joint 0.974, prototypes 0.872,
  organism 0.665/0.200 — numpy anchors exact); phase 33c committed run:
  all reproduction anchors REPRODUCED (see row 33c).

- **2026-08-05 (T1.7 eviction-ledger diagnostic, remote host, numpy 2.4.6 /
  numba 0.66)**: post-instrumentation (the `evict_debug` hook in
  organism.py/fastpath.py), E1 31/31 PASS both backends (numba 10.9s,
  numpy 61.7s), `test_fastpath_equivalence.py` all green; phase 33e's
  gate: 33c anchors reproduced exactly (0.712/0.169, census [9,7,4,6,14],
  baseline 0.665/0.200), ledger on/off bitwise identical, numpy and
  numba ledgers identical row-for-row (171 rows). Torch arms not re-run
  (organism-only diagnostic; torch not needed).

- **2026-08-06 (T2.2 phase 37, remote host, numpy 2.4.6 / numba 0.66)**:
  E1 31/31 PASS both backends (numpy 81.5s, numba 15.5s) before AND after
  `phase37_vn_calibration.py` landed (additive script only, no
  organism.py/fastpath.py changes); `phase37_vn_calibration.py` itself:
  Part A synthetic gate PASS all sigmas, Part B real corpus (8-book
  Gutenberg fetch, 408 464 in-vocab tokens) qcal coverage 275/395 (alpha=0.5)
  and 289/395 (alpha=0.0) — see row 37.

## What the external reviews got right (kept) and wrong (dropped)

Kept: vectorize so the corpus can scale (E2), external baselines (a small
gradient-trained model on the same corpus as an *oracle ceiling*, phase-9B
style — attach to Phase 27), demonstrate downstream utility of polysemy
detection (Phase 28 + wiring into generation), regression/testing discipline
(E1), hierarchy/compositionality above the category FSM (Phase 29).

Dropped, with reasons recorded so they aren't re-derived: SSM/Mamba core
(replaces the research question; gradient-trained), MoE gating (routing here
is information-limited, not cost-limited — phase 18 measured it), EWC/replay
(solves a failure mode this architecture measurably doesn't have),
differentiable external memory (a different project), post-hoc consolidation
clustering (re-opens phase 9's closed negative result: re-sorting provably
inherits online mixing).
